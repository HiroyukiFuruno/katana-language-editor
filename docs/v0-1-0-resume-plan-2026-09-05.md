# KLE v0.1.0 再開台帳 (2026-09-05)

## 継続実装 (2026-09-05)

### カーソル下流の直接Call Hierarchyとmodifier参照

- 実LSP prepareCallHierarchy/outgoingCallsを接続し、3rootから19target record/
  33callsiteを採取した。egui内targetとepaintの8targetは固定Cargo.lockのarchive、
  tar member、展開済みbytesが一致する場合だけsource identityを認証する。件数は機能数/
  網羅率ではなく、target名の意味解決も行わない。
- prepared rootのidentifier/URI/Function又はMethod型、UTF-16範囲、caller包含、
  空fromRanges拒否、range/excerptソート対応を検査した。LF/CRLF/最終空行/日本語/
  VS16/非BMPとsurrogate分断、null/empty/malformedの区別を回帰化した。
  semantic_complete=false、transitive_traversal_performed=falseは維持する。
- mainレビューで許可範囲外の既存egui helperへの追加を新診断側へ戻し、既存helperに
  今回の差分を残さなかった。siblingやregistry cacheは編集していない。
- 最新実runnerは`target/acceptance/cursor-calls-epaint-lsp-20260906-r1/report.json`、
  errors[]、engineexit0。epaint source identity 8件を記録した。r2再現性と全report SHA256は
  次の実行で確認する。
- modifier参照6ケースを追加。固定egui入力参照は38成功。Alt単語/command本文境界と
  Shift固定端、macOS限定mac_cmd行境界を確認した。Ctrl系や全Unicode単語規則、
  KUC比較・物理OS入力の成功へは昇格しない。
- main fmt/strict Clippy/AST成功、全体Rust607成功/1失敗、exit101。唯一の失敗は
  Continuation(Search(CloseNotApplied))。doctest別実行41成功、最終Python診断32成功。
  `/tmp/kle-cursor-calls-gates-20260905-r1.log`、
  `/tmp/kle-cursor-calls-diagnostic-20260905-r2.log`、
  `/tmp/kle-cursor-calls-doctests-20260905-r1.log`。coverage再計測は未実施。
- KUC #40はOpenを再確認。固定KatanAはclean。全closure/full Storybook/三OS/公開/
  cleanupは未完了。2担当はclose済み。stage/commit/pushなし。

### カーソル移動の限定定義解決と実行参照

- `text.cursor-navigation-family`の固定ソース分岐と未解決のGalley/word/AccessKit/OS
  境界を`docs/v0-1-0-cursor-navigation-reference.md`へ具体化し、要件書から参照した。
  source SHAは既存の認証済み114file診断に一致する。
- builderからCCursorRange::on_event、on_key_press、2箇所のmove_single_cursorを実LSPで
  解決した。手書きbrace解析を撤去し、documentSymbolの構造化rangeを採用。
  method/impl/query包含、一意性、識別子/URI、archive/source一致、UTF-16を検査する。
- main実runner r2はerrors[]、engineexit0、4辺を採取した。
  `target/acceptance/cursor-lsp-20260905-r2/report.json` SHA256は
  b09c84dba2df6e11a3d411b6b5ab2df4f732e5ae5a4652a20811f2785d43fcc9。
  semantic_complete=false / execution_performed=falseを維持し、native成功とはしない。
- 実Context/TextEdit/Stringによる8入力参照を追加した。mainのレビューで、日本語を
  跨がず選択collapseだけを見たケースを補強し、星/VS16/日本語位置を各frameでassert。
  runtime stateのフレーム間再注入、OS偽装、grapheme要件の引下げはしていない。
- main最終fmt/strict Clippy/AST成功、Python23成功、Rust601成功/1失敗、exit101。
  失敗は引き続きContinuation(Search(CloseNotApplied))。doctestは別実行41成功。
  `/tmp/kle-cursor-gates-20260905-r2.log`と`/tmp/kle-cursor-doctests-20260905-r1.log`。
  initial gateで発見した分割後の未使用定数は除去済み。coverage再計測は未実施。
- 公開gateは正式source-closure artifacts不在でexit1。固定KatanAはclean。
  全navigation分岐、KUC比較、三OS/AX/native入力、公開とcleanupは未完了。
  Luna/Terra担当はclose済み、sibling編集、stage/commit/pushなし。

### 外部仕様解析の完了誤判定とsource採取漏れ

- canonicalとdiagnostic共通producerに未採取の推移的semantic closure理由を保持した。
  5入口・lock・呼出しが揃ってもcompleteへ昇格しない。認証済みRust fileのSHAは
  入口定義の有無で省略しない。実固定KatanA診断では外部egui採取が3から114fileへ
  増え、cursor_range/text_buffer等も保持した。symbolsは5、invocationは14のまま。
- 公開validatorの外部依存未検査を修正した。egui欠落、package重複、未完了、未解決、
  replacement leaf不在、空/空白のmetadata/source/symbol/span/invocation、fingerprint
  不一致を拒否する。RC/KLE公開前/採用後の実validator経路を3回帰で確認した。
  これは必要条件の検査であり、semantic graphと実行leafの完全な照合は未実装である。
- mainはworkerのtype complexity、expect、magic numberを修正し、空白変異fixtureの
  fingerprint再計算を訂正した。変異後もfingerprint検査が成功した上で当該fieldの
  検査が拒否することを確認し、テストの別原因による成功を防いだ。2担当はclose済み。
- main最終fmt/strict Clippy/AST成功、Python17成功、Rust593成功/1失敗、exit101。
  唯一の失敗はStorybookのContinuation(Search(CloseNotApplied))、KUC #44。
  doctestは別実行で41成功。ログは/tmp/kle-external-closure-gates-20260905-r4.log、
  /tmp/kle-external-closure-doctests-20260905-r1.log。coverageは再計測していない。
- 診断r27/r28は途中再buildのgenerator SHA差で不一致。確定後r29/r30はJSON bytes一致、
  SHA256 c99cfefbe290a67670806d7b10394859a44d7098f5873827b3f6a05b5fb357c2。
  diagnostic_only=true、passing_claimed=false、execution_performed=falseを保持する。
- cargo outdated --workspaceは更新候補なし。KUC #40/#43/#44はOpenを再確認。
  公開gateは正式source-closure artifact不足でexit1。固定KatanAはclean。
  全semantic解析・full Storybook・三OS実入力・公開・cleanupは未完了。
  sibling編集、stage/commit/push/publicationは実施していない。

### 色診断のliteral/hintとHSL検出漏れ

- Violationにliteral/hintを追加し、color ruleは同じSourceFileのAST spanからconstant、
  引数を含むconstructor、string literalの原文を保持する。既存newはNone、他ruleの
  report先頭行とNone時の出力は完全一致で維持。追加値はescapeした補足行へ出す。
- レビューでcolumnのbyte直接加算、改行終端の列受理、empty spanを修正した。
  日本語/VS16付きstarが同じ行で前置されたケースのcolumn29と原文を確認し、CRLF、
  raw string、複数行callも検証。範囲外ではDiagnosticSpanを返し、先に収集した違反も
  部分成功として返さない。source helperのoffsetはprivateのままchild testで検査する。
  任意の同長別sourceとASTの同一性まで証明するものではなく、productionの同一
  SourceFile構築契約を前提とする。元sourceをplaceholder/token再整形で代替しない。
- 明示specのhsl/hsla検出漏れも修正し、既存RGBと同じtrim/case規則へ追加した。
  mainでCSS判定2件と診断5件、実SourceFile scope経路のliteral/hintも検証した。
- main最終fmt/strict Clippy/AST成功、Python17成功、Rust573成功/1失敗、exit101。
  残る失敗は検索CloseNotApplied、Storybook40成功/1失敗。
  `/tmp/kle-color-diagnostics-gates-20260905-r1.log`。workspace doctest41成功、
  `/tmp/kle-color-diagnostics-doctests-20260905-r1.log`。旧coverage値は流用しない。
- neutral consumer成功、公開gateは正式source-closure証跡不在でexit1。
  KUC #44は再照会でもOpen。UI/全leaf/三OS/正式公開/cleanupは未完了であり、今回の
  lint診断要件の実装をeditor完成とは扱わない。Luna/Terra担当はclose済み。
  stage/commit/push/sibling編集なし、依存追加なし。

### 旧theme DTO除去と色lint回避の撤去

- Rgba/ColorTokens/ColorTokensInput/Theme/EditorThemeとroot/types exportを除去した。
  theme.rs一ファイルを削除し、代替DTOやKUC型の再exportは追加していない。
  各型・公開パスの10独立compile-failとstyle定義所有権の5回帰を追加した。
- 色lintの案内をKUC/host境界へ同期する調査で、file-level color allowとtests/preset
  path名による除外、attribute ruleのcolor allow例外を確認した。事前設計を追記し、
  これらを撤去。literal検出と明示cfg(test) visitor処理は維持した。
  owned temporary workspaceの実SourceFile読込で、祖先/path名/inner allowでproductionが
  抜けないことを確認。対象外の走査済み2fileも非空fixtureで別検証した。
  属性6回帰と色5回帰で、outer/inner/qualified/cfg_attr allow、通常属性、literalと
  非literal、明示cfg(test)を検査した。新たな例外は追加していない。
- main全体r1はfmt/strict Clippy/AST成功、Python17成功、Rust559成功/1失敗、exit101。
  検索CloseNotAppliedのみが残り、Storybook40成功/1失敗。
  `/tmp/kle-theme-ownership-gates-20260905-r1.log`。workspace doctest41成功、
  `/tmp/kle-theme-ownership-doctests-20260905-r1.log`。worker途中の別module作成中の失敗は
  合格根拠に使わず、全変更後のmain結果を使用した。旧coverage値は流用しない。
- neutral consumer成功。公開gateは正式source-closure証跡不在でexit1。
  Themeの除去やlint成功を色role/theme切替/最終pixelの互換証明にしない。
  全leaf、三OS、KUC #40/#43/#44、正式公開とcleanupは未完了。3担当はclose済み。
  stage/commit/push/sibling編集なし。この時点で残ったliteral/hint診断要件は上記工程で
  実装・検証した。案内文修正だけで診断schemaを完成扱いにはしていない。

### generic presentation config入口の除去

- EditorConfig/Inputのtheme/strings/localeとconstructor転送を除去した。
  呼出しがなくmainのmodにも未接続だったStorybookConfigFactory一ファイルを削除し、
  KLE local palette変換/英語preset/仮clipboard storeを別の場所へ移さなかった。
  現行KUC scenario session/root/入力/描画の実装は変更していない。
- 各struct・各fieldへのアクセスを6独立compile-failで拒否し、残るconstructorの正例を
  追加した。既存ASTのfield検査を6名へ拡張し、nested/private/rawと無関係型/literalの
  回帰を追加。型そのもの（Theme/Strings/Locale等）と残るconfigのclipboard/highlighter
  責務は別工程であり、UI所有権移行全体は未完了。
- theming/i18n specの旧KLE config DI/KDV preset必須記述をKUC/host境界へ修正した。
  semantic color role、翻訳キーの網羅性・型による欠落拒否、英語表示、RTL/gutter/caret/
  AccessKitと拡張性の要件は残す。API除去をその実動作の証明とはしない。
- main r1は整形差分で失敗し、just fmt後r2を実行。fmt/strict Clippy/AST成功、
  Python17成功、Rust545成功/1失敗でexit101。検索CloseNotAppliedのみが残り、
  Storybook40成功/1失敗。`/tmp/kle-presentation-config-gates-20260905-r2.log`。
  workspace doctest31成功、`/tmp/kle-presentation-config-doctests-20260905-r1.log`。
- native smokeは実ウィンドウ2frameでexit0、neutral consumer成功でegui依存なし。
  公開gateは正式source-closure証跡不在でexit1。native smokeをIME/全機能の実入力証跡に
  数えない。全leaf/三OS/KUC不具合/公開/cleanupは未完了。旧coverage値も流用しない。
  両Luna担当はclose済み、stage/commit/push/sibling編集なし。

### 旧settings DTOの除去

- 事前設計に従いsettings7型とroot/types exportを除去した。settings.rsだけを削除し、
  KLEのshortcut競合計算も撤去。旧API内部test一件も除去したが、hostの競合/保存動作の
  検証を不要・完了とはしない。別名DTO、互換shim、KUC直接編集なし。
- 各型・root/typesごとの14独立compile-failを追加した。旧apply_settings拒否doctestは
  EditorSettings importを除去し、unit型を使ってtrait非所属を検査する。
  mainの同一consumer正例はrustc -D warningsでexit0、旧メソッド追加だけの負例は
  E0407のみでexit1。`target/acceptance/kle-settings-method-probe-r1.rs`と
  `kle-settings-method-negative-r1.jsonl`は限定compiler証拠であり実入力証拠ではない。
- ASTに`legacy-settings-definition`を追加し、7名のstruct/enum/type alias、raw/private/
  nestedを拒否する。初回mainゲートはtest219行・関数54行で失敗。設定専用2testsを
  legacy_settings_definition_tests.rsへ分離し、file200/function30の上限を維持した。
  既存config/styleの9回帰と設定2回帰を合わせ11件成功。
- main最終r2はfmt/strict Clippy/AST成功、Python17成功、Rust544成功/1失敗、exit101。
  検索CloseNotAppliedだけが残り、Storybook40成功/1失敗。
  `/tmp/kle-settings-dto-gates-20260905-r2.log`。workspace doctest24成功、
  `/tmp/kle-settings-dto-doctests-20260905-r1.log`。旧coverage値は転用しない。
- neutral consumer/check-types成功。main diff check/OpenSpec strict成功、固定KatanA clean。
  cargo outdated --workspaceは更新候補なし、最新KUC GitHub Releaseは0.3.6、#44はOpen。
  公開ゲートは正式source-closure証跡不在でexit1。全leaf/三OS/KUC不具合/正式公開/cleanup
  は未完了。両Luna担当はclose済み、stage/commit/pushなし。

### 旧style DTOの除去

- 事前設計に従いneutralの未使用`Typography`/`Spacing`定義とroot/types exportを
  除去した。`style.rs`一ファイルだけを削除し、別名DTO、KLE fallback、KUC変更なし。
  root/typesそれぞれの各型importを拒否する4独立compile-failを追加した。
- 既存config AST検査へ`legacy-ui-style-definition`を追加した。neutral内の同名
  struct/enum/type aliasをraw/private/nested含め拒否する。既存field検査を保ち、
  解析エラーの伝播、無関係型/literal、source scopeを含む9回帰が成功。
- main最終 `RUST_TEST_THREADS=2 just fmt-check lint ast-lint unit-test`は静的検査成功、
  Python17成功、Rust543成功/1失敗でexit101。失敗は検索CloseNotAppliedのみ、
  Storybook40成功/1失敗。`/tmp/kle-style-boundary-gates-20260905-r1.log`。
  workspace doctest10成功、`/tmp/kle-style-boundary-doctests-20260905-r1.log`。
  worker途中のfmt差分報告ではなく、上記の最終tree検証を判断に使う。
- neutral consumer成功で依存treeにeguiなし。OpenSpec strict/diff check成功。
  固定KatanA clean、公開gateは正式source-closure証跡不在でexit1。
  両Luna担当はclose済み。stage/commit/pushなし。coverageは再実行しておらず旧値を流用しない。
- これは旧型の二重所有除去だけであり、KUCのfont/spacing機能、HiDPI、三OS実入力、
  exact color glyph/hit test、全leaf結合、正式公開は未完了。settings/theme等も別途残る。

### neutral providerの実接続と現在の検証結果

- 追加の所有権AST検査を実装し、通常runnerを22規則へ更新。正規neutral moduleだけを
  許可し、同名trait/enum/struct/type aliasのprivate/nested/raw identifierも拒否する。
  初回worker案のtests名path除外をレビューで撤回し、解析エラーの空集合化と非排他的
  fixture確保も修正した。owner許可/再定義拒否/re-export/comment/stringを5回帰で検査。
- main全体r1はtest helperの37行とfixtureコメント表記でAST拒否。規則を緩めず
  helperを責務分割して修正した。r2はfmt/strict Clippy/AST成功、Python17成功、
  Rust540成功/1失敗、exit101。残る失敗は検索CloseNotAppliedでStorybook40成功/1失敗。
  `/tmp/kle-provider-ownership-gates-20260905-r2.log`。別実行workspace doctest6成功、
  `/tmp/kle-provider-ownership-doc-20260905-r1.log`。coverageの旧数値は流用しない。
  OpenSpec strictとdiff check成功。担当Lunaはclose済み、stage/commit/pushなし。
- provider trait/errorの唯一の定義をneutral crateへ移した。associated `Lease`により
  vendor型を分離し、egui binding/editorは実KUC leaseとの等価制約を持つ。
  eguiの既存importは同一型のre-exportであり、二重traitやpayload変換ではない。
- Storybook/downstreamのproviderと型制約を同期した。interface consumerは新traitの
  compile-only検査へ変更し、未接続状態をtyped errorとする。編集成功を模擬しない。
- main確認: neutral2 unit tests、workspace6 doctests成功。実KUC leaseの誤型、
  二重move、cloneをcompile-failで検査した。providerの型同一性も回帰で検査する。
  `/tmp/kle-neutral-provider-doctests-20260905-r1.log`。
- 最終static r3はexit 0。fmt/strict Clippy/AST成功。既存block0.1.6の
  future-incompat warningは残る。native smokeは2frame表示でexit 0であり実入力証拠ではない。
  `/tmp/kle-neutral-provider-static-20260905-r3.log`。
- unit r2はRust535成功/1失敗、exit101。検索Closeの`CloseNotApplied`が継続し、
  Storybook40成功/1失敗。全体coverageの旧合格値をこの変更へ転用しない。
  `/tmp/kle-neutral-provider-unit-20260905-r2.log`。
- 再照会でKUC #40/#43/#44はOpen。KUCを直接編集せずIssue-first handoffを維持する。
  `cargo outdated --workspace`は更新候補なし、neutral interface check成功、
  `KATANA_REPO`なしのupdate dry-run成功、固定KatanA checkoutはclean。
- `just kle-release-parity-check`は正式source-closure artifact不在でexit1。
  旧neutral DTO/controlの除去、全leafの意味分類・実入力結合、三OS証跡、正式公開と
  cleanupは未完了。KatanA #336は公開後の別gateであり、この失敗原因ではない。

### generic config field除去とStorybook同期の調査

- `EditorConfig`/`EditorConfigInput`の`typography`/`spacing`/`settings` fieldと
  constructor転送を除去した。interface診断と未接続の旧Storybook configの生成処理・
  不要helperも同期した。設定型のpublic export、theme/string/clipboard等は未完了。
- `legacy-config-generic-field` AST ruleを通常rule runnerへ登録した。neutralの対象
  2structだけを検査し、private/raw identifierの再追加も拒否する。main reviewで
  field内constにある無関係なstructの誤検出を修正し、scope/literal含め6回帰を確認。
- main追加testの整形差分で初回fmtが失敗し、整形後の全体r2はexit 0。
  Rust532 tests/11 suites (132.99s)、Python17、doctest1、Clippy0 errors、AST成功。
  `/tmp/kle-config-boundary-gates-20260905-r2.log`。
  coverage exit 0、line18,131/23,272 (77.91%)、閾値64維持。
  `/tmp/kle-config-boundary-coverage-20260905-r1.log`。
- `just storybook-window-smoke`は実nativeでexit 0、`just katana-interface-check`も
  exit 0。`env -u KATANA_REPO just --dry-run update`はexit 0で通常更新にKatanAを要求しない。
  `cargo outdated --workspace`再照会は更新候補なし。これらは公開完了の証拠ではない。
- [/] 続くStorybook同期修正: mainが公開KUC0.3.6 `scenario_session.rs`を再確認した。
  現root_runtimeは初期lease後にproviderを破棄し、sessionの更新presentation/newrevisionを
  発行する`synchronize_lease`を呼ばなかった。公開EguiTextCommandSurfaceEditorを保持し、
  interactive/artifactの両経路を同期付き呼出しへ寄せた。上記ゲートはこの追加修正前。
- 最初のworker案はHostProjectionBindingを直接使い既存storybook lintで失敗した。
  mainが公開editor経由へ修正し、direct-kle-binding禁止は維持した。最終fmt/strict
  Clippy/ASTはexit 0 (`/tmp/kle-provider-sync-static-20260905-r3.log`)。
  実KUC scenarioによる連続frame/retain一回/sync各frame一回の回帰はmainで2件成功。
  最終 `just storybook-window-smoke` は実nativeでexit 0。
- [ ] 同期後の検索Close: mainの最終公開editor経由テスト1件が
  `Continuation(Search(CloseNotApplied))`で失敗する (17.20s)。
  `/tmp/kle-provider-sync-motion-20260905-r2.log`。KUC sessionのupdateは
  `CommandChromeSearchEvent::Strip`以外を無視し、CloseRequested状態を保持しない。
  root同期はsearch_closed_by_interactionをfalseへ戻すため、次projectionで検索が戻る。
  KUC #44へsource/repro/依存更新/公開条件をIssue-firstで引き継いだ。
  https://github.com/HiroyukiFuruno/katana-ui-core/issues/44
  KLEでvisible状態を管理する補完、同期迂回、test skipはしない。KUC直接編集なし。
  したがって532件/coverage77.91%を現在の全体合格と扱わない。
- 最終 `RUST_TEST_THREADS=2 just unit-test` はRust532成功/1失敗、Python17成功で
  exit 101。失敗は上記検索Closeだけで、storybookは40成功/1失敗 (43.93s)。
  `/tmp/kle-provider-sync-unit-20260905-r1.log`。必須失敗で後続doctestは実行されない。
  同期修正後のcoverage全体は再実施せず、修正前の値を流用しない。
  OpenSpec strict/diff check成功、固定KatanA clean。公開ゲートは正式source-closure
  artifacts不足でexit 1。KUC #40/#43/#44、旧neutral型、全leaf、三OS実入力、公開・
  cleanupは未完了。全担当agentはclose済み。stage/commit/push/publicationなし。
- 当時未実装だったneutral provider接続は上記工程で実装・限定検証済み。
  旧neutral DTO/control全体の移行完了ではない。

### 旧設定更新入口の除去とCI必須検査

- `LanguageEditor::apply_settings`とFloem/interface診断hostの実装・呼出しを除去した。
  Floemの無処理成功や設定storeの別名shimを残していない。診断hostのconfigは
  DTO構築だけで、実editorへの設定適用証拠とはしない。EditorSettings等の型、
  config field、typography/spacing、control/clipboard/accessibility残骸は未完了。
- public traitのcompile-fail doctestを追加し、main reviewで`unimplemented!()`を
  通常の本文保持へ直した。旧メソッドがtraitに存在しないことを検査する。
  `just unit-test`へworkspace doctest実行を追加し、CIからの検査漏れを防いだ。
- CI `Run coverage`の`continue-on-error: true`を除去し、失敗を無視しない。
  release auditにworkflow検査を接続し、job/stepの失敗無視、必須command欠落の
  回帰を追加した。既存のrecipe/job/step範囲を読む検査で、一般YAML parserではない。
- 初回全体検証は回帰追加で既存test fileが307行となりASTで失敗した。上限300を
  維持しworkflow契約testを専用ファイルへ分離した。初回成功とは報告しない。
- 最終 `RUST_TEST_THREADS=2 just fmt-check unit-test lint ast-lint`: exit 0。
  Rust526 tests/11 suites (134.66s)、Python17、workspace doctest1、Clippy0 errors、
  AST成功。`/tmp/kle-settings-boundary-gates-20260905-r2.log`。
  coverage exit 0、line18,086/23,236 (77.84%)、閾値64維持。
  `/tmp/kle-settings-boundary-coverage-20260905-r2.log`。
- `just katana-interface-check`はexit 0でneutral treeにeguiなし。OpenSpec strictと
  diff check成功、固定KatanA clean。settings specの旧所有責務を現行境界へ同期し、
  autosave/shortcut/表示設定の動作要件はsource-derived owner別の未検証要件として保持。
- 最終公開ゲートは正式source-closure artifacts不足でexit 1。KUC #40/#43、
  全leafの結合、三OS実入力、公開、cleanupは未完了。sibling編集、stage/commit/pushなし。
  Luna担当2名はclose済み。今回の変更はnative描画に触れず、先の表示不具合は残る。

### 次のKLE固有の責務整理

- [ ] neutral crateの`EditorConfig{Input}`が`Typography`/`Spacing`/`EditorSettings`を
  公開していた旧APIの残りを整理する。config fieldとinterface構築からの利用、
  `apply_settings`入口とTypography/Spacingの型・exportは除去済み。
  EditorSettings等のsettings7型・exportも上記工程で除去済み。
  theme/localization/control等の旧APIと実動作の証跡は残っている。
  これはKUC #40/#43待ちではなく、tasks 4-6の未達である。
- main再確認: 現行egui rootは`HostProjectionBinding`のみ保持する一方、neutralの
  `lib.rs`は旧control/clipboard/accessibility APIをpublicに残す。
  Storybookの旧`config.rs`も残るが、`main.rs`にmod宣言がなく現実行経路ではない。
  sourceの存在とruntimeの利用を混同せず、compile graphとpublic API双方を検査する。
- 直接依存の現時点再照会 `cargo outdated --workspace --root-deps-only` はexit 0、
  更新候補なし。推移的依存を含む最終release再検証の代替とはしない。

### 対話Storybookの寿命と実画面で残る不具合

- 追加修正: `storybook-window-smoke`がheadlessの`--smoke`を呼んでいたため、
  nativeの`--interactive --frames 2`へ変更した。recipe本体の回帰を追加し、mainで
  1件実行/成功を確認した (worker報告の非修飾`--exact`は根拠にしない)。
  修正後の`just storybook-window-smoke`は実native起動後exit 0。
  `just fmt-check lint ast-lint`もexit 0。以下522件/coverageは追加前の全体結果であり、
  このrecipe/test追加後は対象回帰と静的検査・実window smokeのみ再実施した。
- 既定の対話起動からフレーム制限を外し、明示した `--frames` のみ有限とした。
  対話表示は既存KUC `show_current_and_forward_once` を呼び、毎frameの固定pathへの
  artifact書込みを除いた。artifactの上書き拒否や検証ゲートは変更していない。
  描画失敗はwindow終了後も呼出し元へErrを返し、成功として隠さない。
- 実KUC rootの連続2frame、opaque identity/receipt、フレーム上限、エラー伝播の
  回帰を追加した。汎用入力・文字・レイアウト状態をKLEへ移していない。
- 最終 `RUST_TEST_THREADS=2 just fmt-check unit-test lint ast-lint`: exit 0、
  Rust522 tests/11 suites (136.16s)、Python17、Clippy 0 errors、AST成功。
  `/tmp/kle-interactive-lifecycle-gates-20260905-r3.log`。
  coverage exit 0、line18,048/23,202 (77.79%)、閾値64維持。
  `/tmp/kle-interactive-lifecycle-coverage-20260905-r1.log`。
- native `--interactive --frames 2` はexit 0。既定の無制限起動は撮影まで維持し、
  自分の検証プロセスをTERMで終了した (143)。ユーザー操作終了の成功証拠ではない。
  CUAでは非bundleのアプリを取得できず、実keyboard/IME操作は未検証。
- 実画面に行番号の過大表示とDiagnostics文字の重なりが残る。
  `/var/folders/ql/4640yx8s22zg367pjjld7yc00000gn/T/codex-shot-2026-09-05_19-49-44.png`。
  撮影時のpixels_per_pointは未記録。日本語/色付きstarが見えても正確な計測・hit test・
  三OS入力の合格とはしない。固定8scenarioの表示をfull editor完成とはしない。
- mainが公開KUC 0.3.6のgutter logical/physical換算漏れ、Diagnostics計測scale不一致を
  再確認し、Open Issueとの重複を調べてKUC #43へ引き継いだ。
  https://github.com/HiroyukiFuruno/katana-ui-core/issues/43
  `docs/kuc-hidpi-layout-handoff-2026-09-05.md`に観測・未証明範囲・数値検証条件・
  依存更新と公開の条件を固定した。#40 artifact planとは別。KUC直接編集なし。
- 継続時の `just kle-release-parity-check` は正式source-closure artifacts不足でexit 1。
  OpenSpec strict/diff check成功、固定KatanA clean。全leaf結合・三OS実入力・公開・
  cleanupは未完了。stage/commit/push/publicationなし。

### 同一Contextでの入力履歴参照

- test-onlyの`ReferenceEditor`へ既存harnessを整理し、同一Context/Id/Stringを
  保持して入力フレームを連続実行する。初期focus/selection以外の状態上書き、
  独自Undo、sleep、timeout延長、OS上書きはない。headless textureは明示解放する。
- 入力->Undo->Cmd-Shift-Z/Cmd-YのRedo、選択置換後の復元、履歴なしno-op、
  Undo後の新規入力によるRedo破棄を4テストで追加した。参照計24件。
  stable_time=1.0に対応する単調なRawInput.timeを入力なしframeへ渡す。
  mainで各中間frameの本文/cursor/changedを補強し、selection復元ではCCursorの
  PartialEqが無視するprefer_next_rowも個別比較した。pixel証拠にはしない。
- 最新`RUST_TEST_THREADS=2 just fmt-check unit-test lint ast-lint`: exit 0。
  Rust515 tests/11 suites (129.59s)、Python17 tests、Clippy 0 errors、AST成功。
  `/tmp/kle-reference-history-gates-20260905-r1.log`。
- coverage exit 0、line17,915/23,071 (77.65%)、閾値64は変更なし。
  `/tmp/kle-reference-history-coverage-20260905-r1.log`。
  OpenSpec strictとdiff check成功、固定KatanA clean。担当Terraはclose済み。
- 最新公開ゲートは正式source-closure artifacts不足でexit 1。
  `/tmp/kle-reference-history-release-gate-20260905-r1.log`。
  これは固定egui参照の履歴検証で、host external change、document/workspace間の
  履歴隔離、全leaf結合、KUC比較、三OS実入力、公開を証明しない。
  production/依存manifest/lockfile/sibling編集・stage/commit/pushなし。

### 修飾キー別の削除実行参照

- `tests/support/reference_delete_modifiers.rs`へ12テストを追加し、既存の実egui
  harnessを共有した。Alt/Ctrlの前後単語削除、mac_cmdの前後段落削除、
  Ctrl-H/K/U/W、Ctrl-Wの選択あり/なし、修飾なしのkey-onlyイベントを検査する。
  参照テストは計20件。本文・cursor・changedを個別assertし、OSは上書きしない。
- main reviewでmac_cmdとcommandフラグの整合性を補正した。途中のr1合格だけを
  最終差分の合格とせず、修正後r2で全体ゲートを再実行した。
- 最新`RUST_TEST_THREADS=2 just fmt-check unit-test lint ast-lint`: exit 0。
  Rust511 tests/11 suites (150.38s)、Python17 tests。Clippy 0 errors、AST成功。
  `/tmp/kle-reference-modifiers-gates-20260905-r2.log`。
- coverage exit 0、line17,917/23,071 (77.66%)、閾値64維持。
  `/tmp/kle-reference-modifiers-coverage-20260905-r1.log`。
  OpenSpec strictとdiff check成功、固定KatanA clean。担当Lunaはclose済み。
- 最新公開ゲートは正式source-closure artifacts不足でexit 1。
  `/tmp/kle-reference-modifiers-release-gate-20260905-r1.log`。
  このmacOS上の参照結果はWindows Shift-Deleteガード、全modifier/selection組合せ、
  全source leaf、KUC比較、三OS実入力を証明しない。KUC/KatanA編集なし、
  stage/commit/push/publicationなし。全要件結合、公開、cleanupは未完了。

### 固定eguiの削除イベント実行参照

- `tools/katana-parity-check/tests/egui_reference_delete.rs`を追加し、eguiは
  検証専用dev-dependencyとした。実Context/RawInput/TextEdit/Stringを使い、
  初期focus/selection設定後の入力フレームから本文・caret・changedを確認する。
  texture deltaはheadless参照として明示解放し、pixel/OS入力の証拠にはしない。
- lockの全egui packageから単一version/source/checksumを構造化検査する。
  `cargo update --offline -p katana-parity-check`は新規version更新0件で、
  既存egui 0.36.1の検証用依存を接続した。KUC UIの複製はない。
- 8参照テストは通常Backspace/Delete、境界、selection、read-only、idle、
  exact VS16、固定依存identityを確認する。初回はAPI/texture lifecycle不一致を
  修正した。境界削除は本文不変でもchanged=true、idleはfalseと実測した。
  VS16付きstarの末尾BackspaceはVS16だけを削除する。これをKUCのgrapheme
  保持要件の代用にせず、参照実装との既知の差として記録する。
- 最新`RUST_TEST_THREADS=2 just fmt-check unit-test lint ast-lint`: exit 0。
  Rust499 tests/11 suites (125.27s)、Python診断17 tests、Clippy 0 errors。
  `/tmp/kle-reference-delete-gates-20260905-r1.log`。Cargo表示のwarningは
  `cargo report future-incompatibilities --id 1`でblock 0.1.6と再確認した。
- coverage exit 0、line 17,916/23,071 (77.66%)、閾値64維持。
  `/tmp/kle-reference-delete-coverage-20260905-r1.log`。
  OpenSpec strict、diff checkも成功。固定KatanAはclean、担当Lunaはclose済み。
- 最新公開ゲートは正式source-closure artifacts不足でexit 1。
  `/tmp/kle-reference-delete-release-gate-20260905-r1.log`。KUC #40は再取得でも
  Open/コメントなし。参照8テストは全delete-branch-family、KUC比較、三OS実入力、
  KatanA実ホストの代替ではない。全leaf結合・公開・cleanupは未完了。
  sibling編集・stage/commit/push/publicationなし。

### TextBuffer既定メソッドと動的呼出し境界

- `text_buffer_definition_probe.py`をrunnerへ接続し、builderとtext_bufferの
  認証処理を同一固定archive SHAで共通化した。URI/member/bytesを照合して
  text_bufferのみ追加didOpenし、builderを重複openしない。
- r4実解析は同一呼出しanchorが3メソッドに存在するためexit 1で停止した。
  最初の一致で代用せず、問い合わせをdelete_previous_charの範囲へ限定し、
  隣のメソッドに同一呼出しがあるfixtureを回帰へ追加した。
- r5実解析はexit 0、engine exit 0、errorsなし。
  `target/acceptance/rust-analyzer-probe-20260905-r5/report.json` に、
  builder:1386 -> text_buffer:133(delete_previous_char)、
  text_buffer:137 -> :125(delete_selected_ccursor_range)、
  :126 -> :45(delete_char_range trait宣言)を記録した。
  text_buffer SHAは `6ac11dc0237bf7250bfae23c8139c56ae352ecb92b9ecfa699bc5b73203d0989`。
- 最後の辺は`unresolved_dynamic_dispatch`、全体semantic_complete=false。
  KatanA renderの`buffer: &mut String`は固定ソースから読めるが、実装選択・
  全イベント実行・KUC同等動作の証明には昇格していない。
- 最新`just diagnostic-probe-test`: 17 passed/skipなし。UTF-16 surrogate/VS16
  位置、callsite誤採用、source改変、曖昧anchor、未解決境界を検査する。
  fmt/ASTとdiff checkはexit 0。Rust本体の変更・workspace全テスト/coverageの
  再実行なし。固定KatanAはclean、sibling編集・commit・pushなし。
  担当Terraはclose済み。全要件leaf、三OS実入力、公開、cleanupは未完了。

### eguiイベント処理への実定義解決

- 診断helper `scripts/diagnostics/egui_definition_probe.py` を追加し、固定SHAで
  認証した同一archive bytesからbuilder memberを読み、展開先bytesと照合して
  didOpenする。返却URI・単一定義・UTF-16 symbol rangeを照合する。
- 実LSP r3はexit 0、engine exit 0、errorsなし。
  `target/acceptance/rust-analyzer-probe-20260905-r3/report.json` に
  show内builder.rs:565からevents定義:1042、events内:1219から
  check_for_mutating_key_press定義:1369への解決を記録した。
  source SHAは `1c2198aa1c397d40e0e384cb6e69252822a6bdf870b8b3d9ddafb2dfd5f98cbd`。
- main reviewでcallsite/definition混同と、目的の拒否条件まで到達しないテストを
  修正した。hash関数を偽造せずfixture SHAを設定し、拒否理由も照合する。
  macOS一時pathのcanonical表記違いで早期失敗していたfixtureも修正した。
- `just diagnostic-probe-test`: 11 tests passed、skipなし。
  通常`unit-test`の依存へ接続した。`just fmt-check ast-lint`もexit 0。
  Rust workspace全テスト/coverageは今回は再実行していない。
- これは明示した2辺の診断であって、全推移解析や要件網羅の完成ではない。
  canonical manifest・公開ゲートへ昇格していない。次はTextBufferの動的dispatch、
  cursor/undo/IME分岐の根拠を認証付きで全leafへ結合する。KUC/UI移植なし、
  sibling編集・commit・pushなし。担当Lunaはclose済み。

### 実Rust意味解析エンジンの限定プローブ

- 前turnは状態報告のみ。今回は `scripts/diagnostics/rust-analyzer-probe.py` を
  固定KatanAに実行し、rust-analyzer 1.97.1のLSP definitionを確認した。
  `target/acceptance/rust-analyzer-probe-20260905-r1/report.json` はexit 0、
  health=ok、engine exit 0。固定checkoutは実行前後ともclean。
- `TextEdit::multiline`、`text_edit.show`、`i.consume_shortcut` は単一定義へ解決した。
  返却先はegui 0.36.1のbuilder.rs:123、builder.rs:436、input_state/mod.rs:737。
  固定lock SHAに一致するcrate archiveの各memberと展開先bytesを照合し、
  UTF-16 rangeが該当symbol名に一致することを独立確認した。
- Lunaの修正をレビューし、LSP header上限とprotocol ValueError後のchild終了を
  4単体テストで再検証した。修正版r2もexit 0、r1と3件のquery結果は同一。
  `just fmt-check ast-lint`、`git diff --check`はexit 0。担当agentはclose済み。
  今回Rust本体の変更はなく、workspace全テスト・coverageは前項の既存結果であり
  今回再実行したとは扱わない。
- build scripts/proc macrosは無効、Cargoはoffline/locked、生成先はKLE target内。
  この結果は3入口の限定診断であり、全推移的呼出し、macro、動的dispatch、
  platform入力意味論の証明ではない。semantic_complete/execution_performedはfalse。
  正式source-closure manifestや公開ゲートの代替として取り込んでいない。
- 次はこの解決方式を認証済みshow/events/text mutationの推移的解析へ接続する。
  KUC #40は今回のGitHub再取得でもOpen/コメントなし。KUC/KatanAの編集なし。
- 空き容量不足への対処として、実行中バイナリがないことを確認したKLEの
  `katana-parity-check` devビルド成果物のみCargo cleanで整理した。
  ソース、受入証跡、sibling cacheは削除していない。公開は未完了。

### 固定KatanA invocationとvendor入力rootの統合

- source scannerが固定Git blobと照合した同一bytesを外部invocation解析へ接続した。
  重複走査は呼出し入力へ再登録しない。別path集合の未照合再読込を足さず、
  report fingerprintを既存の診断rootに結合する。推移的semantic closureは未完了。
- 実診断r23では本番`editor/paste.rs`のtuple match patternが未採取だった。
  `Event::Paste`のconstructorと`event-pattern`を区別して採取し、似た名前の
  OtherEvent、文字列、コメント、未展開macroを入口として扱わない回帰を追加した。
- 固定Cargo manifestのpatch先`vendor/egui-winit/src/lib.rs`をsource rootへ追加した。
  同packageのlockにはregistry checksumがないため、registry版へ代用せず固定Git blob
  として認証する。IME/keyboard/clipboardとdeclared moduleを追跡し、root省略拒否を
  回帰検証した。KUC内のUI/input実装をKLEへ複製していない。
- 最新 `RUST_TEST_THREADS=2 just fmt-check unit-test lint ast-lint`: exit 0、
  491 tests/10 suites、131.44s。`/tmp/kle-invocation-gates-20260905-r3.log`。
  最初の統合検査はfixtureの通常コメントをASTが拒否したため、WHY blockへ修正して
  全体を再実行した。検査の除外や閾値変更はしていない。担当agentは全てclose済み。
- 最新coverage: exit 0、line 17,915/23,071 (77.65%)、閾値64維持。
  `/tmp/kle-invocation-coverage-20260905-r1.log`。
- 実診断r25/r26は双方exit 0、bytes一致。SHA-256:
  `a69ac4623bfbd50901c823d9da70f12e6849486639d69ddfc2e28a712e947b48`。
  `target/acceptance/requirement-bindings-20260905-r25.json`。
  612 source filesにvendor 5 filesを含み、外部invocationは14件。本番paste pattern
  3件、vendor paste生成、実shortcut spanを記録する。report/root fingerprint一致。
- 候補283、未結合10,213、unbound branch8,342、参照なし172、source-action未解決
  56,324。vendor入力範囲を追加して未解決が増えたもので、機能数や達成率ではない。
  外部推移解析とmacro/dynamic/type ambiguityを保持し、complete/passing/execution=false。
- 最新公開ゲートは正式source-closure artifact set不足でexit 1。
  `/tmp/kle-invocation-release-gate-20260905-r1.log`。KUC #40はOpen/コメントなし。
  KatanA固定checkoutはclean、sibling編集・stage/commit/push/publicationなし。
- 次の実装対象: 認証済みeguiのshow/eventsからCCursorRange、TextBuffer、Undoerへの
  source-derived semantic graphと、vendor入力変換からの結合。winit/clipboard/accesskit
  依存の出自・意味解析、全leaf enrichment、KUC #40接続、三OS実証、公開、#336、
  cleanupは未完了であり、今回のsource採取をそれらの代わりにしない。

### Archive identityと実shortcut入口の最終統合

- 認証済み`.crate`を一次情報にして、展開済み全Rust path集合とbytes hashを
  AST解析前に照合する。通常registryではvendor checksum台帳を要求せず、存在時の
  不正台帳は無視しない。不正path/link/重複/破損footer/サイズ上限と、ソース追加・欠落・
  改変・台帳同時改変からの部分的なAST採取を拒否する回帰を追加した。
- 固定shortcut入口をInputStateへ修正した。Context.input_mutの単一closure binding、
  型由来、typed/destructured/loop/matchのshadowing、module import境界、同名local
  type、closure escape、独立Event seedの維持を検査する。一般Rust型推論の完成ではない。
- Luna/Terraの実装をmainでレビューし、raw TAR fixtureの型付きAPI化、展開上限の
  空read、落ちていたancestor symlink検査、no-manifest改変検査を補った。
  最初の全体ゲートは整形後208行と定数化漏れで不合格。規則を緩めず、関数scopeの
  重複を共通化して再実行した。担当subagentは全てclose済み。
- 最新 `RUST_TEST_THREADS=2 just fmt-check unit-test lint ast-lint`: exit 0、
  486 tests/10 suites、135.28s。
  `/tmp/kle-archive-identity-gates-20260905-r3.log`。
  表示されたwarningはblock 0.1.6の既存future incompatibilityと再確認した。
- 最新 `RUST_TEST_THREADS=2 just coverage`: exit 0、line 17,873/23,052 (77.53%)。
  閾値64は変更していない。`/tmp/kle-archive-identity-coverage-20260905-r1.log`。
- 実診断r21/r22は双方exit 0、JSON bytes一致。SHA-256:
  `900598b72d2898f175cc35e76b2cf00cf7713de76274a9d82b615d292e952fbc`。
  `target/acceptance/requirement-bindings-20260905-r21.json`。
  5入口/3 source filesの認証済みhashとspanを採取し、external fingerprintが診断rootと一致。
  外部reportはinvocation/推移解析を行わない制約だけを未解決として保持し、
  complete=false、passing=false、execution=falseを維持した。
- 診断候補283、未結合10,213、unbound branch7,285、参照なし172、
  source-action未解決53,778。これらは機能数・達成率ではない。
- 最新 `just kle-release-parity-check`: exit 1。正式source-closure artifact setが
  未生成で公開不可。`/tmp/kle-archive-release-gate-20260905-r1.log`。
  残作業は全source/外部依存の意味解析とleaf enrichment、KUC #40公開API接続、
  三OS入力/映像/AX/opaque outcome、公開、公開後#336、事後cleanup。
- `KATANA_REPO`なしのupdate dry-runは成功。直接依存16件に最新候補なし、
  lock更新dry-runも0件。制約内の旧推移依存4件は前項の理由で維持した。
  OpenSpec strictとdiff checkも成功。stage/commit/push/publication/sibling編集なし。

### 実registry診断で判明した入力前提の修正

- 固定ソース診断r18/r19はexit 0、JSON bytes一致。ただし外部source evidenceは空で、
  標準registry cacheに存在しない`.cargo-checksum.json`を要求していたため未解決。
  vendor directory sourceとregistry cacheを混同したKLE側の検証不具合であり、
  KUC待ち・環境不備として扱わない。archive認証を一次情報にする設計へ修正した。
- 同じ固定ソースで`ctx.input_mut(|i| i.consume_shortcut(&parsed))`を確認した。
  実定義はegui `InputState::consume_shortcut`であり、旧REQUIREDの
  `Context::consume_shortcut`は誤り。偽のContext fixtureに合わせず、closureの
  型由来とscopeを検証する修正を実施中。source-universeの記述とroot SHAも更新した。
- Rust path解決は現在moduleへの誤fallbackを廃止した。r18の
  `std::env::consts::OS`は`about_info/mod.rs`自身へ誤結合せず、未知の外部境界として
  明示される。未解決数53,778は診断候補であり、進捗率や機能数ではない。
- r18/r19のpassing/executionはfalse。最新変更後の全体ゲート・実診断は再実行前。
  KUC #40はOpen/コメントなしを再確認した。KatanA固定checkoutはclean。

### T4 正式manifest接続の統合結果

- materializerをinventory -> T2 -> T3 ScanState -> structuredへ接続した。
  `canonical_binding`はleaf-manifestの必須payloadで、全reportを保持する。
  旧forward_route joinはtest-onlyとなり、正式生成には使わない。
- 固定要件/aliasからinventoryとT2を再構築し、T3の親/root/fingerprint/source factsを
  確認してstructured全体を再構築する。leafの不変provenanceは候補と1対1で照合し、
  同数のduplicate/missingも拒否する。productionはcandidateを生成するだけで、
  未解決の意味分類やUI/effect/executionを補完しない。
- release/fullは未解決inventory、T2 unbound、T3 unmatched、参照なしrouteを明示的に
  拒否する。古い静的fixtureのglobal passingを廃止し、整合性の正例と未解決closureの
  拒否例を区別した。mainは再hash済みの縮小branch由来binding拒否、必須payload欠落、
  未知nested field拒否の回帰を追加した。Terraは完了後close済み。
- agentの初回全suiteはディスク不足で失敗したため合格と扱わず、KLEのcoverage用
  再生成可能cacheを`cargo llvm-cov clean`で整理して再検証した。source/acceptance
  artifactsと他repoは削除していない。KatanA固定checkoutはclean。
- main `RUST_TEST_THREADS=2 just fmt-check unit-test lint ast-lint`: exit 0、
  459 tests/10 suites、248.69s。テスト集合と閾値は維持し、同時実行数だけ制限した。
  `/tmp/kle-canonical-integration-gates-20260905-r1.log`。
  Cargoのwarning 1件はblock 0.1.6の既存future incompatibilityであり、strict Clippy
  ruleの無効化ではない。`cargo report future-incompatibilities`で確認した。
- main `RUST_TEST_THREADS=2 just coverage`: exit 0、line 17,400/22,522 (77.26%)。
  既存閾値64は変更していない。`/tmp/kle-canonical-coverage-20260905-r1.log`。
- 固定source診断r16/r17は双方exit 0、cmp一致。SHA-256:
  `6c86121536769ddcd2cbdfba77c8214cfbba02ff298022fbc09731d0e1cf9545`。
  `target/acceptance/requirement-bindings-20260905-r16.json`。
  候補283、未結合binding10,213、unbound branch7,285、参照なしroute172、
  source-action未解決53,724。診断候補数であり機能数ではない。passing/execution=false。
- 実 `just katana-parity-check` はexit 1。正式source-closure artifact setが未生成のため
  公開ゲートは通らない。`/tmp/kle-canonical-release-gate-20260905-r1.log`。
  生成・整合性接続のunit検証と三OSの実証拠完成を混同しない。
- 未完了: 全source/外部dependencyの意味分類とarchive identity自動検証、意味分類を
  実際のKUC公開APIへ接続する工程、KUC #40、三OS入力/映像/AX/opaque outcome、
  正式artifact公開時の中断耐性、v0.1.0公開、公開後#336、事後cleanup。
  stage/commit/push/publication/sibling編集は実施していない。

### 外部入力境界と依存再確認

- 外部sourceのchecksum manifestと全相対pathに、通常ファイル、POSIX path、
  各要素symlink拒否、canonical containment検査を追加した。Lunaの修正をレビューし、
  traversalとancestor symlinkの先に実在する正しいSHAのsourceを置く回帰へ補正した。
  main再実行 `cargo test --locked -p katana-parity-check external_ui_dependency` は
  14 passed、exit 0。全体ゲートはcanonical接続差分の安定後に再実行する。
- 固定egui 0.36.1のcache archive SHAはKatanA lockと一致し、builder.rs/state.rs/
  context.rsのarchive member SHAも監査表と一致した。これは限定的な読取監査で、
  generatorのarchive identity検証や推移的semantic closureの実装完了ではない。
  事前path検査後の同時ファイル差替えに対するhandleベースの保護も未証明である。
- `env -u KATANA_REPO just --dry-run update` はexit 0。`KATANA_REPO`なしで
  update recipeを解決できる。固定source/host操作の明示path要求は維持する。
- `cargo upgrade -i --dry-run` で直接依存14件に更新候補なし。
  `just update-safe` はexit 0、indexmap 2.14.1から2.14.2へ更新した。
  続く `cargo update --dry-run --verbose` は更新0件だった。
- 旧版generic-array 0.14.7はcrypto-common 0.1.7の完全固定により更新不能。
  toml 0.8.2 / toml_datetime 0.6.3 / toml_edit 0.20.2はFloemのLinux GTK経路にあり、
  proc-macro-crate 2.0.2のtoml_datetime完全固定が最新toml 0.8.23と競合する。
  generic-array 0.14.9とtoml 0.8.23の個別dry-runはいずれもresolver error/exit 101。
  siblingや外部依存の制約を改変せず残した。全OSでの実build証明ではない。
- KUC #40はOpen/コメントなし、registry最新0.3.6/not yankedと再確認。
  KatanA固定checkoutはclean。正式な構造化manifest接続はTerraが実装中であり、
  本項をもって全editor parity/公開完了とは判定しない。

### T4 構造化候補の診断接続

- T2/T3をsource/branchへ再照合する`source_leaf_candidates`を診断CLIへ実装した。
  IDを文字列分割せず完全一致で結合し、全root/親fingerprint/source SHA/branch
  facts/全routeのspan-pathと生成箇所の包含を検査する。未知参照、重複、
  fingerprint再計算後の精度過大表示を拒否する。
- 要件未参照route、未結合binding、T2/T3未解決、unbound branchを保持する。
  canonical leaf生成、三OS実入力、KUC実装、passingへの昇格は行っていない。
- 置換2要件は固定KatanAのaction definition/dispatch/DocumentEditOpsへの参照を
  明示した。既存handlerの存在を検索置換UIや全件置換の実装証拠と扱わない。
- Terra実装をレビューし、visibility/借用/テストfixture、AST公開形式、
  duplicate module、精度過大表示検査を修正して統合した。agentはclose済み。
  最終 `just unit-test`: 445 tests/10 suites、exit 0、98.65s。
  `/tmp/kle-structured-unit-final-20260905-r2.log`。
  `just fmt-check lint ast-lint`: exit 0。
  `/tmp/kle-structured-static-20260905-r4.log`。coverageは今回未実行。
- 実診断r14/r15はexit 0、JSON bytes一致。SHA-256:
  `872e455ca2e8e3367c484a026460260660efb275f2bf64f04e549bb2e8a66666`。
  最新artifactは`target/acceptance/requirement-bindings-20260905-r14.json`。
  候補283、要件未参照route172、未結合binding10,213、unbound branch7,285、
  source-action未解決53,724、未解決要件参照23行。構文由来の重複候補を含む
  診断件数であり、機能数や未実装機能数ではない。passing/execution flagsはfalse。
- KUC #40はOpen/コメントなしと再確認。KatanA checkoutはclean、sibling編集と
  stage/commit/push/releaseは未実施。OpenSpec strict/diff checkは成功した。
- 次: 旧canonical leaf生成の文字列joinを構造化経路へ置換する設計と実装、
  外部semantic closureとregistry archiveからのsource identity検証。

### T4a 構造化 AppAction origin の限定投影

- canonical leaf projectionは、既に検証済みのroute definition/construction/dispatch/
  variant-pattern factsから`katana:AppAction::<variant>`を一つだけ投影する。空又は
  Rust identifierとして不正なvariant、candidate provenance改竄、duplicate provenanceを
  回帰で拒否する。
- handler、最終buffer effect、KUC component、opaque transit、Storybook stage、host E2Eは
  引き続き`unresolved:*`又は`downstream_required`であり、statusは`candidate`のままである。
  release validatorを緩和せず、この投影を実行互換性や公開合格の根拠にしない。
- `cargo test -p katana-parity-check --locked`は382成功、固定egui参照38成功、fmt、strict
  Clippy、AST lintは成功した。固定KatanA診断r1は612 source files、283 candidatesを出力し、
  未解決要件12、unbound branch 8,342、未解決action 56,324を保持する。KUC #40/#44、三OS、
  full Storybook、公開、cleanupは未完了。

### T4b 未結合branchの構造分類

- `UnboundBranch`はbranch ID/file/span/hashに加えてsymbol、kind、condition、active profile、
  inactive cfg predicate、incoming edgeを保持する。共有`BranchRecord`の型契約を変更せず、
  diagnostic専用のpredicate DTOへ値を写す。
- fixed KatanA再診断r2は8,342件の未結合branchを構文種別付きで出力した。構文種別は
  `if`/`match`/`cfg`/loop等であり、件数を機能数・到達可能性・実装不足へ読み替えない。
  binding、action effect、KUC/Storybook/host evidence、statusは未解決のままである。
- `cargo test -p katana-parity-check --locked`は383成功、固定egui参照38成功、fmt、strict
  Clippy、AST lint、OpenSpec strict、diff checkは成功した。KUC #40/#44、三OS、full Storybook、
  公開、cleanupは未完了。

### T4c 未解決scan edge種別の構造分類

- `SourceActionUnresolved.source_edge_kind`はscan state由来のedge kindだけを保持し、それ以外の
  unresolvedでは`None`とする。空文字又は空白だけのscan edge kindはfail-closedで拒否し、
  表示用reasonは変更していない。
- fixed KatanA再診断r3は42,013件のunresolved scan edgeを24種別として出力し、
  `source_edge_kind`の欠落又は空値は0件だった。これは実行可能性・handler/effect・UI互換性を
  証明せず、未解決action 56,324件を種類別に追跡可能にしただけである。
- `cargo test -p katana-parity-check --locked`は385成功、固定egui参照38成功、fmt、strict
  Clippy、AST lint、OpenSpec strict、diff checkは成功した。KUC #40/#44、三OS、full Storybook、
  KatanA実ホストE2E、公開、cleanupは未完了。

### T4d 未解決scan edge詳細の構造分類

- `SourceActionUnresolved.source_edge_detail`はscan stateのraw `ClosureEdge.to_symbol`だけを
  保持する。scan以外は`None`であり、kind又はSomeのdetailが空白だけならfail-closedで拒否する。
  raw detailをtarget、外部境界、解決済み、到達可能性として解釈しない。
- fixed KatanA再診断r4は42,013件のscan edgeのうち32,769件にdetailを保持し、kindの欠落又は
  空値、detailの空値はいずれも0件だった。`self`や`Some`のdetailにはlocal source候補がない旨の
  raw reasonも含まれるため、分類値だけでKUC又は外部依存へ除外しない。
- source-action binding限定の11回帰テスト、fmt、AST lintは成功した。全workspace gateと
  OpenSpec strict、diff check、固定KatanA clean確認は続けて実行する。KUC #40/#44、三OS、
  full Storybook、KatanA実ホストE2E、公開、cleanupは未完了。

### T4e lexical resolution結果の構造分類

- `ClosureEdge.lexical_resolution`は`record_resolution`だけが設定するclosed enumで、`local`、
  `unresolved`、`ambiguous`、`ambiguous_alias`を表す。branch、module、method call、direct
  unresolvedは`None`であり、unknown enum値のJSON decodeを拒否する。
- fixed KatanA再診断r5では42,013件のunresolved edgeのうち`local` 2,477件、`unresolved`
  12,618件が構造statusを持つ。残りを外部dependency又は実行不能と推測しない。`local`は
  source path候補の解決結果であり、target materializationや実行effectの証明ではない。
- 388 parity-check testsと固定egui参照38件が成功した。fmt、strict Clippy、AST lint、
  OpenSpec strict、diff checkは続けて実行する。KUC #40/#44、三OS、full Storybook、
  KatanA実ホストE2E、公開、cleanupは未完了。

### T4f 既走査targetとcanonical pathのmaterialization

- local targetが既に`ScanState.files`にrecord済みならpendingへ再登録しない。back edgeの後に
  targetを再走査しない既存の制御では、この再登録が`finalize_unscanned_targets`でfalse
  unresolvedとなっていた。既走査back edgeを再現する回帰を追加した。
- path候補を`canonicalize`してstateへ入れる。case-insensitive filesystemの`crate::Icon`は
  `Icon/mod.rs`という候補文字列で存在判定され得る一方、inventoryは実体path
  `icon/mod.rs`を保持する。正規化で両者のidentityを一致させたが、symbol binding、外部依存、
  実行可能性を解釈してはいない。
- fixed KatanA再診断r7ではunresolved edgeは39,449件。`local` statusは0件、`unresolved`
  statusは12,618件、statusなしは26,831件である。r5の`local` 2,477件はこの二つの
  materialization欠陥だった。389 parity-check testsと固定egui参照38件が成功した。
  fmt、strict Clippy、AST lint、OpenSpec strict、diff check、固定KatanA clean確認は続けて
  実行する。KUC #40/#44、三OS、full Storybook、KatanA実ホストE2E、公開、cleanupは未完了。

### T4g TextEdit builderからshowへの実行入口

- KatanA editor本体の`let text_edit = TextEdit::multiline(...).<builder methods>;`
  と`text_edit.show(ui)`を、同一closure scopeのplain identだけで結合する。bindingはclosure
  depthを保持し、inner closureのbindingを外側へ漏らさず、outer bindingをclosure exit後に復元する。
  shadowing、outer/inner closure越境、別型の`show`、macro、一般型推論は解決済みにしない。
- `TextEdit::show`を固定egui `0.36.1`の必須definitionに加え、認証source fixtureも6入口へ更新した。
  r10ではsix definition、15 KatanA invocationがあり、`TextEdit::show`は
  `crates/katana-ui/src/views/panels/editor/text_edit.rs:66:30-66:48`の1辺として記録された。
- 391 parity-check tests、固定egui参照38件、fmt、strict Clippy、AST lint、OpenSpec strict、
  diff checkが成功した。external semantic dependencyは未採取の推移解析と実行証拠を理由に
  `complete=false`のままである。KUC #40/#44、三OS、full Storybook、KatanA実ホストE2E、公開、
  cleanupは未完了。

### T4 旧leaf joinの入力整合性

- root全14項目の同一性を候補生成前に検査する。source path/branch ID重複、
  未走査sourceを参照するbranchを拒否し、以前の黙った除外を削除した。
  空branch/action集合の各root項目変異と、正常なroute no-matchも回帰検査する。
- 最新 `just fmt-check unit-test lint ast-lint`: exit 0、435 tests/10 suites。
  ログ `/tmp/kle-leaf-input-final-20260905.log`。coverageは今回再実行していない。
  Luna実装をレビュー・統合し、完了後closeした。sibling/commit/push操作なし。
- Shift+Tabの暗黙参照を固定ソースとeguiのfilter依存へ明示した。外部semantic
  closureは未解析として残す。5入口採取は推移的な全入力仕様の解析ではない。
- 最新診断は `target/acceptance/requirement-bindings-20260905-r11.json`、exit 0。
  SHA-256 `d122096aba6beb567fcef5304366ae2bea5c263a1e9110041df02ed1082f8a7e`。
  未解決要件参照23行、source binding254、未結合branch7,285。
  `passing_claimed=false`、`execution_performed=false`。KatanA checkoutはclean。
- 未完了: 旧route文字列によるleaf joinを構造化T2/T3へ接続する設計・実装、
  外部semantic closureの推移解析、全機能の実入力証拠、公開と事後作業。

### T4 参照精度の継続結果

- preview/diagnostic の暗黙参照を固定ソースで確認し、明示パスへ訂正した。
  export の実 handler は `app/action/export.rs` ではなく `app/export.rs`。
  source-universe と root manifest/checksum を同期し、有効な既存 alias は保持した。
- 直接参照/directory/alias のキー衝突で参照が消える診断不具合を修正した。
  brace 展開自体は既存 parser が実施しており、単独の欠落原因ではなかった。
- 完全修飾 AppAction construction を未解決候補として保持する。名前一致だけで
  定義を解決済みにせず、修飾 path/variant/span/macro の根拠を残す。
- 固定 egui builder のSHA一致、Tab既定false、filtered_events経路を確認し、
  旧外部仕様監査のTab indentation記載と公開前host E2E要求を訂正した。
- 最新 `just fmt-check unit-test lint ast-lint`: exit 0、428 tests/10 suites。
  ログ `/tmp/kle-r4-final-gates-20260905.log`。coverageは今回再実行していない。
  初期統合時のalias未使用/未登録export/format/AST失敗を修正後の結果である。
- 診断 r9/r10 は双方 exit 0、JSON bytes一致。SHA-256:
  `7a74eff5d39f33647606528b33c8ffef262c13ea30b83e2f490edbc70e10615a`。
  `target/acceptance/requirement-bindings-20260905-r9.json` が最新の入力根拠。
  未解決要件参照23行、source binding253、構文候補9,975、未結合7,285、
  action route候補204、完全修飾action未解決80件。全て診断候補で機能数ではない。
- KUC Issue #40 は Open、コメントなし。registry sparse index の最新0.3.6は
  not yanked。crates.io REST API は403だったため公開indexで再確認した。
- KatanA checkoutはclean。sibling edit、stage/commit/push/publicationは実施していない。
  subagent3件は終了・close済み。OpenSpec strictとdiff checkも成功した。
- 次は23行の外部/内部symbol semantics、replace拡張境界、旧leaf joinの全root
  同一性と構造化T2/T3統合を進める。KUC待ちだけを理由に停止しない。

- [/] 作業の整理だけで終了しない。T2/T3、直接参照の補完、診断 CLI、公開ゲートの
  境界修正を実装した。最新差分で全 workspace 426 tests、fmt、strict Clippy、AST、
  coverage が合格した。line coverage は 16,552/21,723 (76.20%)。
  全 editor parity と v0.1.0 公開は未完了である。
- T2: source SHA / branch span / excerpt SHA と要件候補を結合し、全 branch を逆引きする。
  未対応 branch、missing / duplicate source、branch 不在を残し、重複 ID と root 全項目の
  不一致を拒否する。固定 alias / universe fingerprint と実 Rust parser 入力も検証済み。
- 実診断の初回は root manifest の checksum 表記不一致で失敗した。実 source-universe
  SHA は既存値と一致しているが、manifest が raw hex、validator が `sha256:` 付きである。
  入力のすり替えや検証緩和ではなく、manifest とチェックイン済み入力の回帰検査を修正した。
- T3: 構造化した action definition / construction / dispatch と source SHA/span を結合した。
  alias、macro、曖昧な定義、dispatch 不在、未結合要件を残す。行単位の包含候補であり、
  同一行の入れ子の因果関係や実行効果を証明しない。
- 要件表の完全パス / 相対パス / brace / directory と短縮 alias を同じ診断へ接続した。
  空 source、暗黙参照、既知と未知の混在、duplicate source / ID、source cell 変更、
  directory prefix の境界を回帰検査する。実行済みや passing へ昇格する経路はない。
- 固定 KatanA 607 files / 9,975 構文候補に対して、要件表145行、requirement-file 215組、
  requirement-branch 8,674組、action route 204候補を出力した。未解決の要件行44、
  未結合 branch 7,358 が残る。多対多の候補件数を機能数や達成率と扱わない。
- 最終診断 `target/acceptance/requirement-bindings-20260905-r7.json` と `r8.json` は
  `cmp` で一致。両者の SHA-256 は
  `02185cc11dcb5be18052ea4f1263f7a498d014663c0bb2536975909a800156e6`。
  `diagnostic_only=true`、`execution_performed=false`、`passing_claimed=false` を維持する。
- `env -u KATANA_REPO just kle-release-parity-check` は canonical artifacts 不在で exit 1。
  KatanA checkout や #336 host E2E を前提として失敗したのではない。
  `kle-release` は KLE candidate integrity を別に検査し、欠落・三 OS/profile不一致・
  symlink・hash改変・別leafへのstage再利用を拒否する。KUC #40 producer の消費は未実装。
- `just katana-interface-check katana-downstream-check` は exit 0。中立 API への egui 非漏洩と
  registry KUC 0.3.6 consumer の構築を確認した。後者は診断であり KatanA host proof ではない。
- canonical schema を `1` に限定する回帰 1 件、既存 artifact validator 17 tests が合格。
  診断 schema を公開用へ流用しない。
- KUC #40 は再確認時も Open、コメントなし。依存しないソース解析・検証整備は継続する。
  380 tests の旧全体結果は以下の履歴であり、現在の426 testsの記録と区別する。

## 2026-09-05 限定依存更新

- KUC registry `0.3.5 -> 0.3.6`、source-closure/release ref、release-blocker契約を同期する。generic KUC ownershipは維持し、KUC #40のfull artifact未実装は未完了のままとする。
- 隔離probeでKUC `0.3.6` の `egui + text-raster` をRust `1.97.1`で実コンパイルし、`tinyvec 1.13.2`（`fontdb -> cosmic-text/resvg`）が通過したため、`1.12.0` pinは解除する。
- `publish-crates.sh` を含む CI / release 実行契約も `0.3.6` に同期した。

## 完了条件と境界

KatanA editor 全機能とユーザー追加要件を対象に、要件の網羅性、実装、
full Storybook、三 OS の KLE public input と KUC 表示・AccessKit・KLE の
観測可能な結果を検証し、crates.io / GitHub Release の v0.1.0 公開と
後片付けまで完了する。KatanA 固有の最終効果は公開後の別 Issue で検証する。
部分的なテスト合格や動画だけで全機能互換と判定しない。

- KLE は KUC の汎用 UI と opaque root を利用する。
- 汎用描画、入力、絵文字、動画合成の不足は KUC の Issue 経由で扱う。
- sibling repository の編集はこの再開作業に含めない。
- KatanA #336 は KLE 公開後の採用と host E2E。KLE のソース解析・公開の前提ではない。
- 既存 staged / unstaged 差分と別 worktree を保持する。
- 実装は Spark、利用不可時は Luna、複雑な原因調査は Terra に分担する。

## 現在の根拠

| 項目 | 再確認した事実 | 判断 |
| --- | --- | --- |
| KLE 作業場所 | `release/v0.1.0`、HEAD `be313bf428fc5577813701e42bd18303c69cad71`、大量の既存未コミット差分 | 公開可能な履歴への整理が必要 |
| 要件解析 | 固定 KatanA SHA `4f6a6287c650a38633c7baeb544a92e739c68567`、source-universe / root / alias 台帳と validator が存在 | 台帳の存在は完全な実行証跡ではない |
| KUC 公開版 | crates.io `0.3.6`、GitHub Release は 2026-09-05T00:24:07Z 公開 | 0.3.5 公開待ちという旧記録を更新する |
| KUC 0.3.6 の内容 | PR #42: canonical text-face selection と raster host 設定伝播 | #40 の consumer-defined artifact plan 完了とは別 |
| KUC #40 | Open、コメントなし | 全 source-derived leaf の artifact 接続は未達 |
| Floem #1086 | Open、コメントなし | future-incompatibility の解消を再確認する必要がある |
| KLE #9 / #11 | 両方 Open | hooks / 公開後整理、三 OS CI は残作業 |
| KatanA #336 | Open | KLE 公開後の実 host 検証を別 Issue で行う |

## 前回報告の扱い

- 2026-09-04 の 374 tests 等は当時の部分検証。0.3.6 取り込み後の証跡に流用しない。
- motion artifact は frame-000 から frame-091 まで生成後に観測期間中の
  ファイル更新がなく、処理を中断した記録がある。stack sample、内部処理時間、
  更新前後の lockfile 比較がないため、無限ループ、KUC の不具合、依存更新による
  回帰という原因は未確定として再調査する。
- `tinyvec 1.13.2` 非互換という旧断定は撤回する。Rust `1.97.1` の隔離 probe と
  更新後 workspace check が通過し、`1.12.0` 固定を解除した。
- 過去の local renderer 残存記録は、現行実装の定義と到達性を確認してから
  Task 化する。古い文書だけを根拠に完了済み作業をやり直さない。

## 実行順序

| ID | 状態 | 必要 Task | 完了証拠 / 依存 |
| --- | --- | --- | --- |
| R1 | 完了 | 作業台帳と実装の差異を整理 | 古い記録と現行残件を分類し、再開時の入口を一本化 |
| R2 | 完了 (ローカル) | KUC 0.3.6 と Rust 依存を更新 | registry manifest / lock / CI ref 同期済み。関連 tests / AST / clippy、更新後の全 workspace 374 tests 合格。公開ゲート全体は未完了 |
| R3 | 完了 | motion 出力の長時間処理を診断・限定改善 | NeuQuant profile 採取、依存のみ opt-level=3、46 frame の変更前後 GIF / MP4 / source / decode hash 一致。full editor 完了とは別 |
| R4 | 実装中 | 全ソースから canonical closure と leaf 対応を生成 | T1/T2/T3と直接参照診断を実装・検証。次は未結合branch/未解決actionの意味分類とcanonical leaf接続。KUC #40 / KatanA #336 待ちにしない |
| R5 | 未完了 | KLE の全操作と full Storybook を接続 | 検索 / 置換、Markdown controls、gutter、history、clipboard、diagnostics、view / tabs、AccessKit、IME の個別証拠。汎用 artifact plan は KUC #40 |
| R6 | 未完了 | 三 OS CI と公開 RC ゲート | KLE #11。同一 run の profile / source / leaf / artifact 相関、strict tests / coverage / package |
| R7 | 未完了 | 公開境界を整合し、Draft PR、レビュー、v0.1.0 公開 | 下記の文書 / validator 矛盾を修正。KLE 所有の全 leaf 検証は維持。P0/P1 解消、required checks、tag / Release / registry の取得確認 |
| R8 | 未完了 | 公開後 cleanup | KLE #9 hooks、不要 local / remote branch / worktree 整理、default branch switch と pull |
| R9 | 別 Issue | KatanA の公開版採用と全機能 host E2E | KatanA #336。実入力から buffer / state / file など最終効果まで検証。KLE 公開を待たせない |

GitHub Actions の依存更新候補は既存 KLE PR #12、Rust 更新候補は PR #14 にある。
Rust 更新後に差分を照合し、既存 PR と作業を重複させず統合する。
PR #14 の Rust 7 依存は現行 lock が同等以上であることを確認した
（egui 0.36.1、ignore 0.4.33、syn 3.0.5、toml 1.1.5 など）。まだ正式統合していないため
PR は閉じない。Floem の default branch 最新 commit も採用中の
`778bb5f2aa08429e579ee2e6ac97e84fbf18b618` と一致した。

### 公開境界の文書矛盾

ユーザーの「KatanA #336 は KLE 公開後の別 Issue」という指示と
`docs/v0-1-0-release-integration-design.md` の公開順を基準とする。
後発 `docs/v0-1-0-release-candidate-host-adoption-design.md`、tasks 13.4a と
現行 `--mode full` が KatanA host effect を final v0.1.0 の前提へ戻しており、
未整合だった。この再開台帳の初稿にも同じ前提を引き継いだため訂正し、
`kle-release` と公開後の `downstream-full` を実装・テストした。
KLE 所有 leaf の public input / KUC frame / AccessKit / observable result は
一つも省略せず、公開後の KatanA 固有 effect 検証だけを別責務へ分離する。
文書修正だけでなく mode の negative tests と実コマンドの失敗理由を確認した。

## 検証記録

再開後の実行結果をここへ追記する。実行中、中断、未実行は合格に含めない。

- 再開時: `just --list`、KLE worktree / HEAD、関連 Issue、KUC registry / Release を確認。
- `PlatformTextSurface` / `LineGutterModel` / `StorybookFallbackRenderer` の実装は
  現行 KLE に残っていない。禁止検査の fixture と未コミット削除差分を、実装残存と
  混同しない。現行 Storybook は KUC root receipt / `MotionArtifactWriter` を利用する。
- writer は staging 作成後に scratch で正規化 PNG、GIF、ffmpeg probe、source hash、
  MP4 encode / decode、manifest を処理してから公開先へコピーする。以前の未完了
  run の空 staging は、この区間を絞る証拠であり無限停止の証拠ではない。
  registry 0.3.5 / 0.3.6 の writer 実装は同一である。
- KLE GitHub Release 一覧は空、registry `katana-language-editor@0.1.0` は未公開。
- `openspec validate v0-1-0-language-editor-extraction --strict` は再開台帳追加後に合格。
- `just update`、workspace check、release-gate 40 tests、fmt、release shell 構文検査が合格。
  Rust 依存を KUC を含む 12 packages 更新した。`block 0.1.6` の Cargo
  future-incompatibility 警告は残存し、Floem #1086 の対応完了とは判定しない。
- `just lint`、`just ast-lint`、`RUSTFLAGS='-D warnings' cargo build --locked -p kle-storybook`
  は合格。`KATANA_REPO` 未設定の `just --dry-run update` も成功した。
- source-closure CI の docs trigger を alias / source-universe / 要件台帳の単独変更にも
  適用し、YAML の push / pull_request 両方のマッチ条件を確認した。
- 中核 API 7、egui 接続 16、parity checker 239 の計 262 tests は全件合格。
- 最適化設定を含む最新差分で `just unit-test` は 374 tests / 10 suites、96.51 秒で合格。
- R4 T1 と fixture 分離の追加後、最終 `just fmt-check unit-test lint ast-lint` は exit 0。
  workspace 380 tests / 10 suites、128.87 秒、clippy 0 errors（既知の Cargo future warning 1 件）、
  AST 2 tests + workspace boundary 1 test が合格。これを今回の最新コードの結果とする。
- `just katana-parity-rc-check` は exit 1。canonical source-closure artifacts が未生成という
  必須証拠不足であり、単体 tests の合格とは分離する。判定条件を緩和しない。
- 旧断定と検証結果は KLE [Issue #8 の訂正コメント](https://github.com/HiroyukiFuruno/katana-language-editor/issues/8#issuecomment-5548633750) に記録した。

## R3 の限定改善設計

- 46 frame の診断 run を開始し、frame 生成後の実プロセスから 5 秒の stack sample を採取。
  主スレッド 3804 samples のうち 3657 が KUC GIF writer の `color_quant::NeuQuant`
  にある。デバッグビルドの色数変換が支配的であり、停止や依存回帰の証明ではない。
- 変更候補は KLE workspace の `[profile.dev.package.color_quant] opt-level = 3` のみ。
  KUC writer、入力、画像、量子化パラメータ、frame 数、合否条件は変更しない。
  汎用処理は引き続き公開 KUC 所有で、ローカル実装へ複製しない。
- 現行 46 frame run の完了後、最適化設定で同じ run を実行し、source PNG / GIF /
  MP4 decoded hash と frame 数を照合する。不一致や品質条件未達なら採用しない。
- この artifact は部分的な headless 検証であり、実 OS 入力、実 KatanA、full editor
  全 leaf の検証結果には数えない。
- 比較結果: 変更前 run は完了済み。出力 directory birth から completion marker まで
  434 秒、変更後は同じ計測区間で 84 秒（process 全体の `/usr/bin/time -p` は 89.07 秒）。
  単一 run 比較であり普遍的な速度保証ではない。source PNG、source/decode frame hash、
  semantic evidence、GIF、MP4 が一致し、manifest の差は出力 path とそれを含む canonical hash のみ。
- 実出力: `target/acceptance/kle-motion-resume-20260905-r2/variable-viewport-motion.mp4`。
  GIF SHA256 `2a3f9a442b7ef1ab206391b918f802eb6b13bf7b5a0e542786747a25c43b0e2c`、
  MP4 SHA256 `8d86f9dcb5867589a567f59314ed8c4605f9612667ad2e49f9da749d69d8da3c`。

## R4 の現状と次の手

- canonical source-closure output は未生成。現存する実 profile は
  `target/source-closure/local-20260904-macos` だけで三 OS 同一 run input ではない。
- 現行 `branch_catalog.rs` は全 branch を未分類のまま残し、`leaf_join.rs` は
  同じ source file に現れた branch / action の候補を生成するが、要件分類、visible path、
  KUC binding、効果、Storybook stage の結合は未完成である。
- 実行証拠と Storybook artifact は materializer が生成しない。
  source/action/branch/leaf の候補生成 tests が通っても完全性の証明にはならない。
- 次の実装は source SHA/span を根拠とした branch / action / handler / terminal candidate
  と要件分類の結合。まず限定設計を `docs/v0-1-0-source-leaf-binding-design.md` に固定する。
  三 OS input 不足は公開証拠の制約であり、このソース結合実装全体を停止する理由ではない。
- 設計レビュー後、T1 の検証済み alias 入力 API と回帰テストの実装を Luna medium へ委譲。
  leaf status、canonical artifact、validator、UI の変更は対象に含めない。
- T1 初回レビューで追加 tests の `unwrap()` と既存の cross-collection duplicate 検証の
  欠落を検出した。`just lint` も 18 errors で拒否したため、初回 worker の focused tests /
  cargo check 合格だけでは統合せず修正を委譲した。
- T1 完了: `verified_alias_entries()` / `validated_entries()` が既存の分類検査を共有し、
  requirement ID / short reference / source path の順で検証済み入力を返す。
  schema / revision、欠落、内部重複、外部分類との重複、未知 path、外部参照除外、順序非依存を
  7 tests で検証した。70 entries は現在の short-reference 入力数であり全機能数ではない。
- レビュー指摘後の fmt 差分と空集合で成立するテストを修正し、最終全体チェックで合格した。
  次は設計 T2 の branch 対応と unbound 検出。candidate を passing に昇格してはいない。

## 再検証中に見つけた自動化の弱点

- 再検証の一回は disk の `No space left on device` で compile 失敗。今回の隔離 probe
  target だけを CACHEDIR.TAG / manifest 確認後に `cargo clean` し、164.1 MiB 解放した。
  既存ソース、差分、動画は保持。後の `df` は 63 GiB 空きであり、継続的な枯渇とは断定しない。
- 別 run は `rejects_indirect_simulator_harness_call` が意図した不正入力を拒否せず失敗した。
  単独と capability manifest 14 tests の再実行は通過した。単発の原因は未確定。
- `FixtureBuilder::root` は PID と wall-clock timestamp だけでパスを決め、作成時の
  排他的予約をせず返している。時計値が衝突すると他 test の内容や cleanup が干渉し得る。
- 限定修正設計: test fixture のみ、process-local sequence と `fs::create_dir` による
  所有権確保を加える。作成失敗した既存 path は所有・削除しない。実時間の速さに依存せず
  同一 timestamp で複数 fixture を作成し、固有 path と書込内容・drop の分離を検証する。
  商用 validator と判定基準は変えず、単に再試行して合格したことを安定性証拠にしない。
- 上記を実装し、同一 timestamp の分離と既存 payload 保全の 2 回帰 tests を追加。
  capability manifest 16 tests と最終 workspace 380 tests が合格した。
  元の単発失敗が timestamp 衝突によるものだったという因果までは証明していない。
- egui の 16 / 17 tests の差は `storybook-artifacts` feature によるものと確認した。
  当該ファイルの並行変更は確認していない。

## 関連 Issue

- KLE: [#8](https://github.com/HiroyukiFuruno/katana-language-editor/issues/8)、[#9](https://github.com/HiroyukiFuruno/katana-language-editor/issues/9)、[#11](https://github.com/HiroyukiFuruno/katana-language-editor/issues/11)
- KUC: [#40](https://github.com/HiroyukiFuruno/katana-ui-core/issues/40)、[v0.3.6](https://github.com/HiroyukiFuruno/katana-ui-core/releases/tag/v0.3.6)
- host: [KatanA #336](https://github.com/HiroyukiFuruno/KatanA/issues/336)
- 依存: [Floem #1086](https://github.com/lapce/floem/issues/1086)
