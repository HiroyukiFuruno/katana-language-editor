# Tasks: katana-language-editor v0.1.0

## 2026-09-05 再開時の入口

- [x] カーソル下流の直接callHierarchy診断とmodifier参照を追加した。実LSP3rootから19target record/33callsiteを採取し、epaint8targetは固定Cargo.lockのarchive、tar member、展開済みbytes一致時のみsource identityとして認証する。target名の意味解決、再帰closure/native/KUC比較は未完了。

- [x] カーソル移動の限定参照を追加した。認証済みcursor_rangeへの実LSP4辺と構造化owner rangeを確認し、実eguiの選択解除/Shift選択/HomeEnd/command+A/未押下/Unicode位置の8参照を追加。main全体601成功/1失敗（検索Close）、静的検査/Python23/doctest41成功。全navigation分岐、KUC比較、native/AXの未解決範囲は保持する。

- [x] 外部仕様解析の完了誤判定を修正した。canonical/diagnostic共通producerで未採取の推移的semantic closureを保持し、全認証Rust sourceのSHAを出力する。公開validatorの外部依存未検査も修正し、RC/KLE公開/採用後の各modeで欠落・未完了を拒否する。main最終静的検査/41 doctests成功、全体593成功/1失敗（検索Close）。実診断r29/r30一致、114file/5symbol/14invocation。全semantic解析・実入力は未完了。

- [x] 色診断literal/hint要件を実装した。構造化field・escape付きreport、同一SourceFileのAST span原文、UTF-8/CRLF/複数行、empty/範囲外spanのfail-closedと部分結果破棄を検証。明示specのhsl/hsla検出漏れも修正。最終静的検査/41 doctests成功、全体573成功/1失敗（検索Close）。editor互換性や公開の完成ではない。
- [x] 旧theme5型とroot/types exportを除去した。10独立compile-failとstyle所有権ASTを検証し、literal color lintの案内をKUC/host境界へ同期。発見したallow/path名によるproduction検査回避も撤去し、属性/実SourceFile fixtureで回帰した。最終静的検査/41 doctests成功、全体559成功/1失敗（検索Close）。色roleと切替の実証拠は未完了のまま。
- [x] generic presentation field除去: EditorConfig/Inputのtheme/strings/localeを除去し、未接続の旧StorybookConfigFactoryを撤去した。6独立field拒否/constructor正例/ASTを検証、最終静的検査/31 doctests/native smoke成功、全体545成功/1失敗（検索Close）。色・翻訳・RTLの機能要件はKUC/hostの未完了証跡として維持する。
- [x] 限定依存更新: KUC registry `0.3.5 -> 0.3.6`、CI/ref、release-blocker契約を同期した。KUC #40のfull artifact未実装は完了扱いにしない。
- [x] 2026-09-07 compatible dependency update: `cargo upgrade -i` / `cargo update`でKUC registry requirementとlock解決を`0.3.6 -> 0.3.7`へ更新し、crossbeam / ipnet / redoxの最新互換版をlockfileへ解決した。release / source-closure workflowと公開契約も同じKUC版へ同期し、`cargo check --workspace --all-targets --locked`を通過した。KUC Storybook contractは`Continuation(Search(CloseNotApplied))`を再現して失敗し、KUC #44をKLEで隠蔽しない。Floem HEADは依然`wgpu -> metal -> block 0.1.6`を含みfuture-incompatibilityを残すため、依存更新をrelease完了とは扱わない。
- [x] 2026-09-10 compatible dependency update: 公開registry `katana-ui-core 0.3.8`へrequirementとlock解決を更新し、local KUC manifest実行をKLEのregistry consumer contract gateから除去した。macOSで`cargo run -p kle-storybook -- --contract-check`と`cargo test -p kle-storybook --locked kuc -- --test-threads=1`（4 passed）を確認した。KUC #40の`ConsumerArtifactPlanV1`はopaque presentation tokenを直接要求する一方、neutral host projection leaseからtokenを渡すpublic bridgeを提供しないため、KLEがtoken/RawInput/rendererを偽造せずにconsumer-defined full artifactを発行することはまだできない。この不足をKUC taskへhandoffし、三OS artifact evidenceは未完了のまま維持する。
- [/] 2026-09-12 registry dependency update: 公開registry `katana-ui-core =0.3.9`へexact pinとlock解決を更新し、`just update`で残る直接・推移依存も最新互換版へ更新した。KUC公開bridge `ConsumerArtifactStageBinding::from_host_projection_lease`だけでfull editor sequenceを組み立て、KLEがtoken/RawInput/renderer/font policyを所有しないconsumer artifact pathを追加した。`cargo test -p kle-storybook --locked -- --test-threads=1`は43 passed、source-closure/release-gate静的テストも通過した。しかしmacOSの実行`just kuc-contract-check`は公開KUC `ConsumerArtifactPlanIssuer::new()`のdefault Unicode optionsがunpinのため`ColorEmojiUnpinned { profile_id: "macos" }`で停止する。KLEでplatform font hashを補完せず、KUC taskへhandoffした。`release-blocker-audit`は`kuc-consumer-artifact`としてこの実行失敗を必ず列挙する。KUC修正版の公開とmacOS/Windows/Linuxの正確な`⭐️`色glyph・IME・計測・hit-test evidenceが揃うまで本項とreleaseは未完了。
- [/] 2026-09-14 KUC #57 acceptance: 公開registry `katana-ui-core =0.3.11`へのみ更新する。KLEはconsumer側からplatform policy/font hashを注入せず、`ConsumerArtifactPlanIssuer::new()`のdefault issuerを使う`just kuc-contract-check`で、full editor sequenceの日本語IME・正確な`⭐️` (`U+2B50 U+FE0F`)・計測・hit-test evidenceを再受入する。KUC Release v0.3.11 / crates.io の公開 checksum `45eacea577b7a36cbdc7aecbcabbe56ebce11239ae9e6d36f4f244fdabbf6557` を受領済み。実行結果と三OS evidenceは本行の後続検証で確定するまで未完了とする。
  - 2026-09-14 macOS registry consumer acceptance: `just kuc-contract-check` は exit 0。`kle-storybook --contract-check` が immutable artifact `target/acceptance/kle-storybook-consumer-artifact/run-61701-1789330717150407000` を生成し、focused KUC suite は 5 passed / 39 filtered / 179.05s で終了した。KLE は default issuer のみを用い、font/hash/platform policy を注入していない。Windows/Linux 実入力・色glyph evidence と KLE release gate は未完了のまま維持する。
- [/] 2026-09-13 KUC #54 execution: registry source と checksum `1180cf3ff66d8f030179f60633aec012885047434676c85db4eca38f27d69d54` を lockfile で確認し、`just kuc-contract-check` は終了コード0で通過した。KUC public default issuer に対する consumer artifact は immutable run directory `target/acceptance/kle-storybook-consumer-artifact/run-3455-1789261055299844000` に出力され、`cargo test -p kle-storybook --locked kuc -- --test-threads=1` は 5 passed / 39 filtered / 113.07s。KLEはKUCが所有する retained action target だけを opaque binding に用い、font policy/RawInput/rendererを追加していない。macOS local accept は成立したが、Windows/Linux実入力と色glyphの独立証跡、KLE full release gateは未完了とする。
- [/] 2026-09-13 dependency compatibility blocker: `just release-blocker-audit` は KUC consumer artifact blocker を列挙しなくなったが、Floem main `778bb5f2aa08429e579ee2e6ac97e84fbf18b618` の `wgpu 27.0.1 -> metal 0.32.0 -> block 0.1.6` 経路を future-incompatibility として fail-closed で列挙した。Floem HEAD は同revisionであり、KLE local `[patch]`、監査除外、lint緩和は行わない。公開済み上流更新を取り込むため [Issue #15](https://github.com/HiroyukiFuruno/katana-language-editor/issues/15) を起票した。解消後は依存更新と全strict gateを再実行する。
- [x] 2026-09-20 source-closure / cross-platform gate repair: `--canonical-root artifacts` に対してpublication側が `artifacts/` を二重付加していたため、canonical destinationを `artifacts/v0-1-0/source-closure-input/<fixed-katana-revision>` に修正し、出力先を固定する回帰を追加した。Windowsではdiagnostic fixtureがUnix絶対pathをURI化しており、`Path.as_uri()`が相対pathとして拒否されたため、全fixtureをOS絶対file URIへ統一し、epaint URI parserもWindows drive pathを保持する。CIで次に検出したsource/archive認証の相違は、fixtureの`write_text()`がWindowsでCRLFを生成しtar memberのLF bytesと異なることによるため、fixtureも`write_bytes(utf8)`へ固定した。運用のbyte-exact認証は緩和しない。release-preflight/release workflowには固定KatanA SHAのread-only checkoutを加え、source-bound unit testの参照rootを保証する。さらにCIで検出したhard-coded local KatanA pathを固定revision cacheへ置換し、workflow checkoutとunit testの参照rootを同一化した。test-and-buildもKLEと固定KatanAを兄弟checkoutとして固定し、KLEのAST検査が外部参照を走査しないことをworkflow契約と4件の回帰で検証する。diagnostic 37件、workspace 649件/41 doctest、fmt、strict Clippy、AST、release-gate 50件、publication contract 5件は成功した。実3 OS workflow証跡はpush後に再評価し、Floem #15は未完のまま維持する。
- [x] 隔離probe: KUC `0.3.6` の `egui + text-raster` をRust `1.97.1`で実コンパイルし、`tinyvec 1.13.2`を確認。`1.12.0` pinは不要と判定。
- [x] `scripts/release/publish-crates.sh` を含む公開契約を KUC `0.3.6` に同期した。
- [x] R4 T1: 検証済み requirement source alias の入力 API と 7 回帰 tests を実装した。全 branch との結合・要件網羅性の証明は後続 T2 以降であり未完了。
- [x] test fixture の同時刻衝突と既存 path の誤削除を防ぐ排他的予約、および 2 回帰 tests を追加した。最終 fmt / workspace 380 tests / lint / AST 検査は合格。
- [/] R7 公開境界を実装する。KLE 公開前は source-derived 全 leaf、三 OS profile、KUC opaque receipt の `kle-release` evidence だけを fail-closed で検証し、KatanA #336 host E2E は final v0.1.0 公開後の `downstream-full` に分離する。KLE 所有の全 leaf 検証は減らさない。KUC #40 published producer が未提供のため `kle-release` は現在も意図して失敗する。

最新の状態・作業順・検証結果は `docs/v0-1-0-resume-plan-2026-09-05.md` を参照する。
- [x] neutral provider接続: trait/errorをneutralへ一本化し、adapterは実KUC Leaseの等価制約と同一定義の再exportを使う。neutral consumer、型同一性、6 doctests、実KUC同期回帰を確認。全体unitは検索Closeで535成功/1失敗のため公開完了ではない。
- [x] provider定義所有権のAST検査: 正規neutral module外の同名trait/型の再定義を拒否し、re-exportと文字列は許可する。nested/private/raw identifier、tests名のpath、正規owner、通常rule runnerの適用を5回帰で検査し、mainのfmt/Clippy/AST再検証で成功。全機能互換や別名の意味的複製までを証明するものではない。
- [x] R4 T2/T3: source SHA / branch span / action definition-construction-dispatch の候補を結合し、未対応を保持する診断CLIを実装した。要件表の完全/相対/brace/directory参照も同じ診断へ接続し、145行・215 source bindingを出力した。候補をpassingへ昇格しない。
- [x] 追加差分の全体検証: workspace 426 tests、fmt、strict Clippy、AST、coverage (line 76.20%) が合格。固定ソース診断2回のJSON bytesが一致し、KatanA checkoutはcleanのままである。
- [ ] R4 T4: 44の未解決要件行、7,358の未結合構文候補、未解決action/外部依存を意味分類し、全source-derived leafへ接続する。診断件数を機能数や達成率に読み替えず、KUC #40 / KatanA #336待ちにはしない。
- [x] R4 T4 限定参照修正: preview/diagnostic の暗黙参照・誤パス、診断の参照上書き、完全修飾AppAction候補の欠落を修正した。最新428 tests/fmt/strict Clippy/AST合格。r9/r10診断はbytes一致、未解決要件参照23行・未結合構文7,285・修飾action未解決80件。意味解析と実装証拠の完成ではない。
- [x] R4 T4 join入力整合性: 全root14項目、source path/branch ID重複、未走査source参照を結合前に拒否する。空集合のroot変異と正常no-matchを検証し、435 workspace tests/fmt/strict Clippy/AST合格。
- [x] R4 T4 canonical joinの生成・整合性接続: materializerは構造化inventory/T2/T3から候補を生成し、全reportをleaf-manifestの必須payloadとして保持する。再構築照合、候補とleafの1対1 provenance、欠落/未知field拒否、未解決を残したrelease拒否を実装した。旧文字列joinはtest-only。459 workspace tests/fmt/strict Clippy/AST合格。意味分類・実入力・公開の完成ではない。
- [ ] R4 T4 semantic enrichment: 構造化候補を根拠付きのobservable leaf、KUC公開入力/opaque outcome/Storybook stageと結合する。現producerのunresolved action/UI/effectを実証拠なしでpassingへ変更しない。T3のsource facts全採取とartifact公開時の中断耐性も別途検証する。
- [x] R4 T4a action origin限定投影: 構造化candidateの検証済みdefinition/construction/dispatch factsからのみ`katana:AppAction::<variant>`をcandidate leafへ投影した。空/不正variant、candidate provenance改竄、重複を拒否する。handler/effect/KUC/Storybook/host evidence、terminal status、release validatorは変更していない。
- [x] R4 T4b unbound branch構造分類: materialize済み`BranchRecord`のsymbol/kind/condition/profile/cfg predicate/incoming edge factsを未結合記録へ保持した。各fieldのコピーとfingerprint変化を検査する。binding、reachable effect、KUC/Storybook/host evidence、candidate status、release validatorは変更していない。
- [x] R4 T4c unresolved scan edge構造分類: `SourceActionUnresolved`へ既存scan edgeのkindを専用fieldとして保持し、reason文字列を再解析せず分類する。空白のみのkindを拒否し、sort/fingerprint変化を回帰検査した。action route/effect/KUC/Storybook/host evidence、status、release validatorは変更していない。
- [x] R4 T4d unresolved scan edge詳細構造分類: scan stateの`ClosureEdge.to_symbol`を`source_edge_detail`として保持した。kind/detailの空白値を拒否し、sort/fingerprint/順序を回帰検査した。detailをtarget、解決、到達可能性、action route/effect/KUC/Storybook/host evidence、status、release validatorへ昇格していない。
- [x] R4 T4e lexical resolution結果構造分類: `LexicalResolution`由来のclosed statusをedge生成時に保持し、scan edgeへ転記した。unknown enum decodeを拒否し、sort/fingerprint/順序を回帰検査した。to_path、外部dependency、実行可能性、action route/effect/KUC/Storybook/host evidence、status、release validatorへ昇格していない。同名free functionの名前解決はshadowing scopeを証明できないため対象外とする。
- [x] R4 T4f source closure既走査target back edge: target pathが既に同一scan stateでrecord済みならpendingへ追加せず、false unresolvedを防ぐ。候補pathはcanonicalizeしてcase-insensitive filesystem上でもinventoryと同じ実体pathへ正規化する。既走査back edgeと正規化pathを回帰検査し、fixed KatanA r7で`local` false unresolved 0件を確認した。未走査/失敗target、source hash、edge kind/symbol/effect、実行到達性、action route/KUC/Storybook/host evidence、status、release validatorを変更しない。
- [x] R4 T4g external TextEdit builder-to-show entry: 明示的な`TextEdit::multiline`由来の同一closure scope局所bindingだけから`show`呼出しを採取し、egui source definitionとKatanA invocationを追加した。shadowing、outer/inner closure越境、別型の`show`を回帰で拒否し、fixed KatanA r10で`text_edit.rs:66`の1辺を確認した。一般型推論、macro越境、show内部の推移、input/IME/layout/effect、KUC/Storybook/host evidence、status、release validatorを変更しない。
- [x] R4 T4h-a external direct semantic edge: 認証済みegui sourceのseed definitionから、同一file free function、source集合で一意なassociated function、明示型parameter receiverのmethodだけを構造edgeとして採取し、fingerprint入力へ追加した。closure/untyped receiverを拒否する回帰を追加し、fixed KatanA r12で`load_state -> TextEditState::load`、`store_state -> TextEditState::store`、`show -> TextEditState::load`を確認した。全推移、input/IME/layout/effect、KUC/Storybook/host evidence、status、release validatorを変更しない。
- [x] R4 T4h-b same-scope exact initializer: 同一blockで先行する、source集合で一意かつ明示return typeが`Type`/`Self`の非chain associated callだけをplain bindingの型として保持した。shadowingで型を消去し、closure/untyped receiver/chainを拒否する回帰を追加した。fixed KatanA r13では`TextEditState::load(...).unwrap_or_default()`を型確定と誤認せず、direct edge数12件と`store_state -> TextEditState::store`を確認した。全推移、input/IME/layout/effect、KUC/Storybook/host evidence、status、release validatorを変更しない。
- [x] R4 T4h-c derived Option wrapper and clone chain: source集合で一意な`Option<Type|Self>` return、同一sourceの一意な`#[derive(Default)] struct Type`、引数なし`unwrap_or_default()`だけをplain bindingの型として採取した。さらに一意な`#[derive(Clone)]`を確認した同型の引数なし`clone()`一段だけをtype-preserving receiverとして許可し、全他chainを拒否する回帰を追加した。fixed KatanA r14で`TextEdit::show -> TextEditState::store`（builder.rs:951）を追加し、direct edge数13件・`complete=false`を確認した。全推移、input/IME/layout/effect、KUC/Storybook/host evidence、status、release validatorを変更しない。
- [x] R4 T4h-d unique Ui reference receiver: `TextEdit::show`の明示`&Ui` parameterから、source集合で唯一のnon-generic `struct Ui` と唯一のnon-generic inherent `Ui::ctx(&self)`だけをdirect edgeに採取した。trait/alias/複数候補、`&&Ui`、qualified/generic type、shadow、macro、destructuring、deferred scope、receiver chainを拒否する回帰を追加した。fixed KatanA r16で`TextEdit::show -> Ui::ctx`の4span（builder.rs:534/930/951/990）を確認し、`complete=false`を維持した。ctxの戻り値・その先の呼出し、input/IME/layout/effect、KUC/Storybook/host evidence、status、release validatorを変更しない。
- [x] R4 T4h-e inner Ui-to-Context receiver: `TextEdit::show`の明示`&Ui` parameterから、唯一のnon-generic inherent `Ui::ctx(&self) -> &Context`と唯一のnon-generic inherent `Context::method(&self, ...)`を確認した`ui.ctx().method(...)`の内側callだけをdirect edgeとして採取した。outer chainは解析せず、trait/alias/複数候補/generic/値receiver/shadow/macro/closure/patternを拒否する回帰を追加した。fixed KatanA r17で`TextEdit::show -> Context::layer_transform_to_global`（builder.rs:930-932）を確認し、`complete=false`を維持した。outer chainの結果、input/IME/layout/effect、KUC/Storybook/host evidence、status、release validatorを変更しない。
- [x] R4 T4h-f invoked local closure callback: `TextEdit::show`直下の一意なlocal closure bindingが、同一fileで一意なfree functionを呼び、同じblockから直接invokeされる場合だけcallback内のfree callをdirect edgeへ採取した。closureの実行回数、captured state、引数、branch、input/effectを解決せず、nested closure/macro及び同名bindingを受理しない。fixed KatanA r18で`TextEdit::show -> events`（builder.rs:565-582）を確認し、`complete=false`を維持した。
- [/] R4 T4h external direct semantic edge: 未分類wrapper/method chain、複数候補、cross-file unqualified function、field/tuple/pattern binding、shadowing/closure/macro/trait/dynamic dispatchの構造的な分類と、全推移への接続は未完了。T4h-a/b/c/d/e/fだけでsemantic closureを完了扱いにしない。
- [x] 構造化T2/T3の診断接続: `source_leaf_candidates`を実装し、全root/fingerprint/source SHA/span、重複・未知参照、精度過大表示を検査する。未結合項目も保持する。最終445 tests/fmt/strict Clippy/AST合格、実診断r14/r15のJSON bytes一致。canonical採用や実行証拠の完了ではない。
- [ ] R4 T4 外部semantic closure: 認証済み全114Rust fileのSHA保持と、5入口だけでcompleteにしない共通producer/public validatorを実装済み。`cursor_range.on_event`、input filtering、text mutationへの推移的解析と各branch/実行leaf照合は未完了。symbol evidenceは依然5入口に限定される。固定registry checksum/source SHAと各branch根拠が必要で、KUCの汎用実装をKLEへ複製しない。
- [x] 固定egui削除の限定実行参照: 検証専用dev-dependencyと実Context/RawInput/TextEdit/Stringによる8テストを追加した。lock identity、通常削除、選択、read-only、境界の本文不変/changed通知、VS16削除を確認した。KUCのgrapheme要件を弱めず、全delete-branch-familyや三OS実入力の合格証拠にはしない。最新全499 Rust testsと17診断tests、fmt/Clippy/ASTは成功。
- [x] 外部source identity: 固定Cargo.lockのSHAからregistry archiveを認証し、展開先の全Rust path集合とbytesを照合してからASTへ渡す。vendor用checksum台帳の必須化を撤回し、存在時のみ追加の厳格な整合性制約とした。不正path/link/破損footer/サイズ上限、追加・欠落・同時改変を回帰検証した。全486 tests/fmt/Clippy/AST合格、実診断r21でも認証済み5入口を採取した。同時filesystem差替え保護と推移的semantic closureは未完了で、この項目の完了を全機能踏襲と扱わない。
- [x] 外部shortcut入口の修正: 存在しないContext::consume_shortcutではなく、固定KatanAのContext::input_mut closureからInputState::consume_shortcutへ結合する。型由来・shadowing・module import scope・独立seed採取の回帰を追加した。一般Rust型推論と実入力を証明するものではない。
- [x] 固定KatanA invocation診断接続: 同じ固定blob照合済みbytesをvisitorへ渡し、本番Event::Pasteのtuple patternを採取する。未走査だったvendor egui-winitの入力rootを追加し、Git blob identityでdeclared moduleも走査する。491 tests/fmt/Clippy/AST、coverage77.65%、実診断r25/r26一致。14 invocationの採取は全推移解析や三OS実入力の完了ではない。
- [x] 外部pathの限定修正: checksum通常ファイル、POSIX相対path、symlink各要素、canonical containmentを事前検査する。実在する正しいSHAの外部sourceへのtraversal/symlink回帰を含め対象14 tests/main再検証合格。その後archive identityは上記工程で検証済み。同時差替え保護は引き続き未証明。
本書の過去の日付付き記録は当時の証拠であり、現在の合格を意味しない。
KUC 0.3.6 は公開済みで取り込みを開始する。KUC #40、canonical 全 leaf の証拠、
三 OS 実入力、正式公開と cleanup は引き続き未完了。KatanA #336 は公開後の別 Issue。
motion writer の長時間処理は GIF 量子化を実測し、同一出力のビルド最適化を確認した。
tinyvec `1.13.2` は実コンパイルに成功したため非互換の旧断定を撤回する。

## Branch Rule

- **標準ブランチ**: `release/v0.1.0`
- **作業ブランチ**: `feature/v0.1.0-task-x`

---

## 0. katana editor parity 要件を固定する

- [/] 0.1 KatanA が現在持つ editor 要件を可視化し、要件定義書 `docs/v0-1-0-editor-requirements.md` を作成する。初期台帳は作成済みだが、source-derived closure が未生成で、document search と別系統の workspace search（File Name / Markdown Content、include/exclude、history、result select/jump）、host action/data refresh を含む完全な source universe は未確定である。
- [/] 0.2 要件定義書と OpenSpec specs/tasks の差分をなくす。strict validation が OpenSpec 文書構造しか検査せず、canonical source closure/branch/leaf/execution/artifact manifest も未実装のため、完全同期は未達である。
- [/] 0.3 要件ごとに対応する自動検証を紐付け、検証不能な項目を release blocker として残さない。各要件の最低 leaf 表と release blocker は記録済みで、2026-08-27 に `katana-parity-check` の 192 tests は通過した。しかし canonical source-closure provenance は dirty KLE worktree を fail closed し、KUC manifest/lock 不整合により `just check` の KUC contract gate が失敗し、fixed KatanA revision は public KLE bridge を持たない。全 leaf の executable evidence は未接続である。
- [x] 0.4 要件書の short source reference を固定KatanA source identity に閉じる。2026-09-04 に machine-resolvable path の release-gated coverage audit に加え、`docs/v0-1-0-editor-requirement-source-aliases.json` が主表の短縮参照70件を requirement-ID 単位で固定 revision の exact source path へ分類した。`RequirementSourceAliasLedger` は missing/duplicate/stale/unused/external-boundary 偽装、source-universe/root-manifest 未到達を fail-closed にする。alias bytes は capture CLI の必須入力、root provenance の独立 evidence/fingerprint、materialized root identity、Justfile/workflow release-contract regression に接続し、改竄後の hash を合わせても checker ledger と異なる input を拒否する。source-closure 137件、release-gate 40件、AST lint、strict Clippy を通過した。canonical terminal artifact、三OS Storybook、KatanA host E2E の代替ではない。

---

## 1. neutral interface を確定する

- [x] 1.1 `LanguageEditor` trait と DTO（`TextContent` / `EditorConfig` / `CursorPosition` / `Selection` / `EditorEvent` / `EditorOutput`）を本実装向けに拡張する
- [x] 1.2 `katana-language-editor` の `cargo tree` に `egui` が含まれないことを確認する
- [x] 1.3 KatanA が interface crate のみを依存しても型エラーが出ないことを確認する
- [x] 1.4 `EditorConfig` のフィールドは theme / strings / typography / spacing / settings / syntax_highlighter を `Option` ではなく必須として持つ
- [x] 1.5 全 DTO に `#[non_exhaustive]` を付けて将来追加可能にしつつ、必須フィールドの追加で SemVer break が起きないこと（または起きる場合は意図的であること）を docs に明示する

---

## 2. テーマ DI を定義する

- [x] 2.1 `Theme { colors: ColorTokens, dark: bool }` と `ColorTokens`（前景・背景・選択・キャレット・ガター・ハイライト各種 / セマンティック alias 含む）を neutral crate に定義する
- [x] 2.2 `EditorConfig::theme: Theme` を **non-nullable** で受け取る（`Option<Theme>` を許容しない）
- [x] 2.3 host (KDV) 側で `kdv-presets` を提供する前提を docs に明記し、KLE 内に default preset を持たないことを確認する
- [x] 2.4 egui 実装は注入された `Theme` のみで描画し、`egui::Visuals::default()` 直参照を持たない

---

## 3. i18n DI を定義する

- [x] 3.1 `Locale`（言語コード + direction LTR/RTL）と `Strings`（editor が出す全 UI テキストのキー集合）を neutral crate に定義する
- [x] 3.2 `EditorConfig::strings: Strings` / `EditorConfig::locale: Locale` を **non-nullable** で受け取る
- [x] 3.3 host (KDV) が **en preset** を必須で渡す前提を docs に明記し、KLE 内に default 文字列を持たないことを確認する
- [x] 3.4 editor 実装にユーザー可視文字列リテラルが残らないことを CI で検査する（手動レビューでも可、後続 task でルール化）

---

## 4. タイポグラフィ / スペーシング DI を定義する

- [x] 4.1a 旧Typography/Spacingの型定義とroot/types exportを除去した。4独立compile-fail、neutral consumer、同名型再導入のAST拒否を検証した。main最終静的検査/10 doctests成功、全体543成功/1失敗（検索Close）。KUC描画/設定/計測自体の検証とは分離し、4.1全体は未完了。
- [/] 4.1 旧 neutral `Typography`（font family を含む）/`Spacing` は KUC `PlatformFontCatalog` と root-owned layout より前の残骸である。Spark は font discovery/fallback/raster/layout policy を KUC へ移し、KLE config が font family/spacing/pixel policy を所有しないことを AST/API で検証する。2026-08-23: KUC public text-command rootは同一frameの実測metricsを本文、automatic gutter、search input、primary/floating command chrome、context menuへ接続し、RawInputで開いたcontext menuを含むadapter 145件、KUC core 226件、text-raster 27件、format、対象crate strict clippyを通過した。KLE config除去、視覚数値gate、3 OS color glyph、Storybook/host integrationは未達のため本項は未完了。
- [/] 4.2 旧 `EditorConfig::typography` / `spacing` 必須 API は KLE に generic visual policy を残すため完了扱いにしない。host は KUC root に generic presentation capability を渡し、KLE は保存・解釈・fallback しない。
- [/] 4.3 旧 KDV preset 必須化は KLE v0.1.0 の source-of-truth ではない。KDV/KUC 共通の font/visual capability は KUC contract で閉じ、KLE release は KDV-specific preset を前提にしない。
- [x] 4.1/4.2/6.4 限定field除去: EditorConfig/Inputのtypography/spacing/settings fieldと利用側生成を除去し、ASTにprivate/raw identifierを含む再導入拒否を追加した。2026-09-05除去時532 tests/静的検査/coverage77.91%成功。公開型全体は未除去であり、続くStorybook同期修正後の最新全体結果は検索Closeの1失敗 (KUC #44) を含むため、全gate合格としない。

---

## 5. Host 制御 API を公開する

- [/] 5.1 旧 `EditorScrollControl` は KLE に logical row/offset/pixel/range を持たせるため v0.1.0 opaque-root 境界に違反する。Spark は KLE API/実装/テストを削除し、KUC retained scroll と host-issued opaque target のみへ置換する。API 定義済みは完了根拠にしない。
- [/] 5.2 旧 `EditorWriteAccess` は KLE content/range/origin mutation を許すため違反する。Spark は KUC single-consumption input transport と unchanged KatanA host mutation route へ置換し、KLE local write API を残さない。
- [/] 5.3 旧 `EditorViewControl` は KLE focus/read-only state を公開・保持するため違反する。Spark は KUC retained focus/input policy と host-projected capability に置換し、KLE は opaque event transit のみとする。
- [/] 5.4 旧 `EditorHistoryControl` は KLE history ownership を許すため違反する。Spark は KUC generic retained history UI と KatanA authoritative history route へ置換し、KLE の undo/redo state/API を除去する。
- [/] 5.5 旧 `EditorSelectionControl` は KLE cursor/selection/range ownership を許すため違反する。Spark は KUC retained selection と host-authoritative semantic selection route へ置換し、KLE selection API を除去する。
- [/] 5.6 旧 `EditorSearchControl` は KLE query/match/replace/regex semantics を許すため違反する。Spark は KUC `SearchControlStrip` retained UI と one-time transport、KatanA document search host route へ置換する。Replace / Replace All は固定 KatanA source に一般 UI/action route がないため、KatanA host 追加仕様判断まで release blocker とし、KLE local replace/search semantics や KUC への Katana 固有 API 持込みで埋めない。
- [/] 5.7 旧 `EditorClipboardControl` と host-injected `ClipboardBackend` は KLE clipboard ownership を許すため違反する。Spark は KUC payload-minimized transaction と unchanged KatanA native acquisition route へ置換する。
- [/] 5.8 旧 diagnostics/decorations sink は KLE diagnostic/range/hover ownership を許すため違反する。Spark は KUC generic panel/gutter presentation と opaque host-issued targets に置換する。
- [/] 5.9 旧 `EditorAccessibility` は KLE accessibility-tree responsibility を許すため違反する。Spark は KUC root-owned AccessKit snapshot/validation に置換し、KLE accessibility state/API を除去する。
- [/] 5.10 旧 egui MVP の部分対応を `EditorError::Unsupported` に退避させる方針は v0.1.0 full KatanA editor parity と矛盾するため完了扱いを取り消す。Spark は `crates/katana-language-editor-egui/Cargo.toml` の MVP description、`unsupported_controls.rs`、`host_controls.rs`、`search_control.rs`、`tools/kle-storybook/src/host_runtime_helpers.rs` の `Unsupported` success expectation を source-derived actual KUC/KLE/host path に置換する。KatanA source-derived feature または user-mandated replace/replace-all で `Unsupported` を返す release path、又はそれを成功とする test は AST/contract/release gate で fail する。Floem への将来移譲を根拠に v0.1.0 を部分実装にしてはならない。

---

## 6. 設定 IF を定義する

- [x] 6.1a 旧settings7型とroot/types exportを除去し、14独立compile-failとAST再導入拒否を検証した。既存apply_settings検査は未解決型に依存せず、mainの正例exit0/負例E0407で確認。最終静的検査/24 doctests成功、全体544成功/1失敗（検索Close）。hostのautosave/shortcutとKUC表示設定の動作証跡は未完了のまま。
- [/] 6.1 旧 `EditorSettings` は autosave/shortcut/word-wrap/tab/line/font semantics を KLE に集約するため current boundary に違反する。generic KUC input policy と KatanA host document policy を混在させず、KLE state/API から除去する。
- [/] 6.2 旧 `AutosavePolicy` は KatanA document/file lifecycle であり KLE v0.1.0 の API ではない。KLE に timer/dirty/write policy を残さない。
- [/] 6.3 旧 `ShortcutMap` は KatanA `shell_ui_shortcuts` の context/arbitration/modifier-specificity を KLE に複製させるため違反する。KatanA host router と KUC retained text-input reservation の source-derived proof へ置換する。
- [/] 6.4 旧 `EditorConfig::settings` 必須 API は 6.1 と同じ責務混在を固定するため、KUC/host の各 ownership へ解消するまで完了扱いにしない。
- [/] 6.5 旧 `apply_settings` runtime API は KLE generic/document state mutation を許すため違反する。KUC retained presentation policy 又は KatanA host state の各々の既存 route に置換し、KLE に update store を残さない。
- [x] 6.5 限定入口除去: `LanguageEditor::apply_settings`とFloem/interface診断hostの実装・呼出しを除去し、compile-fail doctestを通常unit-test/CIへ接続した。2026-09-05最終526 Rust tests、17 Python、doctest1、fmt/Clippy/AST、coverage77.84%成功。設定DTO/field残骸、KUC/host設定routeと全leaf証明は未完了で、6.5全体を完了にはしない。

---

## 7. ast-lint 拡張: 色リテラル禁止ルール

- [x] 7.1 `kle-linter` に `prohibited-color-literal` ルールを追加する
  - 検出対象（最低限）: `egui::Color32::*`、`#xxx` / `#xxxxxx` / `rgb(...)` / `rgba(...)` 文字列リテラル、`Color::rgb(...)` 系コンストラクタ、Floem 移行後の `floem::peniko::Color` リテラル
  - 例外: `kdv-presets` 配下、テストコード、明示的 `#[allow(kle_lint::prohibited_color_literal)]`
- [/] 7.2 `crates/katana-language-editor*` 配下に新ルールを適用し、違反ゼロにする。2026-08-14 の `just ast-lint` は KLE local `platform_text_surface.rs` を含む責務/色リテラル違反で失敗している。
- [ ] 7.3 `kle-linter` の CI が新ルール込みで通ることを確認する

---

## 8. Floem 移行準備（git dependency 固定）

- [x] 8.1 `crates/katana-language-editor-floem/` skeleton crate を新規追加し、workspace member に加える
- [x] 8.2 ルート `Cargo.toml` の `[workspace.dependencies]` に `floem = { git = "https://github.com/lapce/floem", rev = "<pinned-sha>" }` を追加し、**crates.io の floem は使用しない**ルールを docs / project.md に明文化する
- [x] 8.3 skeleton crate は neutral 契約がコンパイルできることだけを保証し、本実装は v0.2.x で行う旨を README に明記する
- [x] 8.4 `cargo tree -p katana-language-editor-floem | grep floem` が git source を指していることを確認する

---

## 9. KatanA editor 実装を移管する（KatanA full editor parity）

- [/] 9.1 KatanA `katana-ui` editor の UI-framework 非依存契約と egui adapter 範囲を `katana-language-editor` / `katana-language-editor-egui` へ移管する。KLE が KUC generic surface を実消費しない限り完了扱いにしない。固定 KatanA revision は requirement-source であり、KLE の公開前 runtime dependency にはしない。KLE release evidence は source-derived leaf ごとの KLE public input、KUC frame/AccessKit、KLE observable result/no-mutation で閉じる。KatanA の採用・依存更新・actual host E2E は KLE 公開後に [KatanA #336](https://github.com/HiroyukiFuruno/KatanA/issues/336) で扱う downstream task であり、KLE v0.1.0 の blocker にしない。KLE から KatanA を直接変更しない。
- [x] 9.2 `EditorConfig` 経由の設定注入を確立し、`katana-ui` のグローバル設定依存を排除する
- [/] 9.3 絵文字フォント管理（macOS / Windows プラットフォーム差異）は KUC の generic contract を source of truth とし、egui crate は adapter として利用する。KUC runtime は追加済みだが、KDV direct `cosmic-text` 残置、KLE same-surface input、KUC/KLE/KDV guard が未完了である。
- [/] 9.4 `MarkdownSyntaxHighlighter` / `EditorLayouter` / `EditorConfig::syntax_highlighter` の fixed KatanA revision における実 editor call path を、source-universe generated closure で先に解決する。2026-08-14 の static source search では `TextEditRenderer` が layouter を渡さず、全 production `katana-ui/src` で `EditorLayouter` / `MarkdownSyntaxHighlighter` の construction/call を発見せず、唯一の `syntax_highlighter` use は default empty spans を確認する `types.rs` test だった。よって generator が macro/trait/extern edge を解決するまでは `unreachable_helper` candidate とし、syntax-visible parity を推測してはならない。resolved edge が確認された場合だけその exact branch を KUC semantic-span binding へ接続する。到達不能が証明された定義は source hash/span/rationale 付き helper として記録し、KLE local highlighter を追加しない。
- [/] 9.5 egui crate 内に色リテラルが残らないことを `kle-linter` で検証する。KLE local `platform_text_surface.rs` が残る間は `prohibited-color-literal` / KUC ownership 違反が残るため、KUC binding migration と同時に違反ゼロへ戻す。
- [/] 9.6 KatanA editor toolbar / context menu / authoring command / image ingest / markdown format action の全 action contract を KLE 側で表現し、host adapter で実行できることを検証する。neutral contract と KLE/KUC の実入力は一部存在するが、KatanA 実ホストでの入力から authoring 14 操作・code kind 17 種・ingest/format/save の実副作用までの evidence は未完了である。直接 `UpdateBuffer` 注入又は action 等価比較だけを完了根拠にしてはならない。
- [/] 9.6a neutral `EditorActionRequest` と Storybook action callback coverage で save / format / image ingest / authoring command / diagnostic fix / view mode / split direction を検証する。旧 callback coverage は残るが、current KUC root/actual KatanA host effect を欠くため release evidence ではない。
- [/] 9.6b authoring transform 後の pending cursor restore を `EditorCursorRestoreControl` と Storybook contract で検証する。Storybook contract だけでは KatanA 実 cursor/undo effect を証明しない。
- [/] 9.6c KatanA authoring command inventory、code block kind inventory、toolbar/code-block menu state、suppression reason を neutral contract と Storybook contract で検証する。inventory は source-derived closure と exact KUC/KatanA proof へ置換が必要である。
- [ ] 9.6d clipboard file URL paste を payload-free correlated clipboard transaction と KLE public-input/KUC receipt/no-replay evidence で検証する。`IngestClipboardFileUrls { urls }`、画像 bytes/file-list/URL を KLE/KUC action/event/state に持つ既存 release path を削除する。OS clipboard の raw-image/file-list/file-URL priority と KatanA document effect は、公開済み KLE を採用する KatanA #336 の downstream host E2E で検証する。Storybook callback/downstream adapter probe は KLE release evidence ではない。
- [ ] 9.6e authoring toolbar の cursor anchor / viewport clamp placement を neutral contract、Storybook contract、downstream adapter probe で検証する。KLE local coordinate/placement contract は KUC ownership に反するため削除する。
- [/] 9.6f authoring toolbar / code-block menu の focus retention、editor focus close、outside click close lifecycle を neutral contract、Storybook contract、downstream adapter probe で検証する。旧 probe は KUC retained root と actual KatanA host effect を欠く。
- [/] 9.6g KatanA editor の save / format / image ingest / authoring / diagnostic / view-split typed action callback を downstream adapter probe で検証する。downstream adapter probe は actual KatanA host evidence ではない。
- [/] 9.7 KatanA editor の line number / diagnostic gutter / hover popup / fix review 起動 / problems panel 連携を KLE contract と egui adapter で検証する。KLE/KUC contract は部分実装だが、KatanA 実 adapter が不在のため source-derived runnable host-effect evidence は未完了である。
- [/] 9.7a diagnostic gutter line から popup item を生成し、fix / fix-all / docs action callback を Storybook contract で検証する。current Storybook gutter contract が失敗し、KatanA review/browser effect も未証明である。
- [/] 9.7b line number gutter state と gutter-line click action callback を Storybook contract で検証する。KUC opaque-root record と KatanA scroll effect が未接続である。
- [/] 9.7c problems panel の open/scope/status count と visible fix-all action callback を neutral contract、egui adapter、Storybook contract で検証する。actual KatanA diagnostics/review effect が未証明である。
- [/] 9.7d line number gutter の hovered line state を neutral contract、egui adapter、Storybook contract、downstream adapter probe で検証する。downstream probe は actual host evidence ではない。
- [/] 9.7e diagnostic gutter popup hover suppression / open-close lifecycle を neutral contract、egui adapter、Storybook contract、downstream adapter probe で検証する。current KUC root record と host action proof が未成立である。
- [/] 9.7f diagnostic gutter popup visual model と fix/fix-all/docs action labels を neutral contract、egui adapter、KUC DiagnosticsList Storybook contract、downstream adapter probe で検証する。generic KUC root and actual host review/browser proof が未成立である。
- [/] 9.8 KatanA editor の view mode / split direction / preview scroll sync / select-and-jump / doc search navigation を KLE contract と downstream KatanA integration で検証する。KLE contract/downstream probe は KLE-owned actual KatanA host-E2E の代替ではなく、source-derived runnable host-effect evidence は未完了である。
- [/] 9.8a neutral navigation contract で preview/editor scroll source、programmatic line scroll source suppression、select-and-jump PreviewOnly fallback、doc search navigation fallback を Storybook contract で検証する。current KUC record and actual KatanA coordinator proof が未成立である。
- [/] 9.8b KatanA 風 downstream adapter probe で navigation/view action roundtrip を `katana-language-editor-egui` 経由で検証する。downstream adapter probe は actual KatanA host evidence ではない。
- [/] 9.9 KatanA editor の dirty buffer / save / undo / external change / reference document read-only / multi-document scoped state を KLE contract と downstream KatanA integration で検証する。direct buffer mutation 又は simulated host-state reflection は KLE-owned actual KatanA host-E2E evidence を代替せず、実ホスト検証は未完了である。
- [/] 9.9a neutral document state contract で dirty state、save clean transition、reference read-only guard、external-change undo pending、workspace/document scoped identity を Storybook contract で検証する。actual KatanA document/file/undo effect が未証明である。
- [/] 9.9b KatanA 風 downstream adapter probe で dirty/save/external undo/reference read-only guard を `katana-language-editor-egui` 経由で検証する。downstream adapter probe は actual KatanA host evidence ではない。
- [ ] 9.9c `TabToolbar` の editor-frame sibling route を KUC opaque root に追加する。generic KUC `BreadcrumbNavigator` / `SourceAddressBar` は controlled opaque descriptor と one-time opaque record を持ち、KLE は candidate target を非観測で一度だけ転送する。submitted source text も non-Clone/non-Serialize one-shot transit として KLE が観測せず host へ渡し、host だけが `OpenUrl` を解決する。KLE は path split/tree lookup/URL validation/history/fetch/source payload/browser state を持たない。virtual/no-workspace/breadcrumb candidate、source input/button/Enter/blank/history の retained root/AccessKit leaf と、actual KatanA `SelectDocument` / `OpenUrl` document/browser/filesystem/network effect を source-derived host E2E で別々に検証する。
- [ ] 9.9d `docs/v0-1-0-neutral-host-request-design.md` に従い KLE host request surface を閉じる。`HostCommand { command }`、authoring の任意 command string、`EditorAuthoringCommand` / `EditorCodeBlockKind` / `EditorAuthoringMenu*`、diagnostic documentation の `url`、clipboard file URL payload を release path から除去する。authoring は host-projected opaque target/revision/correlation を KLE が一度だけ forwarding し、source closure が KatanA `MarkdownAuthoringOp` / `CodeBlockKind` と actual host effect を結ぶ。KLE に closed Markdown enum 又は code-kind catalogue を導入しない。payload-bearing `EditorEvent` / `SearchQuery`、scroll/jump/gutter line/anchor models を KLE persistent API から除去し、document edit、document search、viewport/gutter/select-and-jump、source-address、generic tab-group rename を `EditorAction` 外の non-Clone/non-Serialize 一回消費 transport として KUC event から exact KatanA host bridge へ直送する。Replace / Replace All は固定 KatanA source に一般 route がないため user-mandated extension blocker として別扱いにし、KLE state/manifest/log/Storybook に raw input・query・content・origin・coordinate を保存しない。KUC retained UI は action にしない。AST、generated source-target closure、stale/duplicate/source-specific no-mutation、raw value absence を strict gate にする。
- [/] 9.9e document TabStrip の full host request family を実装する。select/previous/next、close/other/all/left/right、pin/restore、reorder/group drag、create/add/remove/rename/recolor/ungroup/close/collapse group を host-projected opaque tab/group/placement/swatch target と revision/correlation だけで KatanA action に total map する。名称だけは non-Clone/non-Serialize one-shot group-name request、色は host-issued swatch ID とし、KLE は `PathBuf`、tab index、group membership、group name/color、drag geometry、popup/input/palette、persistence を持たない。dirty confirmation-enabled close は current KatanA pending-state/no-document-mutation branch を証明し、renderer のない `ForceCloseDocument` control を KLE/KUC に追加しない。KatanA tab overflow は host request ではなく KUC retained horizontal scroll/active reveal として別 leaf にする。KUC retained menu/inline-edit/palette leaves と every host effect/no-op/stale branchを独立 strict leaf として検証する。
  - 2026-08-26: KUC-private retained overlay が tab の secondary-click menu、submenu tree、separator、current-frame AccessKit、artifact front layer、group popup の host-projected swatch を実装した。rename は独自文字列処理を持たず、既存 KUC `TextSurface` / platform-raster adapter の IME preedit/commit、グラフェム選択、caret、focus、AccessKit TextInput を使用する。RawInput の二次クリック、メニュー項目 primary press/release、スウォッチ primary press/release、rename 入力 focus、IME preedit/commit の `日本語 + ⭐️` artifact、未変更 Enter の no-op、変更 Enter による one-shot proposal、同一 frame の rename/recolor で recolor だけを forward する優先規則、AccessKit の close request/recolor proposalを adapter 実根で検証した。overlay 背景は入力を占有せず、KUC-private press capture が同一 control の primary press/release だけを一回 proposal に変換する。adapter package は192 unit tests、contract suites、strict clippy、diff checkを通過した。KLE transit、KatanA host effect、pointer palette/rename全分岐、drag、dirty close、stale/rejection、3 OS emoji、full Storybook は未達であり、本項は未完了である。
- [ ] 9.9f preview side rail を KLE の部品として実装しない。KUC `PreviewSideRail` の opaque item/panel/source-target/revision/correlation event を一度だけ host bridge に forward し、KLE は TOC/export/story/tools 名、hover timer、panel/overlay geometry、export format、document path、TOC line/scroll state、KatanA `AppState` mutation を持たない。rail/panel mechanics は KUC retained effect、refresh/search/export/story/tools/meta/TOC host outcomes は source-derived unchanged KatanA route として分離し、actual RawInput/AccessKit/opaque frame/declared-effect proof で固定する。

---

## 10. KDV 型 Storybook live harness を実装する

- [x] 10.1 `tools/kle-storybook/` を workspace member として追加する
- [x] 10.2 `just storybook` を interactive window 起動コマンドとして追加し、smoke test alias にしない
- [x] 10.3 `just storybook-window-smoke` / `just storybook-interaction-check` / `just storybook-emoji-check` / `just storybook-contract-check` を追加する
- [/] 10.4 Storybook は `EguiLanguageEditor` 実体と document state を host し、`--interactive` 起動時に実際の `eframe` + `show(ui)` パスを開く。`--smoke`/`--motion-artifact` は同一 public `show` call の KUC TextSurface/CommandChrome artifact と actual root bounds を KUC public compositor へ渡す。`minifb`/fallback renderer/shape count/fixture glyph は release evidence から完全に除去する。KUC task 3.1g-12 と KLE 11.0f2/f3a が未完了のため現状は未完了である。
- [/] 10.5 props / state / event / action / callback の roundtrip を Storybook crate tests で検証する。2026-08-14 の full `kle-storybook` 再実行は 22 件中 12 件成功・10 件失敗で、diagnostic gutter が actual `show` frame を生成しないため roundtrip と artifact が失敗した。全 stage が current KUC record/AccessKit/host effect を通過するまで完了扱いにしない。
- [ ] 10.5a current Storybook gutter failure を KLE local fixture で修繕しない。`StorybookGutterContract` は `set_content` / `set_cursor` / diagnostics / hovered-row 更新後に `latest_kuc_frame` を読むため stale frame を検査している。Spark は KUC opaque-root RawInput scenario に置換し、same-frame root record/AccessKit の gutter target と actual event/effect を検証する。local post-mutation `show`、assertion 緩和、callback counter、fixture artifact はすべて不合格とする。
- [/] 10.6 manual hit-test reconstruction、文字列 parse による action synthesis、KLE/KUC contract bypass を `kle-linter` で禁止する。2026-08-14 の静的確認で fallback renderer、pixel-based jitter fallback、`egui` shape count を motion/smoke acceptance が参照している。compiled acceptance path と AST/lint の両方から除去されるまで完了扱いにしない。
- [ ] 10.7 release readiness 用に Storybook full-spec live acceptance artifact を生成する。toolbar の 5 groups / 12 trigger（inline 4、heading 3、list-quote 3、code dropdown 1、image 1）、context-menu の save/format/horizontal-rule/link/table/clipboard-image と 14 authoring trigger、17 code kinds、file/clipboard-image/file-URL の3 image ingest route、document search/open/query/next/prev/close、user-mandated replace blocker 表示、line gutter、document tab、breadcrumb virtual/no-workspace/menu/candidate、source address input/button/Enter/blank/history、selection/read-only、Japanese/`⭐️` VS16、KUC records/AccessKit と class-appropriate host callback evidence を一つの actual RawInput script/manifest に出力する。source address submit は actual KatanA `OpenUrl` route、breadcrumb selection は actual `SelectDocument` route を同じ execution record に持つ。KUC task 3.1g-13 の actual ContextMenu adapter 前に KLE local menu を追加しない。既存 fallback renderer の artifact は release evidence ではない。2026-08-23: 実装前設計としてKUC `FullTextCommandSurfaceScenario`がraw fixture/layout/RawInput/artifactを所有し、KLE Storybookはscenario ID/opaque lease/receiptだけを中継する境界を固定した。現行KLE `host_types_presentation.rs`の短文・selection byte range・viewport・常時floating組立はこの設計に違反する残骸であり、KUC scenario実装後に削除する。
  - 2026-09-04: published `katana-ui-core@0.3.5` の public replay は固定8 `FullTextCommandSurfaceScenarioId` に閉じており、source-derived full-editor leaf を KUC-owned stage として要求できない。KLE local RawInput/renderer/fixture reuse は不許可であるため、generic consumer-defined artifact plan を KUC #40 に起票した。公開 registry capability と三OS proof が得られるまで、本項は fail-closed のままとする。
- [x] 10.7d full-spec root の実装順とownershipを `docs/v0-1-0-kuc-full-editor-root-integration-design.md` に固定した。KUCがfull-root projection/event/layout/retained state/AccessKitを所有し、KLEはopaque lease/transitだけを持つ。既存KUC TabStrip、TextViewport、Breadcrumb、Status、Diagnostics、Previewの再利用可否と不足adapterを監査し、KUC root contract -> TabStrip -> Breadcrumb/SourceAddress -> Status/Diagnostics -> Viewport -> scenario gate -> KLE transitの順で実装する。これは設計完了であり、10.7のfull-spec実装・fixed-host evidenceは未達である。
- [ ] 10.8 Storybook 用の deterministic motion artifact（numbered PNG + GIF + manifest + MP4 review video）を `--motion-artifact` で生成し、interactive window と同じ editor surface/render path から full editor input / line number / markdown controls / document search / replace blocker state / document tab / breadcrumb / source address / interaction callback をフレーム列で自動検査する。MP4 は decoded frame hash が compositor PNG sequence と一致することを検証する。別 fallback renderer の pixels、fixture-only canvas、egui shape count は acceptance evidence にしない。
- [ ] 10.9 KLE-Storybook は KUC platform text-raster runtime を通る same-surface artifact で日本語/`⭐️`/variation selector/color glyph を検証する。current source audit で `setup_fonts` path は存在しないため、KLE-specific font setup/OS font fallback を新設せず、KUC adapter migration の acceptance 根拠にも使わない。
- [ ] 10.9a KUC `PlatformTextRasterizer` の platform font discovery を generic process-shared catalog へ移す。現行 TextSurface/CommandChrome/ContextMenu の各 constructor ごとの `FontSystem::new` / `load_system_fonts` 再走査を排除し、per-surface text/glyph/texture/selection cache は独立のままにする。KUC の test-only instrumentation で同一 catalog を使う独立 root/concurrent construction の system-font discovery 回数を一回と検証し、Japanese IME、exact color `⭐️` VS16、ZWJ、pixel determinism、cache isolation/root disposal を全て維持する。`⭐️` は単独 authoritative grapheme crop の chromatic pixel/hash と same-config `☆` control との差分で検証し、`🔥⭐️` のような混在 fixture 全体の色ピクセル数を受理しない。時間閾値、KLE cache、coverage/lint の緩和は代替にしない。
- [ ] 10.9b `docs/v0-1-0-kuc-font-catalog-design.md` に従い、KUC `PlatformFontCatalog` が `FontSystem`、candidate font load、locale/emoji-family fact だけを catalog key ごとに共有し、`SwashCache`、raster/texture cache、text/selection/IME/focus/scroll/AccessKit state を surface ごとに隔離する。TextSurface/CommandChrome/ContextMenu の同一 retained root と独立 root/concurrent construction が一つの catalog を注入・再利用すること、mismatch が typed error になること、catalog 以外の direct `FontSystem::new` を AST で拒否することを strict test で確認する。
- [ ] 10.9c KUC `PlatformFontCatalog` は macOS/Windows/Linux の各 release profile で actual color-emoji face を resolver/provisioning により fail-closed に確定する。macOS `Apple Color Emoji`、Windows `Segoe UI Emoji`、Linux verified provisioned color face の family/face identity/file SHA-256 を root record に残し、各 profile で exact `⭐️` (`U+2B50 U+FE0F`) の isolated chromatic crop と same-config `☆` control の hash/pixel delta を strict test、KUC root、Storybook artifact、KLE/KatanA join により個別検証する。現行 KUC CI で Windows を除外している `storybook-check`/AST lint は profile runtime proof の免除根拠にせず、Windows KUC-root artifact を必須化する。`SansSerif`、missing face、profile skip、KLE/KDV fallback、aggregate emoji fixture、code-point-only assertion は fail とする。
- 2026-08-21: macOS 下層 strict test `platform_emoji_uses_color_pixels_when_apple_color_emoji_is_available` は Apple Color Emoji 必須かつ `⭐️`/`☆` 同一config差分で pass。これは KUC raster の単一OS証拠であり、KUC root artifact、KLE/KatanA join、KDV、Windows、Linux が未実施のため task は未完了。
  - 2026-08-26: KUC retained root のunicode evidence は exact `⭐️`/`☆` crop、font face SHA-256、root RGBA/record、AccessKit snapshot hash に加え、actual `MultilineTextInput` のvalue/scalar sequence/role/boundsを同一root artifactへ結合してmacOSでpassした。KUC CIはmacOS/Windows/Linuxの各profileでmissing face/captureをskipせずfailにするstrict release testとLinux color-emoji provisioningを持つ。Windows/Linuxのremote native artifact、KLE/KatanA execution join、および公開済みKUC revisionに対する再現実行は未実施であるため、taskは未完了とする。
- [ ] 10.7a `FullEditorScenarioManifest` を KLE Storybook の唯一の stage source として実装する。各 `step_id` は source-derived parity leaf（user-mandated replace-current/replace-all を含む）一つ、KUC current-frame record/hit/action、AccessKit node、KUC-composited visible assertion、class-correct input-origin、KLE public opaque transit の receipt 又は no-mutation outcome を持つ。`editor_direct_ui`/host-continuation/user-extension は public `RawInput -> EguiLanguageEditor::show` を必須とする。KatanA-owned shortcut/document/file effect は KLE 公開後の KatanA #336 で physical host trace として検証し、KLE Storybook に host simulator を置かない。aggregate selector、source marker、fixture-only state、Storybook-only actionは一つでも代替にしない。
  - 2026-08-22: host-only manifest の基盤と document-find 全leaf、Replace / Replace All の未達 blocker、workspace modal tab（pointer / keyboard / AccessKit）、File Name filter family（query / include / exclude / match-case / match-word / valid regex / empty / invalid regex / no-workspace）、Markdown Content family（query / Enter / lost-focus refresh / history select-remove-clear-empty / no-workspace / no-result）、File Name / Markdown Content の valid・missing result select/jump、基本 authoring toolbar 14 trigger を登録した。各 leaf は KUC root / AccessKit / KLE transit / class-appropriate effect の4層を必須化している。ただし host-only manifest は実行証跡ではない。selection/read-only/IME/undo を含む authoring lifecycle、code 17 kinds、image、diagnostic、navigation、実KatanA host effect は未登録又は未実行のため、本項は未完了である。
- 2026-08-22: KUC public root の search event transport は opaque target/correlation を actual host effect へ運べないため、token 内に process-local target を保持する暫定 bridge は撤去した。generic opaque host transport の設計・physical RawInput/AccessKit・stale/disabled/no-leak・actual host effect の同一record検証が先行条件であり、SearchStrip 表示又は callback を本項の証拠にしてはならない。
  - 2026-08-22: source audit で `SanitizedDocumentRootFactory` / `RootEventForwarderBridge` が Search だけでなく Command / ContextMenu の sanitized capability transport を outer forwarder 前に直接 `invoke_once` していることを確認した。後段 token は actual host dispatch port を持たず、KLE opaque relay 境界に反する。KUC `KucOpaqueHostEffectBatch` の core は追加済みだが、sanitized bridge 全 class を batch attach + actual-host dispatch へ移行し、RawInput/AccessKit と forwarder-error/no-mutation を確認するまで、本項・SearchStrip・Storybook evidence は未完了である。
  - 2026-08-22: KUC adapter は Command / ContextMenu / Search の direct invoke を `KucOpaqueHostEffectBatch` に移し、actual host dispatch port と 130 adapter tests を通過した。ただし KLE public root は wire-only `EguiTextCommandSurfacePresentationToken` しか受けず、opaque callback registry を渡す lease が存在しない。wire token と non-wire host projection lease の分離、KLE AST prohibition、public KLE -> actual host E2E が次の先行条件であり、KUC adapter 成功だけを KLE/Storybook の実効果証拠にはしない。
  - 2026-08-22: KUC に non-wire `EguiTextCommandSurfaceHostProjectionLease` と generic `KucRootEffectRouter` を追加した。router context は class count だけではなく Text / Toolbar / Floating / Search / ContextMenu の KUC generic event payload snapshot を持ち、query / replacement / option value を actual host router が選別できる。current KUC adapter library は 132 pass、clippy `-D warnings`、format check を通過した。KLE provider/binding はまだ wire token のままで、lease root の actual `show -> router -> outer forward -> actual dispatch` 統合証拠、KLE AST guard、KatanA host E2E は未実施である。
- [ ] 10.7b KDV の interactive/window-stage/headless-live acceptance separation を editor surface に適用する。interactive、headless acceptance、motion runner は同じ retained public `show` / KUC component-tree contract version を宣言して照合する。headless runner は KUC actual RawInput/AccessKit/artifact records を oracle とし、KLE は current opaque descriptor/event の one-time transit だけを行う。KLE local state/callback、coordinate/glyph/line/hit-test/action synthesis、egui shape count、minifb/fallback/fixture renderer を compile/AST/runtime gate で拒否する。
- [ ] 10.7c Storybook/host-E2E は workspace search を document search の aggregate で代替しない。File Name / Markdown Content の tab switch、filename query と include/exclude/match option/invalid regex/no-workspace/no-result、Markdown query/Enter/history select/remove/clear/no-workspace/no-result、filename result の actual `SelectDocument`、Markdown result の actual `SelectDocumentAndJump` を個別 leaf として実入力・KUC root/AccessKit・class-appropriate KatanA effect に結合する。query、regex、history、path、line、range、result list は KLE persistent state/record/manifest に保存せず、KLE local filesystem/search/parser/result-jump 実装を禁止する。
- [ ] 10.8a KDV の artifact/checksum/freshness discipline を full editor stage に実装する。stage ごとの numbered KUC-composited PNG、contact sheet、manifest、SHA-256、GIF、MP4 を生成し、MP4 decode frame hash と PNG sequence の一致、PNG header/dimension/non-empty/duplicate/staleness、KUC compositor provenance を自動検証する。静止画・動画は review artifact であり、同一 `step_id` の KUC record/AccessKit/actual KatanA host effect がなければ release evidence にしない。
  - 2026-08-22: SearchStrip の public KLE root physical input (`1/2 -> 2/2 -> 1/2`) と
    KUC-composited GIF89a 3 frame artifact は追加済み。ただしこれは Storybook host
    effect の evidence であり fixed KatanA host E2E ではない。toolbar/code dropdown/
    context-menu の physical scenario は KLE fixed-coordinate prohibition を満たす KUC
    generic interaction locator が未実装のため未着手であり、表示 artifact を completion
    に数えない。
  - 2026-08-22: KUC worktree に未追跡 `interaction_locator.rs` の未接続骨格があるが、
    root module/public frame/actual RawInput bridge に未接続であり adapter の 134 tests、
    strict clippy、format が green でも検証対象外である。opaque one-shot RawInput apply、
    current-frame actual response/AccessKit construction、root/revision/correlation/double-use
    rejection、toolbar/floating/dropdown 17/search/context の strict physical test が揃うまで
    本 task の implementation/evidence として採用しない。
  - 2026-08-22: 後続差分は locator を root module と facade へ接続し 137 tests を通したが、
    `clippy -D warnings` が unused `mut` で失敗した。さらに current-frame component record
    の利用だけで AccessKit target との同一frame照合と actual-root physical scenarios が不足する。
    strict clippy、actual root RawInput、AccessKit、opaque effect を同一 correlation で結合する
    17 code leaf を含む test が揃うまで、この差分も採用・evidence昇格しない。
  - 2026-08-22 fixed KatanA source audit: Storybook toolbar は 12 action と `text, markdown,
    bash, zsh, mermaid, drawio, plantuml, json, yaml, toml, rust, typescript, javascript, python,
    html, css, sql` の 17 code-kind 定義を持つ。しかし physical RawInput/leaf AccessKit/
    opaque effect/fixed-KatanA-host-E2E はいずれも未証明である。context Save、conditional
    Format、14 Markdown operation、Ingest の Storybook context-menu projection は未実装である。
    locator 完成後も、表示定義を physical scenario や host E2E の代替にせず、toolbar/floating/
    dropdown/context の generic KUC capability -> KLE one-shot relay -> actual host effect を各 leaf
    で接続する。
  - 2026-08-22 host-E2E bridge audit: KUC locator は KUC current-frame target から KUC root
    用 `RawInput` を構成するが、fixed KatanA の toolbar は fixed app の egui click から直接
    `AppAction` を生成する。両者を identity-preserving に接続する adapter は存在しない。
    current `katana-host-e2e` の physical AX/AccessKit proof は Open Workspace bootstrap までで、
    KUC locator -> fixed KatanA toolbar effect の証拠ではない。`trigger_action`、`pending_action`、
    direct `AppAction`、fixture state mutation、fixed coordinate、Storybook hash/counter、
    `request_action` contract test をこの gap の代替に禁止する。次 batch は KLE opaque one-shot
    relay と fixed host の natural UI route を action identity を復号せず結合し、14 authoring、
    17 code-kind、context leafごとに buffer/selection/dirty/preview/AccessKit/no-mutation を
    actual process で検証するまで未完了とする。
  - 2026-08-22 locator collision design correction: same current frame の enabled target が
    identical pointer-interactable bounds を持つ場合、pointer `RawInput` は selector を識別不能で
    ある。KUC は AccessKit action injection で通過させず `Ambiguous` / no-mutation として
    fail-closed にする。actual root overlay fixture はこの invariant の physical
    RawInput/AccessKit/event/effect no-mutation を検証し、production layout overlap の回帰を
    検出する。二 target が各自の opaque pointer event を発行できるという旧 test 前提は誤りで
    あり、採用しない。

---

## 11. KUC 汎用 UI contract への寄せ込みを評価・実装する

- [/] 11.0 KLE migration readiness gate。KUC `TextSurface` の `surface_bounds` / `viewport_bounds` / actual scroll paint-hit-IME-AccessKit、controlled presentation synchronization、automatic numbered gutter、caller-defined sparse visual role、`FloatingCommandToolbar` の self-measured actual floating/dropdown/tooltip contract、controlled `CommandChromeSearchStrip`、consumer-safe retained root surface、visible dropdown item precedence は KUC real-egui test と guard まで確認済みである。ContextMenu の nested/overflow は、submenu 遷移後に stale `smooth_scroll_delta` を再適用して hit target がずれる KUC 汎用不具合を current-frame `RawInput::events` の wheel だけを適用する形で修正した。KUC actual RawInput の三経路は component evidence に限られ、Save / conditional Format / Edit / Ingest、nested code kinds を含む KLE-owned actual `KatanaApp` host E2E の全 leaf 通過は未証明である。KLE UI に座標補正又は擬似 action を追加してはならない。KUC thin binding、Storybook、KLE host E2E、AST gate が未完のため、本項全体は未完とする。
- [ ] 11.0a `docs/v0-1-0-neutral-host-request-design.md` が本節の旧 neutral API より優先する。KLE text binding は opaque KUC root descriptor と single-consumption transport の機械的 forwarding だけを持ち、char/byte/grapheme/range、controlled value、selection/clipboard/history/content/origin、pixel/line/scroll/IME rectangle を計算又は保持しない。AST guard で全てを拒否する。
- [ ] 11.0b KLE syntax/search/diagnostic binding は host-projected opaque semantic span/annotation/target/revision を KUC root へ forwarding するだけとし、injected highlighter、search engine/query/match、gutter line/range、generic color/row/texture/geometry renderer を持たないことを compile/AST/real-egui test で検証する。
- [ ] 11.0c KLE authoring/search binding は KUC root-owned `FloatingCommandToolbar` / `SearchStrip` / `EguiCommandChromeAdapter` に host-projected presentation を一度 forwarding し、KUC event を closed request 又は single-consumption transport として host bridge へ直送するだけとする。KLE persistent `KucCommandChromeBinding`、neutral action/search state、`egui::Area`、button inventory、local form state、popup coordinate、icon fallback を削除した同一 migration diff を実施する。
- [/] 11.0d KUC `TextSurface` の controlled presentation synchronization を完了する。KUC source value、UTF-8 byte selection/caret、read-only/input policy、span/annotation、accessibility DTO と state identity synchronization は実装済みで、focus/drag/scroll/IME/raster state を再生成しない KUC actual-egui/AccessKit test が通る。KLE widget がこの component を実消費し、Japanese/`⭐️`/ZWJ IME preservation を同じ実 surface で検証するまで完了扱いにしない。
- [ ] 11.0d1 KUC `TextSurfaceFocusRequest` の opaque token/requested focus state を KLE `EditorViewControl::focus` / `blur` が転送する。KLE は egui focus memory 又は KUC core action を直接操作せず、KUC output の actual focus fact だけで `focused` を更新する。same token の user-origin focus 保持、new token の request/surrender、Japanese/`⭐️`/IME/preedit/selection/scroll/raster state preservation を KUC actual RawInput と KLE integration で検証する。
- [ ] 11.0e KUC automatic numbered gutter は host-projected opaque marker/target/revisionのみを受ける。KLE は `TextRange`/byte range/start-anchor/line/priority を変換・計算せず forwarding だけを行う。KUC が line number、logical row、row bounds、diagnostic marker SVG raster/hit target、AccessKit、scroll alignment と stable priority resolution を所有する。KLE `LineGutterModel` を削除する同一 migration batch と actual KatanA gutter effect evidence まで完了扱いにしない。
- [ ] 11.0e1 KUC `TextSurface` は opaque host-projected scroll/source target と request token の controlled acknowledgement を持つ。KUC が current layout の row/range-to-rect、scroll mutation/clamp、user wheel 復帰、same-token no-op、新 token の置換、acknowledgement/rejection event を所有し、KatanA host が logical anchor/source mapping を所有する。KLE は char/byte/range/relative pixel/alignment を変換せず opaque event を一回 forwarding する。KLE `scroll_by_pixels`、local line-to-pixel calculation、egui `ScrollArea`、local pixel accumulator は release blocker とする。
- [/] 11.0e1a KUC `RelativePixels` の f32 transport、KUC-owned scale/rounding/clamp、non-finite typed rejection、frame-derived `visible_logical_rows` は KUC core/actual RawInput/AccessKit/artifact/AST gate まで完了した。KLE `scroll_by_pixels` の opaque-token forwarding と `visible_range` の KUC frame record consumption は未実装である。KLE は i32 conversion/residual、`lines()`/newline enumeration/row visibility calculation を持たない。
- [ ] 11.0e2 KLE TextSurface binding は KUC `TextSurfaceEvent` を one-time content/clipboard/history/context/gutter/viewport transport 又は opaque acknowledgement として host bridge へ転送する。KLE neutral host control、content/selection/cursor/gutter row/scroll state を保持せず、KUC post-frame fact を再導出しない。real `RawInput` と actual KatanA host integration test で検証する。
- [ ] 11.0e3 KUC automatic gutter active/hover contract を KLE thin binding に接続する。KLE は KUC frame 前に line count/source/cursor/hover set を使わず、host-projected opaque marker presentation と current-frame opaque target だけを forwarding する。`gutter_lines()`、`request_gutter_line()`、logical-row scroll、neutral DTO copy を KLE public/core から除去する。`LineGutterModel`、line enumeration、row geometry、local active/hover/diagnostic calculationを同じ deletion batchで除去し、Japanese/`⭐️` VS16/caret/hover/marker/scroll/source-update の public RawInput/AccessKit/artifact/host-E2E test と AST rule で固定する。
- [ ] 11.0g `EguiLanguageEditor::show` を retained KUC opaque-root の direct path へ置換し、`EditorResult<()>` を返す。KLE は host-projected root envelope と one-shot input transit を非観測で渡し、KUC current-frame record/AccessKit/error outcomeをそのまま返送する。document identity、content、selection、cursor、focus、event semantic mapping、KUC error reinterpretation を KLE に実装せず、old renderer を呼ばない。
- [ ] 11.0g1 11.0g の cutover interface を atomic migration batch として実装する。`EguiLanguageEditor` は KUC `SanitizedDocumentRoot` 一個だけを retained runtime object とし、current-frame `SanitizedDocumentRootInput` を consuming `synchronize` / one `show` / one `forward_events_once` に渡す。KLE field/getter/queue/manifest/debug に snapshot、selection、cursor、query/match、gutter、scroll/focus request、authoring/menu、KUC child output、artifact aggregate、egui response を残さない。KUC rejection は一回の `EditorError::Internal` 変換だけを行い、legacy renderer/blank/synthetic event/fallback を使わない。input/closed frame を clone/serialize/replay する経路と、KUC root以外の retained generic component を AST/compile test で拒否する。
- [ ] 11.0g2 11.0g1 と同じ migration batch で legacy `LanguageEditor` control surface を opaque host-frame injection / one-shot host bridge に置換する。`EditorWriteAccess`、`EditorSelectionControl`、`EditorSearchControl`、`EditorScrollControl`、`EditorGutterControl`、`EditorAuthoringControl`、local undo/event queue はKLE runtimeに残さず、各 host semantic operation は current projection の opaque target/correlation又は dedicated nonpersistent input transport だけで到達する。KatanA はread-onlyなので、source-compatible KLE-only callerは移行完了/host parityの根拠にしない。
- [ ] 11.0h legacy `EditorViewControl` / `EditorScrollControl` の KLE-owned focus/scroll API を opaque KUC root event/record と host-projected request に置換する。KLE は local focus mutation、logical row/UTF-8/range/pixel/visible-range calculation、`Unsupported` を除去する。
- [ ] 11.0i KLE public widget の real `RawInput` integration test を追加する。Japanese/`⭐️` VS16/IME/identity/selection/cursor/focus/scroll/visible rows/gutter/AccessKit/artifact/KUC adapter failure を検証し、fallback surface が描画されないことを AST と runtime evidence で固定する。
- [ ] 11.0j direct KUC path が検証された後、legacy TextSurface/gutter source は削除 checkpoint まで保持したまま compiled KLE module graph から切り離す。KLE runtime は local renderer field/call を持たず、strict Clippy の dead-code suppression を追加しない。source-level AST blocker は deletion checkpoint まで残る。
- [ ] 11.0k KUC `EguiTextCommandSurface` adapter が TextSurface / toolbar / search / floating / ContextMenu の retained root artifact を generic single opaque output として所有する。KUC public contract は root identity/bounds、KUC 内部で検証済みの layer provenance、final `ArtifactCompositeFrame` RGBA/dimension/hash、current text-command record と AccessKit evidence を同一 frame で返す。child order と child paint plan は KUC 内部 detail であり、consumer に公開しない。missing/duplicate/out-of-order child、record/pixel mismatch は KUC typed error として publish 前に reject する。`docs/v0-1-0-kuc-text-command-root-design.md` の output shape、private child-plan boundary、same-frame composition、typed error、KLE/Storybook prohibition に従う。KLE `EguiKucArtifactAggregate` の root-order reconstruction と Storybook `rgba_to_window_pixels` を KUC output の opaque consumer に置換する。KLE は host-projected opaque envelope と one-shot transit以外の presentation mapping、paint order、RGBA/window pixel conversion、artifact child synthesis を持たない。
- [ ] 11.0k1 11.0k の KUC opaque full-editor root は generic retained `TabStrip` を含む。tab descriptor、active/dirty/pinned/close capability、tab/menu/drag/AccessKit event、focus/order/group presentation は KUC が所有し、KLE は host-projected opaque envelope と one-shot record を非観測で転送するだけとする。KLE の local tab rendering、KatanA document descriptor/action mapping、tab hit-test、tab order/drag geometry、tab menu は AST と public RawInput/AccessKit/root-frame test で拒否する。KatanA document/group/persistence/filesystem effect は actual host E2E でのみ完了とする。
- [ ] 11.0k1a current KUC `WorkspaceTabBarOptions` の flat group / single `group_id` model と sanitized tab projection の nested group contract を整合させる。adapter が group hierarchy を flatten/drop する、KLE が hierarchy/active/order/drag/palette state を補完する、egui response/opaque target を tuple/public API に漏らす方法は禁止する。KUC core は nested generic retained TabStrip model、actual RawInput/keyboard/AccessKit rendering、overflow/menu/drag/rename/recolor/close/collapse family、closed root record/event transport を所有し、sanitized root は同一 frame に実描画して only closed output を返す。pointer select だけの adapter test、core action 直接呼出し、未使用項目の lint suppression は完了根拠にしない。
- [ ] 11.0k1b `SanitizedTabGroup` を label/order/icon-only descriptor のまま採用しない。opaque group target と generic capability を public input に追加し、KUC だけが group select/collapse/menu/rename/recolor/close/ungroup/drag event を current revision/correlation とともに closed transport へ出す。KLE は target の意味、group id、label text、palette、rename text、order、nested membership を read/retain/serialize/replay してはならない。group action target を tab target、label、order、tree traversal から導出する fallback は AST/runtime test で拒否する。
- [ ] 11.0k1c sanitized TabStrip adapter の structural render id は KUC-private widget identity に限定する。structural id を host target 又は forwarded event として出す、opaque tab/group target を label/order/path/tree traversalから再導出する、core `WorkspaceTabBarEvent` を KLE transport へそのまま流す、tab/group event を root output/one-time transport から落とすことは禁止する。KUC は render id -> original opaque target の private mapping を retained root 内だけに持ち、physical pointer/keyboard/AccessKit input から closed target event を一回だけ root transportへ結合する。nested parent relationshipは current KUC core model に渡し、flat renderer が child hierarchyを並列 root group として描画することを RawInput/AccessKit testで拒否する。
- [ ] 11.0k2 11.0k の KUC opaque full-editor root は generic retained `StatusStrip` と `DiagnosticsPanel` を含む。status/activity/dirty/diagnostic count、panel visibility、stable scope key、file/diagnostic disclosure、fix preview/capability と pointer/keyboard/AccessKit event は KUC が所有し、KLE は host-projected opaque envelope と one-shot record を非観測で転送するだけとする。KLE/Storybook の `egui::Panel`、status/list/scope widget、KatanA diagnostic/status DTO/action mapping、label-to-scope conversion、linter filter/batch、preview line calculation、browser URL/path/fix payload保持は AST と public RawInput/AccessKit/root-frame test で拒否する。KatanA linter/document/review/browser state/effect は actual host E2E でのみ完了とする。
  - [/] 2026-08-26: KUC current-source integration sequenceを設計固定した。既存core `StatusBar` / `DiagnosticsList` を再利用し、KUC retained adapter -> optional root slot -> closed artifact/AccessKit -> generic one-shot transport -> RawInput scenario の順に実装する。KLE は model/action/key/bounds/paint plan をimport・decode・保持しない。これは設計完了であり、adapter/root実装、host effect、3 OS emojiは未達である。
- [ ] 11.0k3 11.0k の KUC opaque full-editor root は generic retained `WorkspaceViewport` / `SplitViewport` / `PreviewViewport` を含む。CodeOnly/PreviewOnly/Split、axis/order、resize/ratchet、scroll/hover/anchor、overlay/focus、document-frame/browser/failure input、one-time opaque preview record と AccessKit/artifact は KUC が所有し、KLE は host-projected opaque envelope を非観測で転送するだけとする。KUC は KDV に依存せず、KLE/Storybook は KatanA descriptor/KDV DTO mapping、Markdown parser、KatanA egui preview copy、KDV painter/browser raster、geometry/anchor/scroll cache、fixture bitmap を持たない。Markdown text は KDV `PreviewSurfaceImage`/`ViewerNode.rect` でなく semantic node/span/source target を KUC が PlatformFontCatalog で layout/raster/hit-test する。opaque raster は external document/PDF/media/diagram/browser に限定する。`docs/v0-1-0-kuc-preview-viewport-design.md` の stale/mixed/capability error、same-root RawInput/AccessKit/frame contract を満たすまで完了扱いにしない。
- [ ] 11.0k4 11.0k の KUC opaque full-editor root は generic retained `BreadcrumbNavigator` と `SourceAddressBar` を含む。KUC input は revisioned opaque segment/candidate/history/status descriptor のみであり、path/URL/filesystem/network/response payload/KatanA/KDV/KRR/browser type を受け取らない。KUC は breadcrumb/menu/address text/preedit/history disclosure/overflow/focus/AccessKit/artifact を保持し、KLE は selected opaque target を非観測で一度だけ転送する。submitted text だけは `EditorAction` 外の non-Clone/non-Serialize one-shot valueとして KLE が観測せず hostへ渡し、host だけが `OpenUrl` を解決する。KLE state/manifest/log/Storybook に保存しない。URL validation、history dedup、file access、HTTP、source document、browser session/error/status は actual KatanA host effect でのみ完了とする。KLE/KUC `UrlTabState` setup、URL parser、HTTP stub/mock、source payload、tree lookup/path split、coordinate/geometry は AST と public RawInput/AccessKit/root-frame/host-E2E test で拒否する。
- [/] 11.0k4a KUC `SourceAddressStrip` は presentation-only input/history/candidate retained state、focus/open/disabled transition、targetless generic event と non-Clone/non-Debug/non-Serialize の consume-only `SourceAddressSubmission` を実装した。2026-08-23 に target/display/draft の `Debug` 漏出を拒否し、entry の Serialize を持たせない strict core test 4件、KUC core 全230 unit testsを通過した。native egui adapterは撤去し、shared `PlatformFontCatalog`/metrics/text-raster と同一 live/artifact paint plan に入力・履歴・候補・submitを載せた。Source Address contract 8件、root unit 30件、adapter package clippy `-D warnings` を通過し、`⭐️` VS16/`☆`差分、buttonのactual composite差分、port欠落/拒否/transport別の複数submitを検証した。KUC host facade は non-wire lease を retain/synchronize の同一 retained root に消費し、JSON wire を変更しない。追加必須 gate は facade 経由の real input で異なる二つの draft を別 transport として一回ずつ KUC-private port へ送ること、public frame/debug/receipt に raw draft を出さないこと、KLE AST が `SourceAddress*`/port/adapter/URL/OpenUrl を拒否し opaque host lease だけを許可することである。KLE opaque transit、actual KatanA `OpenUrl` effect、fixed-host evidenceは未実装であり、11.0k4を完了扱いにしない。
- [x] 11.0k4a1 2026-08-23: preloaded draft/button click を facade input evidence として不受理にした。空の `SourceAddressProjectionLease` に physical pointer focus、`RawInput::Text`、Enter を与える経路を KUC common `TextSurface` の focus ownership と Source Address nested input hit route を修正して成立させた。`source_address_strip_contract` 9件は standalone の focus/draft/submit、`host_root_facade_contract` 17件は retained root の異なる二 transport による二 draft を KUC-private port だけへ一回ずつ dispatch し、public frame/record/debug/receipt への raw draft 漏出を拒否する。`text_surface_interaction_contract` 15件、adapter package clippy `-D warnings`、fmt、diff check も通過した。これは KUC host facade の physical-input/non-leak slice に限る。history/candidate/IME/AccessKit の全状態、KLE opaque transit、actual KatanA `OpenUrl` effect、fixed-host evidence、full-spec Storybook は未完了のままであり、11.0k4 を完了扱いにしない。
- [x] 11.0k4a2 `docs/v0-1-0-storybook-full-spec-design.md` の `Navigation Input Scenario Boundary` に従い、KUC `FullTextCommandSurfaceScenarioFactory` に generic `NavigationInput` scenario を追加した。KUC が同一 opaque lease 内で Source Address input/physical pointer/text/Enter/private one-shot acknowledgement port を完結させ、KLE/Storybook は generic scenario ID、opaque transit receipt、KUC artifact/AccessKit のみを消費する。raw text/URL/path/SourceAddress 型/port/OpenUrl を KLE/Storybook に導入していない。KUC scenario physical/non-leak、`source_address_strip_contract` 9件、`host_root_facade_contract` 17件、`text_surface_interaction_contract` 15件、adapter package 全件、clippy `-D warnings`、fmt/diff check、KLE Storybook 29件、KLE AST、OpenSpec strict validation を通過した。複数 TextSurface 間で native focus を奪い返さない generic focus ownership を同時に修正し、検索→置換の actual IME physical regression も package gate で通過した。fixed KatanA actual `OpenUrl` host effect、全 editor leaf、OS 3 profile emoji は別 blocker のままとする。
- [ ] 11.0k5 11.0k の generic retained `TabStrip` は select/previous/next、overflow、close variants、pin/restore、context menu、drag reorder/group move、group create/add/remove/rename/recolor/ungroup/close/collapse、inline rename、opaque swatch palette、keyboard/AccessKit alternative を持つ。KUC は text/drag/palette/geometry/retained state を所有し、KLE は host-projected opaque tab/group/placement/swatch target と revision/correlation を非観測で一度だけ転送するだけとする。group-name text は non-Clone/non-Serialize one-shot host submission、color は host-issued swatch ID とし、KLE `PathBuf`/index/group state/hex/popup/text input/local persistence を AST で拒否する。各 source branch の actual KatanA handler/persistence/no-op/stale effect を source-derived host E2E で証明する。
- [ ] 11.0k5a `docs/v0-1-0-kuc-full-editor-root-integration-design.md` の `Tab Strip Root Integration Plan` に従い、既存 `SanitizedDocumentRoot` を KLE/public host root に接続せず、generic `CloseableTabStrip` adapter と opaque route table だけを `EguiTextCommandSurfaceHostRoot` の non-wire `TabStripProjectionLease` private child へ移植する。KUCはroot-first layout、private one-shot tab port、physical/keyboard/drag/AccessKit/artifact/no-leak/stale/duplicate/disabled gate、generic `WorkspaceTabs` scenario を所有する。KLE/Storybookはopaque lease/closed receiptのみを扱い、tab/group target、ID、index、path、label、color、drag geometry、state、local persistenceを持たない。11.0k5の全tab/group leafとactual KatanA persistence effectが通るまで未完了とする。
  - [ ] 11.0k5a1 KUC root boundaryから `Workspace*` / `CloseableTab*` alias、serializable ID/hex color/raw group-nameを排除し、generic descriptor、opaque capability target、non-wire consuming `TabStripProjectionLease`、private one-shot `TabStripEventPort` を定義する。既存 core は内部実装に留め、root/KLE public API・Debug・record・receipt・JSON へ既存型、target bytes、structural ID、label/order/path/group membershipを出さない。compile/source scan と opaque/no-readback testを追加する。
    - [/] 2026-08-23: KUC に generic descriptor、opaque target/correlation/swatch、full operation capability、non-wire consuming lease、proposal nonce ごとの one-shot port を追加した。port/operation は KUC generic public API に存在するが、payload は非 readback、KLE/Storybook の import/保持は `tab-strip-ownership-boundary` AST で拒否する。KUC strict clippy、focused tab-strip tests、fmt/diff check は通過した。root への消費、physical select/collapse proposal、artifact composition は 11.0k5a2 の初期 slice として実装済みだが、reject rollback、closed root receipt、全操作、actual KatanA effect は未実装であり、11.0k5a1 を完了扱いにしない。
  - [ ] 11.0k5a2 `EguiTextCommandSurfaceHostRoot` の factory/synchronize/root compositionを拡張し、TabStripを同一 retained rootの最初の領域として mount する。KUCはphysical inputからsealed `TabStripProposal`を抽出するだけで、active/collapse/order/pin/membership/color/close/restoreのhost-authoritative presentationをlocal actionでcommitしない。proposal rejection/stale/duplicate/missing port/invalid dropはlast accepted projectionへrollbackし、同一root identityのstrictly newer leaseだけがfinal stateを更新する。二重root/二重transportを拒否し、no-tab root byte compatibility、revision conflict/stale/duplicate/identity mismatch、root-first bounds non-overlap、artifact/AccessKit parent-child orderを実測テストで検証する。
    - [/] 2026-08-23: optional lease を factory/synchronize/root composition へ接続し、TabStrip を root-first rectangle と artifact layer に mount した。physical select と group collapse は proposal port へ一度だけ届き、newer lease なしに accepted presentation を local mutation しない root test を追加した。generic SVG/currentColor icon catalog を KUC に追加し、opaque localized presentation を受ける previous/next を root に実装した。enabled previous は一度だけ proposal を送り、disabled next は physical pointer sequenceでも無送信である。host-projected trailing affordance は normal tab の `RequestClose` と pinned tab の `SetPinned(false)` を physical input から別々に one-shot forward する。renderer は descriptor から proposal を直接組み立てず、KUC-private route table が opaque target/correlation を保持して select/collapse/previous/next/close/unpin を解決する。current-frame entry は `egui::Response` ID、actual bounds、localized AX label、disabled state を登録し、pointer、focused Enter/Space、AccessKit Click はその一経路だけで proposal を解決する。physical RawInput test は actual AX node/bounds、disabled AX no-op、pointer+Enter同一frameの一回だけの forward、public frame/opaque target non-leak を検証する。KUC root frame の AccessKit snapshot hash は全 current-frame evidence を含み、opaque target は hash inputにも出さない。fixed KatanA source に合わせ、tab overflow は popup/proposal ではなく KUC retained horizontal `ScrollArea` と active-tab reveal に訂正した。狭幅 physical frame で `request_active_reveal` と horizontal wheel input が proposal なしに clipped artifact だけを変えることを検証した。strictly newer plain root token は prior TabStrip lease を clear し、その visible-state change を facade result に反映する。2026-08-23 の fixed-source re-audit を requirements/design に反映し、context-menu の exact order（Right は Left より先）、virtual/pinned/group predicates、group popup の outside-primary close、rename/recolor same-frame precedenceを source-span として固定した。KUC route table は context-menu/group-popup の projection entry を opaque parent target と結合して close scopes/restore/pin/create/move/remove/ungroup/close/recolor proposalへ変換し、wrong-context operationには routeを作らない focused testを追加した。これは overlay renderer、submenu、rename/palette physical UI、dismissal、artifact z-order、rejection rollback、fixed-host effect の完成を意味しない。dirty confirmation、close scope/restore/menu、focus traversal、root-first bounds の厳密 non-overlap、lease revision conflict/stale/duplicate/identity、missing/rejecting port rollback、no-tab byte compatibility、AccessKit parent-child order、closed receipt は未実装のため本項は未完了である。KUC generic keyboard/AccessKit coverageはfixed KatanA tab keyboard parityの主張ではない。
  - [ ] 11.0k5a3 KUC private route table と sealed proposal transportを、select/previous/next/overflow/close variants/restore/pin/unpin/group create-add-remove-collapse-rename-recolor-ungroup-close/reorder/group move/dragに接続する。one-shotはroot全体でなくproposal/correlation単位とし、target導出、raw core event forward、disabled/stale/unknown/duplicate/replay、missing/rejecting port、raw name/color/ID/debug leakをfail-closed testで拒否する。group-nameはIME/Enter/Escape/outside/focus returnを持つnon-Clone/non-Serialize one-shot値、paletteはopaque swatch targetに限定する。
  - [ ] 11.0k5a4 KUC `WorkspaceTabs` full-surface scenarioを追加し、KUC内部でpointer、keyboard、drag、AccessKit、artifact、motionを全操作 familyへ対応づける。KLE Storybookはgeneric scenario ID、opaque continuation、closed receiptだけをrelayし、tab group text/geometry/target/stateをfixture、manifest、video metadata、debugへ持たない。GIF/MP4 decoded frame/hash、KUC Japanese/IME/`⭐️` color glyph evidenceを同じ root artifactで検証する。
    - [/] 2026-08-26: KUC `WorkspaceTabs` scenarioを同一retained rootへ追加し、本文、既存generic Markdown command surface、opaque `TabStripProjectionLease`、physical pointer drag、root-composited ghost/indicatorを一つのartifactに結合した。KLE Storybookはscenario IDとopaque lease/receiptだけを中継し、motion artifactの既定frame数をKUC planから導出して固定値による欠落を拒否する。92 frame PNG/GIF/MP4とmanifestを生成し、WorkspaceTabs focused testとKLE Storybook args testを通過した。これはgeneric scenarioの局所証拠であり、全tab/group operation family、keyboard/AccessKit、stale/reject rollback、KatanA host persistence effect、3 OS color emojiは未完了のままとする。
    - [/] 2026-08-26: default workbench composition を設計固定した。`WorkspaceTabs` は KUC 内で tabs、Markdown chrome、line gutter、document-find query/result/highlight、platform raster を一つの root artifact に合成する。Replace / Replace All は fixed KatanA host route 不在のため visible disabled の release blocker とし、KLE local semantics で補完しない。floating/context/code/read-only/IME は physical state-dependent overlay scenario として別途維持する。これは host effect、全操作 family、3 OS emoji の完了根拠ではない。
  - [ ] 11.0k5a5 固定KatanA sourceの tab source branchesを個別にactual host E2Eへ結び、document selection/scroll、close-confirm/force-close、pin/group persistence、reorder/group move、disabled/virtual no-op を物理入力、current-frame AccessKit target、natural action/handler/final stateで証明する。KUC/Storybook/scratch cloneの成功を代替根拠にしない。
- [/] 11.0k6 `docs/v0-1-0-kuc-root-emoji-evidence-tightening-design.md` に従い、KUC retained text-command root は root lifetime で単一の generic `PlatformFontCatalog` を共有する。TextSurface / CommandChrome / SearchStrip / ContextMenu は同じ policy、resolved color face、raw file SHA-256、catalog fingerprint を使い、個別 catalog discovery を作らない。`⭐️` (U+2B50 U+FE0F) / `☆` の crop は別 rasterizer で再描画せず、同一 RawInput trace の final root composite RGBA から KUC 内部だけで採取し、face SHA、catalog fingerprint、root RGBA/record/AccessKit hash と canonical artifact に結合する。KLE/KDV/Storybook への font path/family/pixel/child geometry 公開、font fallback、OS skip、文字列一致だけの合格を禁止する。macOS/Windows/Linux は実 profile artifact がなければ release gate を fail-closed にする。2026-08-22: shared-catalog root regression、current macOS root glyph evidence 4 tests、Apple Color Emoji pixel/hash control test、strict KUC clippy/fmt は通過。Windows/Linux provisioned profile artifacts は未取得のため release blocker のまま。
- [/] 11.0k7 `docs/v0-1-0-kuc-root-motion-artifact-design.md` に従い、KUC Storybook は closed `FullRootArtifact` receipt の順序だけを受けるgeneric GIF/MP4 writerを所有する。writerはKUC生成済みPNGのprovenance/SHA/dimension/unique stage/non-emptyを検証し、KUC管理のencoder/decodeでMP4 frame count/dimensionをfail-closedに検証する。KLE Storybookはpublic opaque root receiptを渡すだけとし、RGBA/PNG/gif/ffmpeg/compositor/fallback/handwritten glyphを保持しない。2026-08-23にKLE receipt stageをKUC連番へ戻し、KUC motion catalogueの完整 cycleだけを受け付けるようにした。KUC/KLE motion plan test、GIF/MP4 artifact生成、`ffprobe`の76 frame/1280x720確認、KLE AST lintは通過した。一方、full-surface数値gate、MP4 decoded-frame hash gate、Windows/Linux profile artifact、fixed KatanA host effectは未達であり、release evidenceではない。
- [/] 11.0k7a `docs/v0-1-0-kuc-full-editor-root-layout-design.md` §11に従い、KUC `FullTextCommandSurfaceScenarioFactory` を40行以上の汎用fixtureと状態遷移付きfull-surface scenarioへ拡張する。KUCがselection/floating lifecycle、find result/close、context submenu、readonly mutation拒否、actual resize/scroll/IMEを所有し、real-egui/AccessKit/RGBA artifactの数値gateを先に追加する。KLEはopaque scenario lease/stage/receipt relayだけを保持し、fixture text、selection/coordinates、font、markdown/host semanticsを追加しない。2026-08-23に42行のCJK/ASCII/Markdown/VS16 fixtureと6 scenarioのKUC catalogueを追加し、KLEはscenario ID/lease/stage/receiptだけをrelayする。motion planはselection、physical focus press/release、find input/IME/next/previous/close、context secondary pointer/Escape/focus return、primary dropdown trigger/itemの各aim/press/releaseとsettleを含む38 frame catalogueの完全な倍数以外をfail-closedにする。KUC scenario testはfocus後のactual scroll、resize、IME preedit/commit eventとstate、context close後のfocus return、dropdown open/select/close、unchanged redrawのlayout/composite byte stabilityを検証する。full-surfaceの全scenario AccessKit/RGBA gate、resize canonical canvas、fixed KatanA effectは未達である。
- [/] 11.0k7b `docs/v0-1-0-kuc-full-editor-root-layout-design.md` §11.6に従い、KUC automatic gutterは実測label幅だけでなくgeneric minimum hit column/paddingを所有し、40行scenarioで52px以上のgutterと本文との非交差をreal-egui/RGBA recordで検証する。KUC scenarioのprimary/floating command chromeは`CommandChromeIcon`による12個のgeneric rich-authoring affordanceをicon-only/tooltip/accessibility/group layoutで描画する。KLEはgutter width、padding、icon、command inventory、label、geometryを供給または補正しない。2026-08-23にexternal-consumer buildのKUC contractで、12操作すべてがAccessKit由来のopaque requestから対応する`CommandActivated`を一度だけdispatchすることを検証した。固定KatanA常設toolbarとの突合で`TaskList`が`Quote`と一致しないことを検出し、KUC generic icon/actionを`blockquote`へ置換して順序をtestで固定した。KUCの同一metrics-frame接続と40行fixtureは存在するが、全12操作のRGBA/AccessKit/full-surface gateは未実装である。固定Katana button inventory/effectはsource-derived native host E2Eまで未証明のrelease blockerとする。
- [/] 11.0k7c `docs/v0-1-0-kuc-full-editor-root-layout-design.md` §11.7に従い、KUC selection scenarioはfixed presentation selectionではなくsame retained rootへのphysical pointer press/move/releaseからselection/floating transitionを生成する。各stageでdocument/gutter/primary chromeを維持し、floating panelのbackground/border/corner radius/inner padding/group spacing/anchor/clamp/outside/Escape/focus returnをKUC paint plan/RGBA/AccessKit/root recordで検証する。KLEはpanel style、range、pointer座標、anchorを持たずopaque stageを適用するだけとする。2026-08-23にcurrent-frame KUC locator由来のphysical press/move/releaseと、実selection/floatingのfail-closed確認を追加した。outside/Escape/focus return、panel RGBA/AccessKit、full-surface維持の数値gateは未達である。
- [/] 11.0k7c1 §3.5.2-3.5.3のKUC `KucOpaqueTextSelectionDriver` / `KucOpaqueMotionContinuation`を実装する。current-frame locatorがactual text-surface recordからprivate hit areaを導き、KUC-issued motion frameだけが`capture_continuation`でdriverのstart/advance/acknowledgeを指定する。KLEはopaque continuationを保持・transitするだけで、selection range、pointer/rect、phase、callback resultを保存又は解釈しない。press/move/releaseは別frame、root mismatch/replay/order/final non-empty selection/floating欠落はfail-closedとする。2026-08-23にKUC public-root contract test、collapsed selection catalogue、KLE current-root artifact callback relayまで実装し、root mismatch/replay/order/empty selection/floating不在をtyped failureにした。KUC/KLE ASTとtarget testは通過したが、real-egui/AccessKit/RGBA/GIF/MP4 decodeの同時gateおよびfixed KatanA host effectは未達である。
- [/] 11.0k7d §3.5.4に従い、KUC `find` scenarioをstatic presentationからsame-retained-rootのopaque find traceへ拡張する。KUCがquery text inputのgeneric AccessKit target、opaque focus request、IME preedit/commit、next、previous、close、fixture-owned generic match/current annotation、disabled Replace/Replace All rejection、event/record/composite correlationを所有する。KLE/Storybookはopaque generic continuationをone-shot relayするだけとし、query/control ID、pointer、range、search state、replace mutationを持たない。2026-08-23にKUC external-consumer contractでquery focus、IME commit、next、previous、CloseRequested、disabled Replace/Replace All rejection、Close後のsearch target不在を検証し、KLE Storybookのopaque relayで76 frame/1280x720 GIF/MP4を生成した。最新artifact fingerprintはGIF `ffdf967a7707399d8cac8e9758d22c95f15146b9efba08e41ff0e0c91722e67b`、MP4 `2e5b2fba97d07071f42656fa98d92adadbe42367b06477d180ecb7eab27d54c2`、canonical manifest `7bb5b53c1be49cb83b7bf261624cc4b29d88fe1d6ec1f082a17477223d5c76a1`、manifest file `b07312749a5aed4fbcb7a125923f7c7c0fe35a58c35f43420b66c297311a4149`。fixture annotationはKUC paint/RGBAのvisual contractであり、固定KatanAの検索意味論やhost effectの証拠ではない。full-surface numeric/RGBA/AccessKit gate、MP4 decoded-frame hash、fixed KatanAのdocument find host effectは未達のrelease blockerとする。
- [/] 11.0k7e 外部consumerが全6 KUC full-surface scenarioをopaque leaseだけでrenderし、closed root recordの1280x720 dimensions、RGBA hash、paint-plan hash、record hash、AccessKit snapshot hashが非空であることを検証する。これは各scenarioのsame-root artifact/AccessKit生成の最低gateであり、KLEはrecord内のgeometry/RGBA/AccessKit payloadを読まない。2026-08-23にKUC external-consumer contract 9 testsで通過した。pixel occupancy、text height、match/currentの色差、floating/contextの全状態、resize canonicalization、MP4 decoded-frame hash、3 OS profile、fixed KatanA host effectは別gateであり、本項の成功で代替してはならない。
- [/] 11.0k7f KUC internal full-surface testで、resting/findのsame-style final composite RGBAを比較し、fixture-owned `generic-search-current` と `generic-search-match` のpaint colorがfind frameでのみ増えることを検証する。paint planの存在、text string、hashだけで合格にしない。KLEはcolor、annotation rect、pixel countを観測又は保存しない。2026-08-23にfocused KUC testで通過した。これはfixture visual contractだけを検証し、host query semantics、3 OS profile、fixed KatanA host effectを代替しない。
- [/] 11.0k7g KUC `context` scenarioをsecondary pointer openだけで終わらせず、same retained rootでEscape closeとtext focus returnまでのopaque stageとして追加する。open frameのcontext record/RGBA/AccessKit、close frameのmenu不在、Closed event、focus returnをKUC testで検証し、motion catalogueは新しい完全cycle以外をfail-closedにする。KLEはcontext item、pointer、menu geometry、close reasonを持たずopaque stageをrelayするだけとする。2026-08-23にKUC internal scenario 18 testsでopen composite、Escape close frame、Closed event、subsequent text focus returnを確認し、external consumer 9 testsでopaque transport経由のClosed eventだけを確認した。KLE Storybookは29-frame cycleの倍数58 frameを生成し、公開frame 016/017でmenu open/closeを限定目視した。固定KatanAのcontext-menu操作/host effectは別未達blockerのままとする。
- [/] 11.0k7h `docs/v0-1-0-kuc-full-editor-root-layout-design.md` §11.3.1に従い、KUC `CommandChromeDropdown`のgeneric language-choice profileをfull-surface scenarioへ接続する。KUCが17個のvisible language label、opaque target、pointer/keyboard/AccessKit open/select/Escape/outside/focus-return、same-root record/RGBA/AccessKit/event transportを所有する。KLEはitem count、label、target、pointer、menu geometry、close reason、selectionを読まずopaque stage/receiptだけをrelayする。2026-08-23にKUC primary dropdown triggerをgeneric `DropdownTrigger`としてAccessKit/locatorへ公開し、foreground layerへ移してtoolbar child clipを排除した。current locatorのopaque clickはaim/press/releaseを別frameで発行し、毎frameでKUC内のAccessKit targetを再解決する。KUC retained rootがAccessKitを自律有効化し、artifact compositorもforeground dropdownを本文後に合成する。full-surface 38-frame catalogueはtrigger/itemの各aim/press/releaseとclose後のsettleを実行し、opened/closed final composite RGBA hashの差、17候補表示、ItemActivated closeをKUC internal testで検証する。external consumer 12 testsでactual rootにおける17候補のclosed/open、pointer選択、ArrowDownによる最終候補到達、Enter選択、Escape後のtrigger focus return/reopenを検証し、KUC package 164 unit + 101 integration tests、strict clippyを通過した。KLE Storybookは76-frame GIF/MP4を生成し、MP4 decodeで1280x720/76 frameを確認した。未達: fixed KatanA `CodeBlockKind`、Markdown transform、undo/dirty/preview/cursor effectとnative host E2E。これらをKUC/KLEへ持ち込まず、固定host E2Eの未達blockerに残す。
- [ ] 11.0k5a 11.0k5 の visible tab/group operation は capability boolean だけで local English/action/target/value を推測しない。KUC sanitized input に localized visible/tooltip/accessibility/empty/error text と opaque target/revision/correlationを明示し、KUCが close/menu/inline IME/palette/drag/keyboard/AccessKit を retained state として所有する。structural render id、tab/group index/order/path/membership、color string、raw group-name text、egui response は public KLE transport/receipt/manifest/Debug に漏らさない。select/collapse foundation を close/menu/rename/recolor/drag/overflow/restore/pin complete と誤認しない。
- [ ] 11.0k5b host effect を伴う close/pin/restore/reorder/group operation は KUC current projection を先行 mutate しない。KUC は non-mutating generic intent を opaque closed transport に一度だけ入れ、tab/group descriptor は host が次 revision projection で確認するまで保持する。dirty confirmation、force-close、persistence、document selectionを KUC generic core state/action に読み込ませず、`TabClosed` 等の local successを host effect の証拠にしない。KUC retained state は menu/focus/hover/inline UI に限定し、physical input、stale/duplicate、unchanged descriptor、host E2Eを検証する。
- [ ] 11.0k6 11.0k の KUC opaque full-editor root は generic retained `PreviewSideRail` と internal `OutlineNavigator` を含む。opaque rail-item/panel/source-target/outline-item descriptor、capability、icon/label/AX data、button/tooltip/focus、host-projected generic leading/trailing pinned placement、overlay anchor/clipping、hover-switch delay/cooldown、sibling exclusion、outside/Escape、empty content、TOC row event、hierarchy/accordion/expand-collapse/active target/scroll-into-view、same-frame artifact/record/AccessKit を KUC が所有する。既存 KUC `structured::CollapsiblePanel`、`disclosure::HoverCard`/`Popover`、generic `TreeView`/disclosure substrate を拡張・合成し、file-specific `FileTree` API や並列 panel/hover/tree 実装を公開 root に複製しない。KUC input/output は TOC/export/story/tools/format/path/line/outline index/anchor/source priority/KatanA/KDV/KRR/browser/slideshow type を含まない。KLE/Storybook の local rail/panel/outline/`egui::Area`/timer/geometry/`TocPosition`/format/path/line/anchor/force-open/host-state mutation を AST と public RawInput/root-frame test で拒否する。rail/panel/outline mechanics は unchanged-host retained evidence、opaque host target は actual KatanA route/relay effectを必須とする。
- [ ] 11.0f KUC CommandChrome/Search controlled synchronization を完了する。Search は既存 KUC `structured::SearchControlStrip` と command-chrome search adapter を opaque root 内で拡張・再利用し、host-projected opaque presentation/capability と KUC one-shot records は KLE を非観測で通過するだけとする。KLE は selection/read-only/search value/result/action/group/item を同期又は保持しない。KUC-owned focus/dropdown/tooltip/input identity を保持し、KLE local label switch、button inventory、second SearchStrip/form store、`egui::TextEdit` は禁止する。
- [ ] 11.0f0 KUC public boundaryに opaque `CommandChromePresentation` を追加する。opaque command/group/item/**host-target** id、order、localized label/tooltip/accessibility text、KUC `UiIconProps`、search strings/capabilities/icons は host/Storybook が注入し、KLE は target/presentation を解釈せず KUC one-shot record と同じ revision/correlation で一度だけ transit する。`EditorConfig::Strings` 又は command id からの KLE label/icon/group switch、`EditorAuthoringCommand` / `EditorCodeBlockKind` collection、unknown injected command omission、blank/fallback controls を AST/contract test で拒否する。neutral crate に KUC concrete type 又は KatanA Markdown enum を漏らさない。
- [ ] 11.0f1 `KucCommandChromeBinding` を egui crate に追加し、KUC task 3.1g-15 の single retained text-command surface model を host-provided opaque presentation と editor-domain visibility policy だけで同期する。KLE は individual floating toolbar/search strip/adapter、KUC `TextSurfaceFrameRecord` caret/viewport bootstrap、old pixel DTO、panel size/clamp、command label/icon/group inventory を持たない。
- [ ] 11.0f2 KUC command/dropdown/search event を opaque host target forwarding 又は single-consumption document-search transport として forwarding する。host-injected item-to-target relation は opaque host target（KatanA toolbar の `IngestImageFile` を含む）だけを target とし、authoring target は unchanged host projection lookup が `AppAction::AuthorMarkdown` へ解決する。KLE command-id/label/icon switch、`EditorAuthoringCommand` / `EditorCodeBlockKind`、Markdown/code-kind enum、query/options/match/current-index/range/byte conversion/content mutationを持たない。Replace / Replace All は KatanA host 追加仕様判断まで blocker とし、active-match scroll は host-projected opaque source targetに置換する。KUC task 3.1g-16 が generic dropdown hit-regionを受入れるまで最終 item をKLEで補正しない。5 groups / 12 toolbar trigger、14 context authoring trigger、17 code kinds、file/clipboard-image/file-URL の3 image ingest route、unknown injected action、disabled/read-only、outside/Escape/focus return、Japanese/`⭐️`、RawInput/AccessKit/artifact/actual KatanA effectを検証する。
- [ ] 11.0f2b document search host transport を KUC current-projection capability に固定する。generic root batch 内の raw `CommandChromeSearchEvent` と sanitized search transport の二重配送を除去し、各 operation target の host-issued one-shot capability が current revision/correlation を解決する。KLE は opaque root の show/forward のみを行い、target/action table/query/replacement/resolver registry/retry queueを保持又は readbackしない。Japanese/`⭐️` VS16/ZWJ の query IME、next/previous/options/closeをphysical RawInputで起動し、KLE release では capability一回、receipt一件、raw root event不在、stale/duplicate/failure no-mutationかつ再送不能、Debug/manifest漏洩なしをKUC/KLEで個別検証する。host の Markdown-aware result/range、replace-one/all、document mutation/undo/scroll は KatanA #336 の公開後 host E2E DoD とする。
- [x] 11.0f2b1 KUC foundation: raw search channel を root batch から一回だけ detach し、canonical sanitized transport にした。target-scoped `FnOnce` capability、opaque rejection、outer forwarder 前の capability dispatch、newer projection 後の old-frame `StaleFrame` guard、Japanese IME exact `⭐️` callback/Debug leak guardを実装した。physical RawInput/AccessKit の enabled leaf 10件（query、replacement、replace-one/all、next/previous、match-case、whole-word、regex、close）ごとに、exact callback一件、raw root search event不在、receipt一件、outer forwarder一件、replay禁止、text leafの日本語/`⭐️` VS16/ZWJ Debug非漏洩を検証した。disabled/target不在、wrong capability kind、callback rejection、newer revision後のstale frameも fail-closed で確認済み（`cargo test -p katana-ui-core-egui-adapter sanitized_ --lib --locked`: 83 passed、all-target clippy `-D warnings`/fmt passed）。KLE thin transit、actual KatanA host effectは parent 11.0f2b の未完条件として別途残る。
- [x] 11.0f2b1a regex enabled target は KUC generic projection から導出し、physical RawInput click の `Regex(true)`、one-shot/replay reject、root-batch non-inclusion、opaque Debug、wrong capability kind no-callback を追加検証した。さらに match-case/whole-word/regex の host-projected initial/acknowledged boolean state を target/capabilityと同じ projection fingerprintへ含め、target不在時の unchecked/disabled、same-revision conflict、newer-revision acknowledgementをKUC RawInput/AccessKit testで確認した。KLEがstateを保持しない controlled generic inputはKUC範囲で成立した（83 passed、all-target clippy/fmt passed）。
- [ ] 11.0f2c command/dropdown activation を target-scoped one-shot capability に正規化する。KUC が toolbar/floating/dropdown/context の KUC-private structural render IDから original opaque targetへ戻し、generic `SanitizedCommandActivationTransport`だけを closed root transportへ入れる。raw `CommandChromeToolbarEvent` / floating event / dropdown child eventを generic root batch/fingerprint/cardinality/outer forwarderに残す、KLEがcommand id/label/group/icon/dropdown index/Markdown/code-kindをreadback又はaction mapする、KUCにKatanA enum/path/clipboard payloadを入れる方法は禁止する。physical RawInput/keyboard/AccessKitのdirect/dropdown、disabled/hidden/unknown、outside/Escape/focus、callback rejection、stale/duplicate/outer forwarder failure、Japanese/`⭐️`/ZWJ Debug/manifest leakをKUC個別leafで検証し、14 Markdown/17 code-kind/3 ingest routeのhost effectは後続KLE/KatanA E2Eへ結合する。
- [x] 11.0f2c1 top toolbar foundation: `SanitizedCommandActivationTransport` が top `CommandChromeToolbarEvent` を root batch から detach し、opaque target-scoped `FnOnce` capability として outer forwarder 前に一度だけ dispatch する。dropdown を持つ command は `SplitSecondary` とし、physical RawInput primary click が direct target のみを、physical secondary click + AccessKit child activation が dropdown target のみを各一回起動することを別 callback で検証した。disabled/hidden/missing/rejected/stale/replay、keyboard one-shot、raw batch非包含、opaque Debug leakも KUC sanitized root test 89件、fmt、all-target clippy `-D warnings` で独立再確認済み。floating toolbar は未接続のため parent 11.0f2c は未完とする。
- [x] 11.0f2c2 context-menu foundation: KUC-private structural item ID から opaque target-scoped `FnOnce` capability を解決し、context raw event を root batch から detach して canonical closed transport だけに入れる。actual RawInput の secondary open -> pointer leaf、nested submenu child、keyboard、AccessKit の各 route を一回だけ callback/forward/replay reject まで検証した。disabled/invisible は callback/event を作らず、missing/rejected/reentrant は typed fail-closed、parent submenu capability は child leaf 選択で不発、target/label は Debug/receipt/transport に非漏洩とした。独立実行で `context_menu_` 4件、`sanitized_` 94件、all-target clippy `-D warnings` は通過した。KUC worktree 全体の fmt gate は他の未整形差分を含むため未達であり、floating toolbar、KLE thin transit、actual KatanA host effect は parent 11.0f2c の未完条件として残す。
- [ ] 11.0f2b2 KLE production cutover は `KucRootBinding` を唯一の retained render object として atomic に置換する。host-projected opaque root input / one-shot forwarder を release path の唯一の入口とし、`KucTextSurfaceBinding`、KLE local content/selection/search/authoring/diagnostic/pixel state、`latest_kuc_*`、child aggregate/output reconstruction、direct egui input/context helper を同一 release graph から除去する。既存 `LanguageEditor` / `Editor*Control` / `EguiLanguageEditor::show(ui)` の semantic compatibility wrapper は release path、Storybook、host-E2E から到達不能に隔離し、source/dependency AST gate と actual RawInput one-call/one-forward recordで検証する。KUC full-root operation contract が先に揃わない限り partial wiring/fallback/feature flag を作らない。
- [ ] 11.0f2a KUC task 3.1g-14 と consumer-safe retained model の 3.1g-15、および visible dropdown item precedence の3.1g-16が完了するまで、KLE は TextSurface と CommandChrome を sequential に同じ `egui::Ui` へ描画して search-strip を root 外へ配置してはならない。KLE は generic single-surface `EguiTextCommandSurfaceAdapter` の controlled input/output を一度だけ domain map し、root/child rect、height reservation、`Ui` child creation、search widget/overlay、floating anchor/viewport bootstrap、dropdown hit-test/event synthesisを持たない。public `show` の actual RawInput/AccessKit で Japanese/`⭐️`/IME/edit/toolbar/document search/replace blocker/focus/outside/Escape/artifactのreachable boundsを検証する。
- [ ] 11.0f3 Storybook の `authoring_contract` から `EditorFloatingMenuPlacement`/`EditorPixel*`、`EditorAuthoringMenuState`、文字列 command/code-kind assertion を削除し、実 KUC floating record/artifact/opaque-target callback assertion に置換する。host presentation は KatanA の 14 Markdown commands/17 code kinds/image ingest/full toolbar group を opaque targets として注入し、actual RawInput の selection/dropdown/outside/Escape/focus-return/disabled/read-only/document search/replace blocker を public editor composition で実証する。full Storybook document search と replace blocker state、same-surface GIF/manifest/video まで繋がる前に source-file-only assertionへ後退させない。
- [/] 11.0f3a KUC task 3.1g-12 の public artifact compositor が完了した後、KLE egui crate が one public `show` call の latest KUC TextSurface/floating/search artifact を read-only aggregate として公開する。single root `show` と aggregate API、Japanese/`⭐️`/selection/search/optional-child clear の actual RawInput record/artifact contract は実装済み。KLE は plan merge、canvas union、pixel blend、texture sampler、glyph/fallback renderer を持たず、Storybook は actual RawInput/eframe root bounds を unchanged `ArtifactCanvasBounds` として KUC compositor に渡す。Storybook の public compositor RGBA/PNG/GIF/manifest/MP4 evidence と fallback/shape-count/manual compositor AST rejection が残る。
- [ ] 11.0f3a-1 public `show` の KUC root evidence を、Japanese/`⭐️` VS16 の色付き RGBA texture、IME/focus/selection/scroll/AccessKit、gutter active/hover/diagnostic marker、unchanged frame/artifact ownershipまで強化する。文字列又は AccessKit label だけを emoji 表示証跡にしない。現行 KUC は opaque root を未公開で、KLE の aggregate/string/shape check は有効な色グリフ証跡ではない。
- [ ] 11.0f3a-2 11.0f3a-1 を通した同一変更で、孤立した KLE-local `PlatformTextSurface`、`LineGutterModel`、`widget_text`、`widget_gutter` と専用 tests の6ファイルを削除し、KLE direct text-raster dependency を除去する。旧 renderer の移動・互換 fallback・AST exclude は禁止する。
- [ ] 11.0f3a-3 `kle-linter` に旧6ファイル名、`PlatformTextSurface`、local raster/texture/gutter renderer の crate-wide residual guardを追加し、KUC thin binding 以外の再導入を failする。
- [ ] 11.0f3b KUC task 3.1g-17 の retained text-command-context root が完了した後、KLE egui binding は KUC TextSurface-derived `ContextTargetRequested` fact と host-injected opaque context-menu tree をその単一 root に同期し、typed item event を neutral target map に一度だけ map する。KLE local right-click/keyboard coordinate、menu/Area、submenu/focus/AccessKit、paint/artifact/compositor order を追加しない。Save、host capability が editable `.md` / `.markdown` の時だけ存在する FormatDocument、14 authoring action、nested 17 code kind、file と clipboard-image の2 visible ingest itemを data-driven map する。clipboard file URL は clipboard-image handler の第三 host outcomeとして実 KatanA adapterで検証し、KLE menu leafを追加しない。public RawInput right-click/keyboard/AccessKit/submenu/disabled/hidden/outside/Escape/focus-return/colored-star artifact test と Storybook same-surface evidence を必須化する。
- [ ] 11.0f3b-1 ContextMenu の viewport overflow は KUC が所有する。KUC public frame record は current menu bounds 内で実際に hit-test 可能な item だけを返し、offscreen item は KUC scroll/focus/AccessKit state を経て新しい record へ更新する。KLE は座標補正、疑似 item selection、手動スクロール、menu bounds 外の record 利用を禁止する。KUC は長い direct submenu の最後の leaf と nested 17 code-kind submenu の最後の leaf を pointer/keyboard/AccessKit open の物理入力で検証し、KLE-owned host E2E は KatanA の実階層（Save / conditional Format / Edit / Ingest）と全 leaf を同じ public record のみで検証する。
- [ ] 11.0f4 KUC floating toolbar の panel measure/placement を完了する。KLE は current KUC `TextSurfaceFrameRecord` 由来の anchor/viewport facts と visibility policy だけを渡し、KUC が panel dimensions、clamp、outside/Escape、focus return を所有する。KLE popup-coordinate algorithm は AST で拒否する。
- [/] 11.0f4a floating toolbar は top toolbar の projection を暗黙複製しない。KUC sanitized root input は top/floating を別々の opaque command projection と controlled visibility policy として保持し、KUC-private structural IDs から各 original target-scoped capability へ一度だけ戻す。input/fingerprint/presentation mapping と top/floating event routing は実装済みで、pointer、keyboard、current-frame AccessKit node click の各物理入力が floating capability だけを一回実行し、top callback不発、root raw event 非包含、forward一回、replay拒否、target/label/geometry/child構造の公開 Debug/transport 非漏洩を確認した。actual RawInput の二段階 drag により non-empty selection から test-only floating record が出現し、collapsed selectionで消失する。disabled/hidden/missing capability、callback rejection、newer revision後のstale frameも fail-closed とし、native egui focus response が消費した `Enter`/`Space` を KUC keyboard router が再配送しない回帰を追加した。Escape/outside dismiss は same-frame floating消失、次empty frameのTextSurface focus returnまで実証した（`sanitized_` 102件、all-target clippy）。KUC は actual selection/caret/viewport record から measure/placement、outside/Escape、focus return、dropdown/AccessKit を所有する。KLE は pixel/caret/viewport/ID/label/indexを readback せず、各 host-provided opaque descriptor と visibility policy の transit に限る。generic root の read-only controlled policy は revisioned input、physical focus後の Text/IME no-mutation、selection維持、AccessKit read-only、enabled opaque command の意味非推測まで `sanitized_` 106件と all-target clippyで確認した。実 selection 後の floating public frame/record/Debug は injected Japanese/`⭐️`/opaque target/child geometry/artifactを公開せず、test-only geometry helperの production accessor不在を同じ suite と clippyで確認した。KLE binding、actual KatanA host effect を固定してから本項を完了にする。
- [ ] 11.0f5 KLE の `EditorFloatingMenuPlacement` / `EditorPixelPoint` / `EditorPixelRect` / `EditorPixelSize`、`EditorAuthoringMenuState::toolbar_placement`、pixel placement constants/module を KUC placement engine へ移行して削除する。KLE は editor-domain visibility/selection/read-only policy のみを保持し、KUC frame record 由来の anchor/viewport facts を転送する。KLE public/core に panel position/size/coordinate algorithm を残さない。
- [ ] 11.0f6 authoring control presentation を host-provided data-driven input に置換する。KLE local `command_label` switch、固定 command inventory、未知 command omission、local icon selection を削除し、opaque command/group/item id、order、localized label/tooltip/accessibility data、adapter-bound host icon props を KUC CommandChrome へ転送する。KatanA の全 group/17 code kinds と unknown opaque command の actual-egui/AccessKit test を必須化する。
- [x] 11.1 KLE の emoji / IME / grapheme caret / text area interaction 要件を KUC 既存 contract と照合する
- [x] 11.2 KLE 側で ad hoc 実装しそうな UI 汎用部を洗い出し、KUC 側に寄せるべきものを記録する
- [/] 11.3 KUC 側に追加が必要な場合は、Katana 固有 namespace を入れずに local KUC repo へ実装する。`kuc-command-chrome-runtime` の generic dropdown runtime/component/actual-egui/guard、TextSurface の scroll paint-hit-IME-AccessKit/gutter/annotation/selection/caret 座標契約、range-anchored marker の SVG bounds/hit/AccessKit/artifact、KUC CommandChrome standalone same-surface artifact、controlled scroll/focus request/acknowledgement、f32 `RelativePixels`/visible logical rows、public generic artifact compositor、consumer-safe retained root surface と visible dropdown item precedence は確認済みである。ContextMenu の generic overflow は 480x220 の KUC RawInput component evidence を持つが、KLE-owned actual `KatanaApp` host E2E（KatanA 実階層、全 leaf）は未証明である。KUC が stale smooth-scroll delta の再適用を修正し、KLE の座標・scroll・action workaround は追加していない。KLE の実 component consumption、Storybook、KLE host E2E、AST gate は未完了である。
- [/] 11.4 KLE は KUC の theme / font / TextSurface / text span / action / hit target / coordinate contract を利用し、egui fallback を正当化しない。temporary `PlatformTextSurface` / `LineGutterModel` / `authoring_helper.rs` と Storybook fallback が残るため未完了である。
- [/] 11.5 KUC に寄せた挙動は KUC 側 contract test と KLE 側 integration/storybook test の両方で検証する。CommandChrome、range-anchored gutter marker、controlled scroll/focus、f32 `RelativePixels`/visible logical rows の KUC core/actual-egui/AccessKit/artifact/guard は完了したが、KLE integration と KLE Storybook evidence は未完了である。
- [ ] 11.6 KDV の `emoji_text` / OS emoji font-family / text-raster 相当の重複実装を KUC platform text-raster runtime 利用へ移し、KLE/KDV が別々の emoji segmentation、font lookup、raster cache を持たないことを確認する
- [/] 11.7 main/orchestrator は Spark 実装結果を KUC/KLE/KDV の責務境界でレビューし、KLE-only または KDV-only の emoji 表示対策を差し戻す。KLE-owned `authoring_helper.rs` と Storybook fallback を boundary violation として差し戻し済みであり、KUC command chrome への置換まで未完了である。
- [ ] 11.8 `⭐️` は入力文字列保持だけでなく、KUC platform text-raster runtime の color glyph output、variation selector の保持、grapheme measure/hit-test を通って実 editor surface に表示されることを KUC/KLE/KDV tests と same-surface Storybook artifact で検証する
- [/] 11.9 Storybook gate (`kuc-contract-check`) と KUC `text_area_contract` は `Type("\\nemoji ⭐️")` と `ImeCommit("⭐️")` で `TextAreaEvent::EmojiInput { grapheme_count: 1 }` を必須化する。KLE same-surface consumption、exact VS16 color-glyph artifact、KLE-owned actual `KatanaApp` IME host E2E、full editor Storybook が未完のため、完了扱いにしない。
- [ ] 11.10 Storybook motion artifact のframe schedule/provenanceはKUC `FullTextCommandSurfaceMotionPlan`だけが所有する。KLEはKUC-issued scenario/stage/provenanceを不透明にrelayし、`RawInput::default()`、KLE stage label、任意のminimumへの丸め、unclassified/idle frameを生成しない。要求frame数が全scenario catalogue未満ならfail-closedとし、GIF/MP4/manifestの全frame数とKUC provenanceの一致をstrict testで検証する。artifactはfixed KatanA host E2Eの代替にしない。
- [ ] 11.10 `docs/v0-1-0-kuc-text-emoji-root-migration-design.md` の境界監査を実装開始ゲートにする。KUC single opaque root frame、`PlatformFontCatalog` の一回限り discovery/load と face SHA、三OS isolated `⭐️` versus `☆`/Japanese/IME/ZWJ proof、KUC full-root Storybook と deterministic manifest が揃うまで、KLE-local surface/aggregate/fallback の置換実装を開始しない。KLE/KDVの重複実装、child artifact再公開、shape-count/minifb/fixture証拠、KatanA変更は不受理とする。

## User Review Phase: boundary corrections

- [/] Design and plan the KUC full editor root layout contract in `docs/v0-1-0-kuc-full-editor-root-layout-design.md`. The design fixes KUC-owned generic slots, measured viewport propagation, command-family mount uniqueness, overlay/focus/IME/artifact contracts, the KLE opaque boundary, Storybook fixture states, and strict verification including separate fixed KatanA host E2E. The first KUC-only measured viewport propagation slice is implemented; three focused real-egui resize/scroll/slot tests pass. KUC text-raster now has an isolated runtime metrics foundation (determinism, scale, Japanese/ASCII/VS16 input preservation; 27 focused tests and strict crate clippy), but root/style/gutter/search/chrome/floating propagation remains unimplemented. The 2026-08-23 public-root motion artifact removed the duplicate primary toolbar but remains visually unacceptable: sparse fixture, initial floating selection, text/gutter readability, search decoration, mount identity, and fixed-host evidence remain open release blockers. No KLE coordinate/font workaround or KatanA-specific API is permitted.
- [/] 「完璧でないなら停止しない」。未達の release precondition、source closure、KUC root、KLE thin binding、full-spec Storybook、strict verification のいずれかが残る間は KLE の完了・承認・リリース可能を報告しない。KatanA actual host E2E は公開後の KatanA #336 DoD として別に追跡する。Spark 実装枠が利用不能な間も、orchestrator は source audit、設計、台帳是正、静的/実行検証を続ける。画面、スクリーンショット、動画、旧 Storybook callback、source-only/downstream simulator は完了根拠にしない。
- [ ] KatanA-compatible authoring toolbar / document search / replace の UI は KLE の独自部品として実装しない。KUC `CommandChrome` / `FloatingCommandToolbar` / `CommandChromeSearchStrip` / SVG raster / egui adapter を設計・実装した後、KLE は opaque descriptor/event の非観測 one-time transit のみを持つ。暫定 `authoring_helper.rs` は release 前に削除する。
- [ ] KatanA-compatible multiline text surface / line gutter / range annotation / IME / selection / generic accessibility UI は KLE の独自部品として実装しない。KUC `TextSurface` / shared egui adapter / TextArea / selection / ScrollArea / ContextMenu / DiagnosticsList を設計・実装した後、KLE は opaque descriptor/event の非観測 one-time transit のみを持つ。暫定 `platform_text_surface.rs` / `line_gutter.rs` と generic egui overlay を release 前に削除する。
- [ ] KatanA が持つ syntax highlighting、normal text copy/cut/paste、shortcut arbitration/conflict/read-only、undo/redo、native IME/ZWJ selection、AccessKit、document find/replace UI、未登録 rendering integration target は full source inventory から個別 feature として再分類し、KUC/KLE/KatanA host owner と runnable evidence が固定されるまで未完了とする。`Unsupported`、host-state hook、KLE-only event injection、source-file existence は完了根拠にしない。
- [/] `tools/katana-parity-check` の fixed 26-row `FEATURES` matrix と fixed 48-ID `LegacyLeafCapabilityAudit` を acceptance route から除去し、`command-chrome` / `text-surface` / `editing-history` / `accessibility-tree` / `find-replace-ui` を含む generated source closure を唯一の requirement-to-owner-to-evidence contract にする。2026-08-21 の実査で `main.rs` が両固定 inventory を実行していることを確認した。unknown feature id、missing source mapping、host-only/UI-only evidence、open blocker を fail し、要件行を減らして pass させない。2026-08-23: production main route から固定 matrix、CapabilityManifest、legacy leaf inventory を完全隔離し、generated six source-closure artifacts と execution record のみを leaf acceptance 入力とした。historical 解析は `#[cfg(test)]` に限定した。2026-08-26: canonical root にKLE/KUCのfull Git SHAとuntrackedを含むclean status evidenceを追加し、dirty treeをartifact生成前にrejectする86件のsource-closure testを通過した。workflow/Justfile materialize CLIはexact `--canonical-root` をstatic contractで検証する。ただしKUCの新規generic root実装は未commitであり、workflowはKUC SHAを全jobへ固定していない。actual source closure CI、3 OS capture、fixed host E2E、emoji proof は未達のため [x] にはしない。
- [x] fixed KatanA revision fixture は live `master` を参照 revision に昇格させず、固定 commit の clean detached checkout を使う。2026-08-21 に fixture unit test が case ごとに full `git clone --no-local` を行い長時間化することを確認した。process-local `OnceLock` の immutable cache を一度だけ固定 revision で clone/detach し、read-only seal 後に fixture で共有する。cache canonical root、HEAD、fixed source bytes、fixture cleanup 非干渉、foreign/wrong revision rejection を `source_closure` 29件で検証済み。shared mutable checkout、live-tree reuse、revision/tree SHA 検証の省略、test timeout の緩和を導入せず、immutable verified cache 又は最小 fixture tree を設計して同一固定 source bytes を検証する。
- [ ] Storybook の full-spec surface は KatanA の toolbar group、selection/read-only disabled state、code-block dropdown、image ingest action、document-search query/previous/next/close/count/highlight、replace/replace-all を実 KUC root と opaque KLE transit として提供し、各操作を real-egui event と actual host resolution/effect で検証する。MVP canvas や fallback artifact は完了根拠にしない。
- [ ] KUC `UiIconProps` の公開 SVG raster runtime と additive `CommandChromeAction` icon contract がない間、KLE/KDV に SVG parser、raster cache、文字アイコン代替を追加しない。KatanA icon assets は host-provided props として注入する。
- [/] KLE 側だけの font fallback 調整で `⭐️` 表示を良しとしない。OS 依存 emoji 表現は KDV 既存実装から KUC の汎用 contract へ移し、KLE/KDV は adapter として使う。KDV の `emoji_text` は KUC alias だが、direct `cosmic-text` 残置と KLE same-surface evidence があるため未完了である。
- [/] Storybook screenshot は判断材料に留め、合否根拠は KUC contract test、KLE input/layout integration、KDV migration test、acceptance artifact gate に置く。
- [/] main/orchestrator は Spark の実装境界違反を監視し、KUC に置くべき汎用 UI が KLE/KDV に重複実装された場合は完了扱いしない。KLE `authoring_helper.rs` と Storybook fallback は KUC migration 完了まで release blocker である。
- [/] 入力文字のガタつきを目視だけで良しとしない。Storybook motion artifact に `motion_artifact_input_line_stable_jitter_detection` / `motion_artifact_caret_bbox_tracks_model_position` / `motion_artifact_japanese_and_star_glyph_bboxes_are_stable` / `motion_artifact_line_numbers_and_markdown_helper_bboxes_are_stable` を追加し、入力行 top/bottom、prefix left/right、実 caret bbox、入力済み日本語/⭐️ glyph bbox、line number bbox、Markdown helper bbox のフレーム間安定性を数値検査する。
- [ ] interactive Storybook の入力文字ガタつきは旧 `setup_fonts` / `ctx.set_fonts` 仮説で処理しない。2026-08-14 の current source audit では当該実装 path が存在しないため、列挙済み test 名も完了根拠ではない。Spark は KUC opaque full-root の continuous public RawInput trace を各 release profile で実装し、append/IME commit/selection edit 中の unchanged prefix glyph/hit bounds、line-gutter bounds、root bounds、catalog fingerprint が exact に不変であり、caret bbox が同一 frame の KUC authoritative caret/hit record と一致することを検証する。KLE request reconstruction、child artifact、shape count、fallback pixels、time threshold、目視は受理しない。
- [/] `motion-artifact` が `EguiLanguageEditor::show` の shape count だけを確認し、動画 pixels を `StorybookFallbackRenderer` から作っていた。これは live editor の証拠ではないため、fallback pixels を acceptance から除外し、KUC platform text-raster runtime を通る同一 editor surface の artifact と数値 contract を release gate にする。

---

## 12. KatanA full editor parity audit / integration

- [ ] 12.1 `katana-ui/src/views/panels/editor/**` と `katana-ui/tests/integration/editor/**` を source of truth として全機能を再棚卸しし、syntax highlighting、clipboard text、shortcut、native input composition、未登録 test target を含む `docs/v0-1-0-katana-editor-full-parity-audit.md` に記録する
- [ ] 12.1a KatanA editor capability manifest を 26 top-level group と strict leaf case の表として、source path/line と decisive symbol、KUC/KLE/KatanA owner、KLE public actual-input evidence、KLE-owned actual KatanA host-E2E evidence の一対一表にして `tools/katana-parity-check` と audit document に追加する。leaf case は最低でも toolbar 5 groups / 12 trigger、context authoring 14 trigger、17 code kinds、file/clipboard-image/file-URL の3 image route を別々に解決する。KatanA は read-only reference とし、KatanA source edit 又は未存在 adapter module を証拠要件にしない。source-only/host-only/Storybook-only/simulator-only evidence、empty `remaining_gap`、未実行 host-E2E を fail し、将来の test 名を実行済み根拠にしない。
- [ ] 12.1b KatanA context menu を Save、editable Markdown extension 条件付き Format、14 authoring、17 code kinds、file image、clipboard image、clipboard file URL paste に分解し、actual right-click/keyboard/AccessKit、enabled/read-only/payload-none、typed host action、artifact、KatanA file IO の個別 evidence を割り当てる。KUC actual ContextMenu adapter 以前に KLE local menu を実装しない。
- [ ] 12.1c KatanA toolbar popup を pointer selection、focus change、outside editor click、diagnostic suppression、viewport clamp、code dropdown close に分解する。Escape は KatanA source に明示根拠が確認できないため、採用する場合は KUC generic keyboard behavior による user-mandated additional behavior と明示し、Katana source parity と偽装しない。
- [ ] 12.1d `find-replace-ui` を confirmed KatanA find/navigation/highlight と、user-mandated additional capability の visible replace/replace-all に分離して matrix/audit/Storybook を更新する。後者を v0.1.0 から削除せず、real provider capability、content effect、scroll/highlight、actual input/AccessKit/artifact、KLE-owned actual `KatanaApp` host-E2E evidence を同じ strict gate に置く。
- [ ] 12.1e text clipboard/cut/copy/paste、undo/redo、native IME composition、AccessKit を egui implicit behavior 又は KLE-only event injection から分離し、actual input と real document state を持つ individual runnable KLE-owned actual `KatanaApp` host-E2E case にする。Japanese/`⭐️` VS16/ZWJ/read-only/shortcut conflict を必須 case とする。
- [ ] 12.1f document lifecycle を `ContentChanged -> dirty -> preview/search/diagnostics refresh -> save/format -> workspace/document scoped undo` の actual KatanA host scenario として実証する。KLE contract、host simulator、source-only test のいずれも単独では completion evidence にしない。
- [ ] 12.1g KLE-owned host-E2E registration を検証する。KatanA reference の `integration/editor/mod.rs` 配下 source-only module、将来の KatanA adapter、又は `ui_integration_parallel.rs` registration を KLE parity の runnable evidence として登録してはならない。release gate は KLE `tools/katana-host-e2e` の実行結果と KatanA source revision/manifest snapshot を要求し、KatanA repo は編集しない。
- [ ] 12.1h `editor-shortcuts`、`image-ingest`、`dirty-save-refresh` の source inventory を KatanA 実装まで拡張する。最低限 `state/command_inventory/edit_commands.rs`、`state/shortcut_context.rs`、`shell_ui/shell_ui_shortcuts.rs`、`app/action/clipboard_image.rs`、`app/action/clipboard_file_url.rs`、`app/action/clipboard_image_macos.rs`、`app/action/process_markdown_formatting.rs`、`app/action/refresh_content.rs` と decisive function を `katana-parity-check` が source/audit/matrix 一対一で検査する。新しい source-only row を runnable evidence と誤認せず、KLE-owned actual `KatanaApp` host E2E 不在時は blocker のままにする。
- [ ] 12.1i search の visible-text/code-only と URL/HTML attribute exclusion、open focus/Enter/Shift+Enter/Up/Down/Escape/zero-result disabled transition、image-payload-over-text-paste と `file://` ingest/no-payload-or-error no-mutation、official diagnostic start-line/same-line aggregation/non-fixable display、Format extension/inline-selection/clipboard-payload enablement を個別 capability row にする。各 row は KatanA source path/line、KUC/KLE/KatanA ownership、public KLE RawInput/AccessKit/artifact、real KatanA host transition の evidenceを必要とし、visible control、source-only、Storybook-only を完了根拠にしない。
- [ ] 12.1j actual `RawInput::Paste` を KUC `ClipboardRequested` と opaque revision/correlation、host-owned document/selection/content snapshot resolution の controlled transaction として実装する。KLE は correlation を一度 forwarding するだけで token/document/content snapshot/char-range/payload を保持又は置換しない。host `Text` は KUC の一度の content transport を acknowledge し、image と画像 `file://` は `IngestClipboardImage` 一度だけを発火して host external change を待つ。KLE/KUC に OS clipboard、`file://`、extension、画像保存を実装しない。Japanese/`⭐️` VS16/ZWJ、通常 text/非画像 URL、payload-none/error、disabled/read-only、stale correlation、document switch、duplicate resolution を public RawInput と real host E2E の両方で厳格に検証し、AST で KLE/KUC の OS/image acquisition と persistent snapshot/range 混入を禁止する。
- [ ] 12.1k `katana-parity-check` を group-level selector 検査から source-derived leaf contract へ拡張する。少なくとも ContextMenu の root order `Save -> conditional Format -> Edit -> Ingest`、親子 path、Format visibility、enabled/read-only、14 direct authoring leaf、17 nested-code leaf、2 visible ingest leaf、physical secondary/keyboard/AccessKit route、KUC overflow refreshed-record invariants を別 leaf にする。clipboard acquisition (`clipboard_image.rs` / `clipboard_file_url.rs` / conditional macOS handler)、shortcut arbitration (`edit_commands.rs` / `shortcut_context.rs` / `shell_ui_shortcuts.rs`)、dirty/format refresh (`process_markdown_formatting.rs` / `refresh_content.rs`)、diagnostic dispatch と AccessKit の decisive source/function も source/audit/matrix 一対一で fail-closed にする。top-level group の green、source-only、Storybook-only、simulator-only、empty `remaining_gap` は leaf completion を満たさない。
- [ ] 12.1l `find-replace-ui` の KatanA find/navigation/highlight と user-mandated visible replace/replace-all を manifest、audit、Storybook、KUC contract 上で別 leaf にする。replace/replace-all を KatanA origin と偽装せず、KUC CommandChrome component、actual RawInput/AccessKit、content mutation、scroll/highlight、artifact、KatanA host-effect evidence を全て要求する。
- [/] 12.1m parity leaf manifest の各行に、KatanA `path:symbol` marker、visible/menu path、state condition、neutral relay 又は shortcut execution mode、KUC/KLE/KatanA owner、class-correct input-origin locator、expected actual KatanA document/state/file effect、executed KLE-owned host-E2E locator を構造化して保持する。`editor_direct_ui`/host-continuation/user-extension は public `RawInput -> EguiLanguageEditor::show`、KatanA-owned `editor_shortcut` は physical KatanA RawInput/router/action trace と same-run KUC root/AccessKit focus/retained input の相関を持ち、KLE mounted-causality を記録してはならない。どれかが欠ける leaf、top-level group の集約、shared selector、Storybook-only/source-only/simulator-only evidence を fail-closed にする。`docs/v0-1-0-editor-requirements.md` の 2026-08-14 leaf table を最小集合として、text/IME、document/history、toolbar lifecycle、context enablement、clipboard/image acquisition、search transition、diagnostic aggregation、shortcut priority、view/split persistence を別 row に維持する。2026-08-14 に Spark が schema の途中差分を残し、`cargo test -p katana-parity-check --locked` は `leaf_inventory_tests_rejections.rs` の sibling module import (`E0432`) で失敗した。さらに、途中差分の `visible_menu_path <- selector`、`state_condition <- "always"`、`typed_kle_request_or_state <- source marker`、`expected_actual_katana_effect <- host effect description` は leaf 固有の source evidence ではないため採用不可である。Spark 実行枠の回復後、compile blocker とこれらの共通テンプレートを先に除去し、各 leaf を exact source branch と executed evidence で個別に埋める。現時点の途中差分は完了根拠にしない。
- [/] 12.1n source-universe gate を先に完成させる。2026-08-14 に actual KatanA revision `4f6a6287c650a38633c7baeb544a92e739c68567` で editor 28、integration test 8、cross-module 21 の initial seed set を `docs/v0-1-0-katana-editor-source-universe.md` に記録したが、同日監査で `views/top_bar/search_logic.rs`、`views/app_frame/tab_toolbar.rs`、`app/action/{dispatch,dispatch_tertiary,process_helpers,process_diagnostics}.rs`、`app/{document,diff_review,preview}.rs`、`app_action{,_types}.rs`、state/theme/linter paths の到達漏れを発見した。従って「計57」は不正確であり、完了根拠ではない。Spark は revision/file SHA-256/AST branch catalog/leaf manifest の相互差分に加え、UI host、action dispatch/handler、data-refresh、generic presentation、test-oracle edge をたどる generated source closure を `katana-parity-check` へ実装する。各 file は decisive function/branch へ展開し、editor behavior leaf 又は非機能 helper の理由を一意に記録する。現行の `REQUIRED_LEAF_COUNT = 48` authoring-only manifest、absent `kle_downstream_adapter.rs` の deferred 扱い、shared actual-input selector/harness は全て不合格とする。missing reference file、未展開 decisive branch 又は closure edge を fail-closed に検出する。KatanA を編集して reference を合わせることは禁止する。
- [/] 12.1n0 `docs/v0-1-0-source-closure-operational-input-design.md` に従い、`SourceClosureInput` の versioned canonical non-generated input、固定 KatanA provenance、三 GitHub profile probe、raw evidence SHA、profile matrix merge、`generated_at_utc` の非同一性扱い、immutability、fail-closed lifecycle を先に契約化する。次の実装 batch は本設計の field-to-capture mapping と Windows/Linux 欠如・不一致 rejection を満たすまで materializer/六 artifact 実装へ進めない。設計書だけでは実装完了・三 OS 実行完了・UI/emoji parity 完了と扱わない。actual-root binding は `ActualReferenceVerifier` が supplied read-only git checkout の固定 revision、typed tree capture、実 bytes SHA、tree fingerprint を scan 前後に検証し、24件の source-closure 回帰テストで確認済み。2026-08-23: run-scoped `target/source-closure/<run-id>` staging、macOS/Windows/Linux の exact profile set、cross-run、partial、symlink、checksum rejection を実装し、assembled input path、validation receipt、independent loader verification まで確認済み。canonical publication は固定 revision の `artifacts/v0-1-0/source-closure-input/<fixed revision>` に temp sibling copy、再 hash、atomic rename を行い、mismatch/no-overwrite を拒否する。三 OS の実 capture、固定 KatanA host effect、emoji proof は未達。
- [/] 12.1n1 `docs/v0-1-0-parity-manifest-schema.md` に従う `source-closure.json`、`action-origins.json`、`branch-catalog.json`、`leaf-manifest.json`、`execution-record.json`、`storybook-artifacts.json` を `katana-parity-check` の唯一の canonical input/output として実装する。六つは KatanA revision/tree、KLE/KUC tree、user-mandated extension ledger、generator fingerprint を一致させる。source closure の unresolved edge、action origin の unresolved/duplicate/invalid extension、branch catalog の unclassified branch、leaf manifest の aggregate/shared selector/source-marker/state `always`、execution の event drop/duplicate/simulator/local completion、artifact の KUC opaque-root/proof/hash 不一致をすべて fail-closed にする。旧 static count や互換 manifest は同時には受理しない。2026-08-21 時点で `source-closure.json`、AST-derived `branch-catalog.json`、AST-derived `action-origins.json` の immutable triple publication は実装済みであり、cfg/if/match は全件 `unclassified`、action origin は definition/construction/dispatch/unique-handler/body/input-candidate/terminal-candidate の source fact を持つが、physical input、origin classification、inter-handler route、final host effect は未証明の `unresolved` のまま validator が reject する。profile schema は `katana_revision` を含むよう改訂し、fixed revision の read-only cloneから新形式macOS raw capture（`/tmp/kle-source-closure-20260821-r2`、profile fingerprint `sha256:e7b12c5636f48e98ba73c0d358f6ea91a20b4fc658a802a4ac3c22ad8d203280`）を再生成済みである。Windows/Linux profile が無いため provenance merge/materialization は意図どおり未実行である。leaf/execution/Storybook artifact の生成と canonical acceptance cutover は実装済みだが、actual canonical artifact が不在のため runtime runner は 3 blockers で fail-closed となる。2026-08-23: canonical immutable publication artifacts は固定 revision 配下に実装し、temp sibling copy + rehash + atomic rename と mismatch/no-overwrite rejection を確認した。assembled input path、validation receipt、independent loader verification も確認済み。source closure CI、3 OS capture、fixed host E2E、emoji proof は未達であり、本項は未完了のままとする。
  2026-08-28: validator は既知 leaf を参照する artifact の存在だけでなく、leaf-manifest の全 leaf に execution-record entry と Storybook artifact entry があり、各 `execution_id` / `storybook_stage_id` が一致することを必須化した。さらに各 leaf の `required_profile_ids` が execution と Storybook artifact の双方で macOS / Windows / Linux 全件を被覆しない場合を reject する。各 Storybook artifact は canonical root 相対の regular media file と実 SHA-256 一致も必須で、missing/tampered media、path traversal、symlink を reject する。missing leaf/profile coverage、stage mismatch、media mismatch の回帰を追加し、focused 15件・`katana-parity-check` 全208件・strict clippy を通過した。これは generated canonical artifact の完全性条件であり、未生成の actual three-profile artifact を補完するものではない。
- [/] 12.1n1a fixed-reference external UI dependency index は `egui` の lock package/source/checksum、AST-derived dependency symbol/span、KatanA AST invocation edgeを `source-closure.json`へ記録する。欠落・曖昧・未読込・macro/dynamic・非`Context` receiver・hash mismatch は `complete=false` と unresolved evidence に留め、visual/emoji parity、leaf、host effectを満たさない。2026-08-21にsource-closure focused 61 tests、strict clippy、changed-file rustfmtは通過したが、actual fixed referenceが5 invocation edgeを満たすか、三OSのtext/emoji実行証跡、leaf joinは未完了である。
- [/] 12.1n1b source/branch/action factsからdeterministic candidate identity/fingerprint/provenanceを作る generated leaf joinを追加した。KUC component、KLE opaque transit、actual host execution/effect、Storybook stageの不足はcandidateごとにunresolved evidenceへ残り、candidate statusはcanonical validatorで必ず拒否される。2026-08-21にsource-closure 71 testsとstrict clippyは通過したが、completed leaf、execution record、Storybook artifact、actual host effectは未実装である。
- [/] 12.1n2 `docs/v0-1-0-source-closure-generator-design.md` の六 phase を Rust `syn` AST、module/import/item index、conservative method/callback/macro resolution として実装する。`AppAction` は definition/dispatch/handler/final effect を別 edge にし、全 construction site を backward trace して action origin を一意分類する。`docs/v0-1-0-user-mandated-leaves.json` は唯一の non-source input として requirement anchor/KUC component/host route/fingerprint を検証し、wildcard/count/category/未登録 extension を拒否する。動的又は曖昧な edge を推測・握り潰ししない。全 direct source/action-route seed の mutation、alias/trait/macro/cfg/early-return/unresolved edge、artifact/event/effect fingerprint mismatch の fail-closed test を追加し、static `INVENTORY_FILES`、token scanning、deferred nonexistent adapter を同時に受理しない。2026-08-23: run-scoped staging、exact profile set/cross-run/partial/symlink/checksum rejection、assembled input、validation receipt、independent loader verification、canonical immutable publication の mismatch/no-overwrite rejectionを generator/materializer 側の静的契約として確認した。parent verification は git diff check、fmt、`katana-parity-check` 183 tests、AST lint、strict workspace lint、OpenSpec strict validation passed。source-closure CI の committed/pushed workflow 実行、三 OS actual capture、固定 KatanA host effect、emoji proof は未達。
- [ ] 12.1n3 KatanA `Cargo.lock` で固定された behavior-bearing external UI dependency を source closure に含める。少なくとも `egui 0.36.1` の package/source/checksum/source-file hash/symbol と KatanA `TextEdit::multiline`、`TextEdit::{load_state,store_state}`、`Event::Paste`、`Context::consume_shortcut` の invoking edge を `external_ui_semantic_dependency` として生成する。外部 UI API は native host-boundary の省略対象ではない。lock/source/symbol/leaf replacement の不一致又は未解決を fail-closed とし、KLE egui fallback、推測した既定挙動、source-only dependency test を受理しない。
- [ ] 12.1n4 source closure は KatanA document `TabBar` の mount、`tab_item`、context menu、drag、group header/popup、navigation と全 emitted `AppAction` を定義/dispatch/handler/final state・persistence effect まで辿る。document tab と workspace-tab shell を source span で区別し、各 select/close/restore/pin/reorder/group/collapse/inline-rename branch を individual leaf にする。dirty close の `pending_close_confirm` は KLE root UI origin を持たない `ForceCloseDocument` continuation として action-origins に `unmounted_editor_state_continuation` を記録し、pending-state/no-document-mutation leafを検証する。KUC generic `TabStrip` root record/AccessKit/actual input と KLE actual host E2E effect が揃うまで、tab group/operation count、source-only handler、fixture mutation、KLE local force-close dialogを受理しない。
- [/] 12.1n5 source closure は KatanA `StatusBar`、`ProblemsPanel`、scope、diagnostic renderer、bulk fixes、fix preview と全 emitted action を mount/input/definition/dispatch/handler/final state・review/browser/persistence effect まで辿る。ready/error/warning/success/info、dirty/activity、panel open/close、OpenTabs/ActiveTab、expand/collapse、official/hidden、zero/nonzero、location/docs/entry-file-visible fix、preview present/missing/invalid/truncated を individual leaf にする。KUC generic StatusStrip/DiagnosticsPanel root record/AccessKit/actual input と class-appropriate declared effect が揃うまで、status text count、source-only handler、simulator/fixture mutation を受理しない。panel/scope/disclosure は `kuc_retained_ui_effect` と unchanged host observation、jump/fix/docs は actual host E2E を必須とする。export activity 等の app-shell sibling は source span の非 editor rationale を必須とする。2026-08-26: local KUC に `EguiStatusBarAdapter` と `EguiDiagnosticsListAdapter` の Raster/AccessKit/action/compositor slice、`StatusDiagnosticsProjectionLease`、public facade-only root composition を追加した。Japanese/`⭐️`、generic status segment press、progress/icon/tooltip/popover、diagnostic severity/item/quickfix、virtualized viewport scroll と standard keyboard action を actual RawInput、AccessKit、one-shot opaque transportで確認した。diagnostic locator は表示 label/path/raw ID を出さない stable opaque identity を使い、offscreen targetを拒否する。generic per-entry code-diff fix preview は pointer/keyboard/AccessKit と outside/Escape close、one-shot root transportで確認した。generic stable-scope selector は label/path conversionなしに membership を絞り込み、pointer/keyboard/AccessKit、stale/disabled reject、root locator、one-shot transportまで確認した。KLE AST は Status/Diagnostics child model/action/event/adapter を拒否し、更新後の KLE Storybook 全30件と92-frame motion artifact gateが通過した。ただし全 individual leaf、fixed-host effect、3 OS emoji evidence は未実装であり、本項は完了ではない。
- [ ] 12.1n6 source closure は KatanA `central_content`、`SplitMode`/`PreviewOnly`、horizontal/vertical split、preview content、Markdown section route、KDV document-surface worker/frame/command/failure、KDV/KRR browser session/input/navigation/frame/failure、fullscreen/slideshow を mount/input/branch/typed event/host state/native boundary/test-oracle まで辿る。CodeOnly/PreviewOnly/Split、axis/order、resize/ratchet/persist、scroll/hover/source target/task、document/browser/fullscreen/slideshow を individual leaf とし、KatanA `Cargo.lock` resolved `katana-document-viewer 0.5.2` の package checksum/API symbol/file hash と non-rendering KLE-to-KUC projection probe を manifest 化する。Markdown text projection は KDV `PreviewSurfaceImage`/`ViewerNode.rect` を reject し semantic node/span/source target を KUC PlatformFontCatalog layout/raster/hit-test に接続する。local KDV `0.2.8` checkout は API evidence に使わない。KUC-to-KDV dependency、KLE local preview renderer/fixture bitmap、aggregate `preview` case、source-only effect は fail-closed とする。
- [ ] 12.1n7 source closure は `view_commands`、preview side tools/Tangochou、`AppAction::{ToggleSplitMode,ToggleCodePreview,SetViewMode,SetSplitDirection,SetPaneOrder,ToggleScrollSync,ToggleTaskList,ToggleSlideshow,ToggleSlideshowHoverHighlight,ToggleSlideshowShowDiagramControls,RefreshDiagrams}` と Escape/close route を definition/dispatch/handler/final state/native effect まで展開する。`RefreshDiagrams` は `shell_ui/shell_ui_update.rs` の theme-update/no-pending enqueue と manual virtual-document refresh の `editor_host_continuation` として backtrace し、pending preservation、cache reset、active/no-active、virtual manual/non-manual を individual leaf にする。`ToggleCodePreview` の三遷移、visible `SetViewMode` 三遷移、direction/order の両方向、sync の両方向、valid/stale task、windowed/already-fullscreen open、restore/preserve close、設定の両方向を individual leaf とし、aggregate cycle/control は受理しない。KLE/KUC は KatanA task parsing/global index/source offset/layout persistence を実装せず、KUC retained viewport leaves と host/native leaves を declared-effect class で分離する。さらに current KatanA が `katana-language-editor` を依存に持たず中央/横split/縦splitで legacy `EditorContent::new` を直接生成することを manifest admission gate に記録し、KatanA が read-only の間は KLE -> actual KatanA rendered-editor effect を成功として記録する execution record を fail-closed にする。
- [ ] 12.1n8 source closure は KatanA lock-resolved `egui 0.36.1` `TextEdit` external semantic dependency を `TextEdit::multiline` invocation から `EventFilter`、`events`、mutating-key handler、cursor-range navigation/pointer helper、IME/accessibility outputまで展開する。`Copy/Cut/Paste/Text/Enter/Tab/Shift+Tab/undo/redo/Delete/Backspace/Ctrl-H/K/U/W/selection navigation/pointer/IME` の every match arm/modifier/selection/read-only/OS predicate を individual leaf にする。KatanA が `code_editor()`/`lock_focus(true)` を使わず default `EventFilter { tab: false }` のため Tab/Shift+Tab は text indentation ではないことを manifest と test に固定し、KUC generic focus route/no-text-mutation と surrounding host routeを検証する。外部 crate source version/checksum/file hash/symbol の不一致、static family count、ASCII-only case、KLE local string/byte/clipboard/geometry implementation、egui fallbackは fail-closed とする。
- [ ] 12.1n9 `docs/v0-1-0-user-mandated-leaves.json` の `replace.current` と `replace.all` だけを `user_mandated_extension` origin として生成する。ただし固定 KatanA source に一般文書 Replace / Replace All の UI/action route が存在しないことを一次事実にし、`AppAction::ReplaceText` / `DocumentEditOps::handle_replace_text` は lint/review single-span route として分類する。ユーザー明示のテキスト置換は削除しないが、KatanA host 追加仕様判断が完了するまで release blocker とする。KLE match/range/byte conversion/content mutation、KLE local replace/search semantics、source-origin UI claim、KUC への Katana 固有 API、unregistered extension、aggregate replace-all count、Storybook-only callback を fail-closed にする。
- [ ] 12.1n10 source closure は KatanA `PreviewSidePanels` と TOC/export/story/tools submodules を direct editor root として展開する。TOC capability、pinned-left/pinned-right、hover open/dismiss/cooldown、empty-source、hierarchy leaf/parent selection、accordion disclosure、expand/collapse-all、CodeOnly/PreviewOnly/Split anchor-priority/fallback、`state/toc.rs` candidate stability/click suppression/generation/reset、active auto-scroll/vertical guide/empty-outline、rail seven control、Export/Story/Tools open/close/hover-delay/sibling branches、four export formats、story toggles/slideshow、tools commands、active/no-active metadata、TOC row/view-mode scroll guard を action origin/branch/leaf/effectまで個別に辿る。KUC retained panel/outline mechanics と actual host effect/UI input relay を declared-effect class で分離し、KLE local rail/panel/outline/timer/format/path/line/anchor/scroll/`TocPosition`/`TocState`/direct `AppState` mutation、shared hover/export selector、action-only fixtureを fail-closed にする。
- [ ] 12.1n11 source closure は current `CommandInventoryItem` 全件を availability、declared shortcut、`shell_ui_shortcuts` priority/context/reservation と action origin/handler/effectまで展開する。save/close/restore/document-search/refresh/image-ingest と全 authoring shortcutは individual `editor_shortcut` leaf、workspace/application-only commandは source-spanned `host_only` recordにする。一 action の menu/shortcut 複数 originを独立記録し、static count、action definitionだけ、shared keyboard trace、host-only省略を fail-closed にする。
- [ ] 12.1n12 source closure は `TabToolbar` の `Breadcrumbs` / `BreadcrumbMenu` と `UrlSourceBar` / `OpenUrl` を editor-frame root として展開する。breadcrumb は virtual final label、slash/backslash/empty segment、no-workspace label、workspace final/non-final、empty/nested directory candidate menu、file candidate selection/close を個別 branch とし、選択時は `SelectDocument -> dispatch -> handle_action_select_document -> DocumentOps::handle_select_document` の existing/new/image/preview/search/diagnostics/temporary-workspace continuationまで辿る。source address は text/preedit/focus、button submit、lost-focus Enter、blank rejection、history absent/open/select-without-submit と、`OpenUrl` の `file://` recognition、invalid URL、local validation/canonicalization/openability/canonical-url failure、local document/HTML/read failure、HTTP pending、HTML browser-source success/failure、binary surface success/failure、collector error/timeout/disconnect/multiple pendingを individual leaf とする。KLE path/URL/tree/history/payload/response parsing・state/fixture、direct `UrlTabState` mutation、KatanA/ehttp/KRR/browser mock、shared `OpenUrl` selectorを fail-closed にする。
- [/] 12.1n13 source closure は KatanA/KUC release matrix の `macos-latest`、`windows-latest`、`ubuntu-latest` ごとに実行する。profile record は actual `rustc -vV`、full `rustc --print cfg`、Cargo feature/dependency graph、lockfile/tree hash と active/inactive `cfg` edge を持ち、三 profile の classified union 以外を受理しない。少なくとも `clipboard_image_macos` mount、clipboard image macOS fallback/non-macOS absence、reveal-in-OS 三 branch、update installer macOS/non-macOS、production-only dispatch callback を source-span classified branch として保持する。profile-only macro/trait/callback/external UI edge、missing runner、default-host reuse、inactive branch omission、aggregate cross-platform result を fail-closed とする。各 generated artifact/execution/media stage は profile fingerprint を持ち、emoji leaf は profile-specific KUC resolved color face/file SHA-256 と isolated `⭐️` versus `☆` proof を必須とする。2026-08-23: workflow は macOS/Windows/Linux の三 artifact を明示的に個別 download し、merge-multiple を使用せず、同一 run の staging/canonical package を upload する構造を持つ。source-closure CI は committed/pushed workflow から実行しておらず、actual three-OS capture、fixed KatanA host effects、emoji proof は未達。parent verification の full `just check` は workspace tests 309件到達後、KUC Storybook compile 中の disk exhaustion で subsequent rerun が blocked となったため、pass と扱わない。
- [ ] 12.1o KatanA source-derived leaf と user-mandated visible replace/replace-all について `EditorError::Unsupported` を release-path から排除する。少なくとも `katana-language-editor-egui` の `unsupported_controls.rs` / `host_controls.rs` / `search_control.rs`、Storybook の `host_runtime_helpers.rs`、crate MVP description を AST で検出し、該当 capability が actual KUC component、KLE opaque one-time transit、KatanA host effect を持つまで fail-closed とする。`Unsupported` を期待する test、Floem future-work、source-only evidence は completion 根拠にならない。
- [ ] 12.2 各 KatanA editor feature を KLE neutral contract、egui adapter、KUC runtime、KatanA host adapter のいずれで満たすか再分類する
- [ ] 12.3 すべての feature に runnable 自動検証を割り当てる。Storybook 表示だけ、`Unsupported` のみ、docs のみ、または source-file existence で KatanA 既存機能の代替完了にしない
- [ ] 12.4 KLE-owned release evidence が、read-only local `katana-ui` を path dependency として用い、KLE public input から source-derived leaf ごとの declared effect を通せることを、全 feature の runnable evidence と open blocker がない状態でのみ通過する release blocker として検証する。`in_process_host_effect` / `native_external_host_effect` は既存 editor integration tests と同等の KatanA document/state/file effect を通し、`kuc_retained_ui_effect` は current KUC root/AccessKit transition と bootstrapped read-only KatanA no-mutation observation を通す。KatanA 側を編集して adapter を追加することは本作業の対象外とする。
- [/] 12.4a `tools/katana-downstream-check` で KatanA 風 downstream adapter が diagnostics/problems、navigation/view、document state を `katana-language-editor-egui` 経由で駆動できることを検証する。host-state simulator のみは actual KatanA host evidence ではないため、source-derived leaf E2E が未完了の間は部分完了とする。
- [/] 12.4c `tools/katana-downstream-check` で KLE action/report を KatanA host state 相当の view mode / split direction / scroll / document search / dirty-save / Problems scope / ingest / authoring / diagnostics state machine へ反映できることを検証する。source-derived KatanA leaf effect と physical KUC input が未証明の間は部分完了とする。
- [/] 12.4d `tools/katana-parity-check` で KatanA baseline の runnable wrapper command と source-only editor module を audit document と照合し、source-file existence を KLE integration evidence と誤認しないことを検証する。2026-08-27 に checker 自体の 192 tests は通過したが、canonical source-closure は dirty KLE provenance を fail closed し、fixed KatanA host bridge/effect evidence は未接続のため部分完了とする。
- [/] 12.4e `tools/katana-downstream-check` の host-state simulator で save / format / file image ingest / clipboard image ingest / clipboard file URL ingest / diagnostic fix-all / view toggle の host-owned side-effect intent が KLE action stream から欠落しないことを検証する。simulator evidence は source-derived actual KatanA side effect の代替ではない。
- [/] 12.4f `tools/katana-downstream-check` で KatanA 風 host event drain が `EditorEvent` の content / origin / cursor / selection / diagnostics / action を host state へ反映できることを検証する。actual KatanA state/file IO と source-derived leaf case が未接続の間は部分完了とする。
- [/] 12.4g `katana-parity-check` は Storybook/artifact work より先に KLE-owned declared-effect target、KatanA source revision/manifest snapshot、各 leaf の source/class-correct input origin/KUC root/AccessKit/class-appropriate effect 証拠を検査し、欠落なら release-check を停止する。`editor_direct_ui`/host-continuation/user-extension は KLE public RawInput、KatanA-owned `editor_shortcut` は physical KatanA router trace と same-run KUC focus record を用い、KLE mounted-causality を偽装しない。`kuc_retained_ui_effect` は bootstrapped KatanA no-mutation observation を必須とする。KatanA adapter module の不在を blocker と誤認しない。
- [ ] 12.4ac KLE-owned release harness は local read-only `katana-ui` を path dependency とし、input-origin class ごとの実経路を実行する。`editor_direct_ui` / `editor_host_continuation` / `user_mandated_extension` は public KUC root `RawInput` / AccessKit record、nonpersistent KLE opaque-transit、unchanged host target resolution、既存 read-only observer による実 KatanA buffer/dirty-save/authoring/search/diagnostic/document effect を revision/correlation で結合する。`editor_shortcut` は KatanA physical `RawInput` / shortcut router / action / handler effect と同じ run の KUC focus 又は retained-input record を結合し、KLE が router を駆動した、又は KLE が mount されたとは主張しない。`KatanaApp::trigger_action`、direct `AppAction` injection、KLE semantic action/event queue は禁止する。KUC-retained UI leaf は KatanA action を発行せず、valid bootstrap 後の unchanged host observation を記録する。KatanA revision/manifest と source snapshot を記録し、egui versions を隔離して release blocker として実行する。clipboard case は payload-free KUC request -> KLE opaque one-shot transit -> KatanA raw-image/file-list/image-file-URL/text acquisition priority、image asset/Markdown host external change、failure/stale/read-only no-mutation を証明する。`katana-parity-check` は未実行 class-appropriate evidence を fail とする。KatanA adoption は明示承認後の別 gate であり、本 task は KatanA を編集しない。
- [ ] 12.4ac1 release host-E2E は通常の mutable `tools/katana-host-e2e/Cargo.toml` を証拠に使わない。`docs/v0-1-0-external-host-e2e-design.md` の Fixed Read-Only Build Selection に従い、compiled manifest rootから導くstandard sibling checkoutのfixed detached clean revision、source-closure profile checksum、canonical provenance fingerprint、KatanA vendor patch source を検証してから隔離 harness をbuild/runする。`--katana-source`又は外部path selector、通常manifestへのfallback、KatanA worktree edit、source mismatch/dirty/profile missingのskipはfailとする。
- [ ] 12.4ad source-derived leaf を `kuc_retained_ui_effect`、`in_process_host_effect`、`native_external_host_effect` に明示分類し、`docs/v0-1-0-external-host-e2e-design.md` を実装契約として release gate に組み込む。KUC retained UI は current root/AccessKit transition と valid bootstrap 後の unchanged host observation を必須とし、KatanA action/direct AppState mutation を禁止する。現行 `ActionBridge` の `UnsupportedAction` / `ExternalHostIntentUnavailable`、headless `pending_dialog_action`、ignored OS clipboard test は完了証跡にしない。file picker、raw image、file list、percent-encoded image `file://` は、KatanA を編集せず隔離した macOS Accessibility/Pasteboard driver で actual KUC root RawInput -> nonpersistent KLE opaque transit -> unchanged KatanaApp host resolution/frame -> same-run asset/link/dirty/explorer 又は no-mutation を証明する。file picker は non-headless KatanA action と AX notification-driven separate worker により real dialog を選択し、`pending_dialog_action` や fixed sleep/retry loop を最終証跡にしない。固定待機・座標クリック・手動操作・KLE/KUC payload 注入・clipboard 非復元・skip は fail とする。
- [ ] 12.4ae KLE の release path から `EditorAction` と KatanA `AppAction` の意味的な total mapping を除去し、closed neutral opaque host-target resolution に置換する。KUC retained UI leaf（Problems panel/scope/disclosure、breadcrumb open/close、source-address edit/history、tab/rail/outline mechanics 等）は host mapping を持たず root transition/no-mutation record とする。host-effect leaf は KUC が host-projected opaque target/revision/correlation を発行し、KLE が非永続で一度だけ転送し、host だけが source-derived KatanA action/handler/effect を解決する。diagnostic fix/docs、gutter activate、scroll sync、select-and-jump、document selection、buffer edit、document search、text clipboard/history を個別 leaf として解決し、host-effect leaf に `UnsupportedAction` を許容しない。Replace / Replace All は固定 KatanA source に一般 UI/action route がないため、user-mandated extension blocker として KatanA host 追加仕様判断まで未完了にする。`HostCommand`、任意 authoring command string、`EditorAuthoringCommand` / `EditorCodeBlockKind` / `EditorAuthoringMenu*`、diagnostic `url`、`IngestClipboardFileUrls { urls }`、KLE persistent content/query/origin/line/anchor API を廃止する。authoring は host-projected opaque target/revision/correlation を一度だけ forward し、host だけが `MarkdownAuthoringOp` / `CodeBlockKind` を解決する。document edit、document search、viewport/gutter/select-and-jump、source-address、group-name submitted text は `EditorAction` 外の一回消費型 transport として exact KatanA route にだけ直送し、KLE は保存・解析・coordinate/range conversion・retry・artifact/log serialization をしない。payload-free ClipboardRequested -> opaque correlated host resolution -> host external document update/no-mutation transaction へ置換し、旧 release path を AST で拒否する。KatanA-specific action/payload/URL parsing は KLE/KUC に追加しない。
- [ ] 12.4ae1 source-closure generator は `AppAction` の definition/dispatch/handler だけで KLE feature を生成しない。全 reachable construction site を backward trace して `editor_direct_ui`、`editor_shortcut`、`editor_host_continuation`、`user_mandated_extension`、`unmounted_editor_state_continuation`、`host_only`、`unresolved` に一意分類した generated `action-origins.json` を出力する。`user_mandated_extension` は fingerprinted `docs/v0-1-0-user-mandated-leaves.json` の exact entry/KUC RawInput/AccessKit と、固定 source にrouteが無い場合の KatanA host 追加仕様判断 blocker を明示している時だけ許可する。dirty close の `CloseDocument -> pending_close_confirm -> ForceCloseDocument` は mandatory regression: current renderer/shortcut 無しの `ForceCloseDocument` を unmounted とし、KLE/KUC local confirmation UI を AST/source-manifest で拒否する。origin 不明、重複分類、action-only fixture、source span 無し、unregistered extension は fail とする。
- [ ] 12.4af `tools/katana-host-e2e` を action-only bridge から source-derived neutral relay へ置換する。direct-root leaf は public KUC RawInput/AccessKit record、KLE one-time nonpersistent transit、unchanged host resolution/effect を revision/correlation で結合する。ordinary user text transport は KLE が内容/origin/cursor/selection/range を保持せず `UpdateBuffer -> handle_update_buffer` へ一度だけ到達し、host external/disk update は KatanA -> KUC root の一方向 projection、clipboard は payload-free correlated host transaction、command/diagnostic/navigation/save/refresh/format は host-issued opaque target の host-only resolution とする。KatanA `editor_shortcut` は physical router mode と same-run KUC focus recordを結合し、KLE relay/mount causality を主張しない。silent drop、duplicate update、cross-document write、wrong-direction host external replay、direct `AppAction` injection、KLE content/query/range/action persistence を fail とし、downstream simulator/Storybook counter を evidence にしない。KLE `search_control.rs` の local `replace` / `replace_all` content mutation は削除対象だが、固定 KatanA source の一般 replace route 欠落を KLE/KUC local semantics で補完しない。visible replace / replace-all は KatanA host 追加仕様判断と actual host effect が揃うまで release blocker とする。
- [ ] 12.4af1 12.4ac/12.4ad/12.4af および既存 host "resolution" の表現を、source-derived physical KatanA UI input relay として実装する。KUC RawInput -> KLE opaque transit の後、host bridge は correlation/revision/source UI locator だけを記録し、unchanged KatanA の actual pointer/key/IME/AccessKit route を semantic dynamic targeting で駆動する。KatanA が自然に出した action/handler/frame/effect だけを記録し、`KatanaApp::trigger_action`、direct `AppAction`/`pending_action` assignment、action-map unit test、fixed coordinate、raw input replay without current target、KatanA adapter/patch を AST/E2E で拒否する。actual UI relay が到達不能な leaf は source span と理由を記録して release fail とし、KLE/KUC implementation 又は simulator で補完しない。
- [ ] 12.4af2 physical host-input driver は KatanA `FullOutput` から同一 frame の AccessKit snapshot を evidence-only に保持し、source manifest の role と host-projected accessible-name hash で current target を一意に発見してから event を送る。frame/node/role/name/bounds hash、source input span、natural action/handler/final effect を同じ execution record に結合する。`with_markdown_editor_document`、direct document/view fixture、`pending_action_for_test()`、pending `AppAction` を final result とする file URL/paste test は migration residue として置換し、physical workspace/document bootstrap と final asset/Markdown/dirty/no-mutation effect を検証する。KLE/KUC は coordinate、semantic input、`AppAction` を生成・保存せず、発見不能/曖昧/無効 target は fail-closed とする。
- [ ] 12.4ag `docs/v0-1-0-external-host-e2e-design.md` の Host Bootstrap Contract を実装する。release host-E2E は物理 workspace/document を KatanA source-derived workspace-open、document-selection、required view/split/search の physical UI routes で開いた後に KLE RawInput を開始し、bootstrap input/action/frame chain を execution record に残す。`app_state_mut()`、direct `Document` creation、`open_documents` / `active_doc_idx` / view/search/diagnostic state の直接変更、`AppAction::OpenWorkspace` 等の直接 action injection を `tools/katana-host-e2e` で AST 拒否し、read-only test hook は既実行 host effect の観察補助以外に使わない。
- [ ] 12.4ag1 macOS physical workspace bootstrap を `docs/v0-1-0-external-host-e2e-design.md` の Executable Physical-Bootstrap Plan に限定して実装する。fixed detached KatanA の通常 `KatanA` binary を別processで起動し、execution-local `CARGO_TARGET_DIR` / `KATANA_CONFIG_DIR` を強制する。OS AX API でKatanA windowと当該controlを前frameのsemantic role、固定KatanAに同梱される全localeの `menu.open_workspace` から生成したソート済み・重複なしのname digest集合、ownershipから一意解決し、real KatanA UI input -> real `rfd` folder dialog -> OS AX dialog selection -> real workspace scan completion -> fresh AX snapshotで対象Markdown tree entryをphysical select -> real document activationの順に証明する。config/workspace fixture/process/AX pre-post/final effectの各digestを一recordに結合し、fixed coordinate、sleep/poll retry、raw label/locale/path/text、default user config、direct `AppAction`/`pending_action`/`AppState` mutation、KatanA edit、missing AX authorizationのskipをfail-closedにする。正規3OS source closureが未取得であるため、このhost E2Eは未達であり、macOS実装やbootstrapの存在だけを完了根拠にしない。これはbootstrapだけであり、KLE token、Storybook、markdown/search/replace/emoji leafの完了根拠にはしない。Windows/Linux native driverとprofileは別未完了taskとして残す。
- [ ] 12.4ag1a physical bootstrap target は `KATANA_OPEN_WORKSPACE_ROLE` / `KATANA_OPEN_WORKSPACE_NAME` 等のraw環境変数、test fixture、user-configから受け取らない。fixed source-closure generator がrevision/profile/source-span付き role digest と、固定KatanAに同梱される全localeの `menu.open_workspace` から生成したソート済み・重複なしのname digest集合を含むtarget recordを発行し、host runnerはchild launch前にそれを検証してinternal locatorを構成する。正規3OS source closureが未取得であるため、このtarget recordによるhost E2Eは未達のまま扱い、raw label/PID/coordinate/path/localeをevidenceやpublic APIへ出す経路、未生成recordのmanual setup、stale/mismatch fallbackはAST/E2Eでfail-closedにする。
- [ ] 12.4ag1b physical native test の現行 `KATANA_FIXED_SOURCE` / `SOURCE_CLOSURE_PROFILE` / `KATANA_NATIVE_TARGET_RECORD` / `KATANA_WORKSPACE_FIXTURE` path env は外部path入力禁止に反するため release blocker とする。次実装は compiled-in canonical workspace layout と manifest root から導く標準 `target/source-closure`、および fixed ref を配置した standard run root のみを読み、native test 内部で一意の workspace fixture を生成する。profile/record の path、raw target/PID/coordinates/fixture path を env、CLI、user config、test fixture 引数から受け取らず、raw値を evidence/public API に出さない。3OS closure assembled artifact は同一 workflow run で protected macOS self-hosted runnerへ渡し、PR任意コードを同 runnerで実行しない（protected dispatch/release only）。runner protection、AX/input authorization、artifact transfer、source/profile/record mismatch、target lookup、final host effect の欠落は skip ではなく fail とする。workflow の構造実装は存在するが、protected actual native run、3OS closure artifact の同一run転送、AX complete bootstrap、final host effect が未証明であるため、本項は `[ ]` release blocker のまま維持する。
- [ ] 12.4ah KLE-owned release harness は KUC StatusStrip/DiagnosticsPanel の public RawInput/AccessKit event を class-appropriate evidence に接続する。ready/error/warning/success/info、dirty/activity、open/close、両 scope、expand/collapse/file disclosure、zero/nonzero、preview、hidden/disabled/stale は `kuc_retained_ui_effect` として same-root transition と unchanged host observation を実証する。location jump、entry/file/visible fix、docs は host-issued opaque target を一度だけ transit し、host alone が `SelectDocumentAndJump`、`ApplyLintFixesForFiles`、`OpenLinterDoc` と実 document/review/browser effect に解決する。KLE linter batch/path/URL/payload/action生成、直接 `AppState` mutation、simulator counter、source-only panel test は拒否する。
- [ ] 12.4ai KLE-owned release harness は KUC WorkspaceViewport/PreviewViewport の public RawInput/AccessKit event を source-derived KatanA view/preview evidence に接続する。generic resize/retained layout/overlay/focus leaves は same-root KUC transition と bootstrapped unchanged host observation を持ち、view mode/axis/order/scroll/select-and-jump/task/document-frame/browser/fullscreen/slideshow host leaves は exact KatanA state, KDV session or native effect を持つ。direct `AppState` mutation、KLE coordinate/scroll/anchor computation、KatanA egui preview copy、KDV private API/fixture bitmap、simulator counter、fixed wait は拒否する。
- [ ] 12.4aj KLE-owned release harness は KUC `BreadcrumbNavigator` / `SourceAddressBar` の public RawInput/AccessKit event を source-derived editor-frame evidence に接続する。breadcrumb virtual/no-workspace/final-label/menu-empty/nested-directory/file-selection と source text/preedit/history absent/open/select/button/lost-focus Enter/blank を individual `kuc_retained_ui_effect` root transition と unchanged bootstrapped host observationで証明する。file candidate は exact KatanA `SelectDocument` dispatch/handler/document/preview/search/diagnostics continuation、source submit は `file://` local document/HTML/error と real-HTTP pending/HTML/binary/error/timeout/disconnect/browser effect を個別に持つ。KLE は opaque target又は非観測 one-shot text transitだけを一度通し、host alone が actionを解決する。real loopback HTTP transport と real temporary file は許可するが、KatanA/ehttp/KRR/browser mock、direct `UrlTabState`/tree/document mutation、KLE URL/path/payload implementation、fixed wait、simulator counter は拒否する。
- [ ] 12.4ak KLE-owned release harness は document search の action-shape gap を actual KatanA UI host-input relay で閉じる。`DocSearchQueryChanged` が query payload を持たず既に `DocSearchBar` が更新した KatanA state を読むこと、close/Escape も UI state branch であることを source closure に固定する。KUC `SearchStrip` の query/close one-shot transport は KLE を non-Clone/non-Serialize で一度だけ通り、bootstrap 済み unchanged KatanA `DocSearchBar` の実 input/focus/close route へリレーされる。open/toggle/focus、empty/filter/nonempty-no-result、button/Enter/Shift+Enter/arrow directions、zero-match no navigation、wrap、Escape lost-focus predicate、editor/preview scroll を individual leaf にし、KUC RawInput/AccessKit、relay fingerprint、KatanA UI effect を join する。direct `SearchState`/`AppState` assignment、action-only query fixture、fixed coordinate/mock/raw-query artifact、KLE search parsing/range/cache/retry は AST/E2E で拒否し、actual UI relay が不能なら fail-closed とする。KatanA source edit は本 task の解決策ではない。
- [ ] 12.4al KLE-owned release harness は KUC `PreviewSideRail` / `OutlineNavigator` の public RawInput/AccessKit event を source-derived side-rail evidenceに接続する。TOC unavailable/available、pinned leading/trailing、hover open/dismiss/cooldown/empty-source、hierarchy leaf/parent selection、accordion disclosure、expand/collapse-all、active target source-priority/stability/click-suppression/generation/reset/auto-scroll/vertical guide/empty outline、each button pointer/keyboard/AX route、panel open/close/hover delay/sibling replacement/outside/Escapeは same-root retained transition と unchanged bootstrapped KatanA observationを持つ。refresh、search、HTML/PDF/PNG/JPG export、story setting/slideshow、tools view action、metadata、TOC row selectionは exact unchanged KatanA handler又は actual KatanA UI input relayの effectを持つ。host semantic timing is driven by deterministic monotonic input-frame timestamps, never sleep/retry. KLE/KUC direct `AppState` mutation、`TocPosition`/`TocState`/format/path/line/anchor payload、coordinate/timer fixture、mock/export counter、fixed waitはAST/E2Eで拒否する。
- [ ] 12.4am KLE-owned release harness は generated command-inventory shortcut leaves を physical key RawInput から KatanA の actual shortcut router/action/handler/effectまで実行する。KatanA は現 revision で KLE を mount しないため、shortcut leaf は `katana_shortcut_router` mode として、same-run の KUC root/AccessKit focus 又は retained-input record を revision/correlation で結合するが、KLE RawInput が router を駆動したとは主張しない。各 active context、availability、text-entry reservation、modifier specificity、first-match/no-double-dispatch、save/close/restore/search/refresh/image ingest/authoring shortcutを個別証明し、workspace/application shell commandは source-spanned host-only recordだけを持つ。KLE/KUC shortcut parser/router、shared keyboard fixture、direct action injection、static command count、mounted KLE integration claimは拒否する。
- [ ] 12.4i KLE-owned host-E2E で KUC root public input / opaque one-shot transit から view mode / select-and-jump / dirty-save-refresh / multi-document state を実 KatanA host state と file IO まで明示的に検証する。KLE は view/document/range semantics を構築せず、KatanA source は marker としてのみ使い、KatanA test module の追加はしない。
- [ ] 12.4j KLE-owned host-E2E の independent KatanA oracle と real buffer transform assertion で、KUC-origin opaque authoring target が KLE one-time transit を経て host-only `AppAction::AuthorMarkdown` 解決へ接続されることを検証する。KLE は authoring request enum/string/payload 又は direct action injection を持たない。
- [ ] 12.4k KLE-owned host-E2E で KUC-origin opaque target が host-only resolution を経て `ToggleCodePreview` / `ToggleSplitMode` / format route の KatanA 実 host state と markdown format file IO へ接続されることを検証する。KLE は action type、format semantics、file IO を持たない。
- [ ] 12.4l KLE-owned host-E2E で、日本語と exact `⭐️` VS16 が public KUC root input、nonpersistent KLE transport、KatanA 実 buffer の順に同じ code point sequence で保持されることを検証する。KLE font fallback/normalization/text buffer は存在させない。
- [ ] 12.4m KLE-owned host-E2E で、reference/read-only document state が write を拒否し、KatanA 実 buffer を変更しないことを検証する。
- [ ] 12.4n KLE-owned host-E2E の fresh-document cases で、source closure が生成した opaque authoring target/revision/correlation から KatanA host projection lookup と `AppAction::AuthorMarkdown` 経由で全 14 Markdown operation と全 17 `CodeBlockKind` が実 buffer を固定期待値どおり変換することを検証する。KLE `RunAuthoringCommand`、command string、code-kind enum 又は直接 `AppAction` injection は拒否する。
- [ ] 12.4o KLE-owned host-E2E で、KUC-origin opaque format target の unchanged KatanA 外部変更が workspace/document scoped `TextEdit` undo state に記録され、2文書それぞれが自分の before-format content へ戻ることを検証する。KLE `FormatDocument` API/semantic action は存在させない。
- [ ] 12.4p KLE-owned host-E2E で、KUC root の一回限り ordinary-content transport が KatanA 実 dirty state、preview hash、doc-search matches、diagnostics timestamp、save/file IO を更新することを検証する。KLE content event/state は完了根拠にしない。
- [ ] 12.4q KLE-owned host-E2E で、KUC diagnostic/problems の opaque target が unchanged host resolution を通じて KatanA 実 `AppAction::ApplyLintFixes` に接続され、lint-fix diff review 起動と confirm 後の実 buffer 更新まで行うことを検証する。KLE diagnostic action/payload は存在させない。
- [ ] 12.4r KLE-owned host-E2E で、public KUC RawInput が opaque authoring target/revision/correlation を一度だけ発行し、unchanged KatanA host authoring path が no-selection / selection-range の pending editor cursor restore を記録することを検証する。KLE の cursor state、command string 又は direct action injection は拒否する。
- [ ] 12.4s KLE-owned host-E2E で、KUC generic file-image opaque target と payload-free clipboard paste request が KatanA 実 `process_image_ingest` に接続され、asset file 生成、markdown link 挿入、dirty state、explorer refresh side effect を検証する。KLE image/clipboard-file-URL intent/payload/API は存在させない。clipboard image は raw image、file list、file URL、macOS pasteboard、supported extension、payload-none/error、read-only を host backend contract と実 host-E2E に分け、ignored live OS clipboard test を完了根拠にしない。
- [ ] 12.4b KLE-owned host-E2E が KUC root event の opaque one-shot transit と host-only resolution により KatanA `KatanaApp` の view mode / scroll / dirty-save / Problems scope へ反映することを検証する。KLE semantic event/action、direct `AppAction` injection、KatanA repository への adapter 追加は不要かつ本作業スコープ外とする。
- [ ] 12.4x KLE-owned host-E2E により、KUC public composition / IME commit から `⭐️` VS16 の KatanA 実 `KatanaApp` buffer 反映と dirty mark を検証する。KLE/KUC raw injection だけ又は KatanA source-only test は完了根拠にしない。
- [ ] 12.4h `tools/katana-parity-check` は source-closure leaf / execution の blocker が残る場合は fail し、open blocker を許容しない release readiness gate であることを保証する。checker はコンパイル可能で strict 184 tests pass だが、source closure artifacts、execution evidence、fixed host E2E が unavailable のため release gate は fail-closed のままであり、未達である。
- [/] 12.4t KLE-owned host-E2E で line-gutter visibility と code-block menu open/select/close を KatanA source marker、KLE public RawInput、real KatanA host effect の三点で検証する。候補名の列挙又は KatanA source-only test は証拠ではない。
- [/] 12.4u KLE-owned host-E2E で toolbar popup の selection/focus/outside editor click/viewport clamp/code dropdown close を三点証拠で検証する。KatanA `toolbar_popup` unit test は source marker の補助情報に限定する。
- [/] 12.4v KLE-owned host-E2E で context-menu / search / navigation / view modes / layout persistence を三点証拠で検証する。KatanA adapter module の有無を根拠にしない。
- [/] 12.4w KLE-owned host-E2E で select-and-jump / scroll-sync を KatanA 実 UI/coordinator effect まで検証する。
- [ ] 12.4y generated source closure で KatanA `layout.rs` / `syntax.rs` / `EditorConfig::syntax_highlighter` の actual editor reachability を先に fail-closed で判定する。runtime/macro/extern edge を含む到達 path がある場合だけ、その exact syntax branch を KLE public RawInput、KUC span/AccessKit record、actual KatanA host-E2E を持つ独立 leaf にする。到達不能が証明された定義は hash/span/rationale 付き helper として catalog に残し、syntax-rendered parity を主張しない。いずれの場合も KLE 独自 highlighter、egui layout、font/color fallback を追加しない。
- [ ] 12.4z KatanA の text clipboard / cut / copy / paste と `ShortcutMap` conflict/read-only behavior を独立 feature として KLE contract、actual host-E2E、release gate に追加する。KatanA `edit_commands` availability、Recording > Modal > Editor > Preview > Global context priority、editor text-entry shortcut preservation、modifier specificity、first-match-only dispatch を host-E2E で個別に検証し、KLE は host shortcut router を再実装しない。
- [ ] 12.4aa KatanA `editor/rendering.rs` の source-only tests を source marker として inventory に登録し、`katana-parity-check` が KLE public RawInput と actual host-E2E の実行を検査するよう更新する。KatanA test target の登録又は編集は要求しない。
- [ ] 12.4ab 日本語 / `⭐️` / ZWJ grapheme 選択で authoring transform と IME composition を実 KatanA UI input path から検証し、host-state hook や KLE-only event injection を唯一の完了根拠にしない
- [ ] 12.4ao `docs/v0-1-0-editor-interaction-source-audit.md` の固定KatanA source監査を実装契約へ反映する。document findのopen/query/next/prev/close、workspace Markdown search、line gutter、常設toolbar、selection floating toolbar、context menu、code-block menu全17種、file/clipboard image ingestを各UI trigger・handler・precondition・close/focus・host effectの個別leafとして登録し、source markerだけでは完了扱いにしない。KUCはgeneric input/text/raster/AccessKit/popup/menuを所有し、KLEはopaque composition/one-shot transitだけを所有し、KatanA固有の検索・authoring transform・document/file IOはhostに残す。
- [/] 12.4ao0 `docs/v0-1-0-fixed-katana-host-observability-design.md` に従い、`katana-host-e2e` は変更不能な固定KatanAのための disposable workspace/isolated config bootstrap、current-frame source-derived editor AccessKit locator、physical pointer/keyboard/AX activation、correlated external host-effect observerを所有する。in-process `KatanaApp`、`RawInput`、pending action、座標、fixed wait、KLE/KUC projection/payload injectionをevidence pathから拒否する。2026-08-23: phase 1 は固定 source の `GlobalWorkspaceState` JSON schemaで isolated `KATANA_CONFIG_DIR/workspace.json` を作成・write/readback検証し、native command前に必ず準備する。75 host library tests と full AST gate は親再検証済み。current-frame editor locator、physical input、external host effects、macOS/Windows/Linux artifactは未実装のまま。
- [/] 12.4ao0a 固定 revision の editor body は無名 `MultilineTextInput` で、親 `ScrollArea`/`Frame` も無名、`egui::Id` はnative AX identityではない。roleのみの選択は衝突するため、source-closure と host harness は geometry/document value/推測label/RawInput/internal idを使う editor locator を拒否し、native current-frame observation がsource-derived correlationを証明するまで `TargetMissing`/`TargetAmbiguous` を fail-closed blocker とする。2026-08-23: observation-only candidate audit はrole/name/description/ancestor/stateをredacted digestとして記録し、source revision/span/role不一致、無名候補、重複候補をtyped failureにする。native AX adapterはOpen Workspace button recordから分離した context-menu surface `MultilineTextInput` contractをgeneric `AXTextArea` translationへ接続し、run固有workspace basename相関、bounded redacted traversal、zero/multiple/attribute/unsupported typed failureを実装した。physical integrationは観測を呼ぶが、2026-08-23 parent実行ではsource-closure target artifact不在のため2 testが`target_record path does not exist`でfail-closed。実native candidate artifact、activation、physical input、external effectは未達であり、KLE/KUCまたは固定KatanAにlabel/semanticsを追加しない。
- [/] 12.4ao4 `docs/v0-1-0-search-host-boundary-design.md` に従い、KLE core/Storybook のlocal query match/range/regex/navigation/replace/document mutation/active-index/text stateを削除し、KUC generic search-control presentation/eventとKLEのopaque one-shot transitだけに移行する。KLE release は public input、one-shot receipt/replay rejection、disabled/no-mutation、`⭐️` U+2B50 U+FE0F を証明し、KLE/KUC/Storybookに host workaround を置かない。2026-08-23: KLE executable `find`/`find_next`/`replace`/`replace_all` と Storybook local algorithms/state/text hostを削除。AST search-host boundary ruleはlocal semantic state/algorithm/mutationとcore routeを拒否し、generic editing utilityはsearch routeから到達不能にする。Markdown-aware match/range/navigation/refresh/scroll/undo/replace の実 document effect は KatanA #336 の公開後 host E2E DoD とする。
- [/] 12.4ao5 `docs/v0-1-0-authoring-host-boundary-design.md` に従い、KLE core/egui/Storybook から `EditorAuthoringCommand`、`EditorCodeBlockKind`、authoring menu/cursor/selection state、authoring command/code-kind string mapping、`RunAuthoringCommand` と local Markdown transform を除去する。KUC generic command chrome/context menu/floating popup のhost-projected presentationとopaque one-shot eventだけを用い、KLEはreceipt/correlation以外の意味状態を持たない。2026-08-23: KLE core public authoring types/lifecycle/control/re-export、`RunAuthoringCommand`、`HostCommand`、`AuthoringTransform` origin、local command/code-kind labelsとStorybook KatanA action enumerationを削除し、Storybook contractをreceipt-onlyへ変更。authoring AST ruleは全KLE/KLE-egui/Storybook pathでcore type/mapping/transform/popup stateを拒否し、visual catalog/opaque receiptを許可する。parent/worker verification: core 20 tests、Storybook 42 tests、full formatter、full AST workspace、strict kle-linter clippy passed。KUC generic interactive leaf coverage、全14 operation/17 code kind/toolbar/context/floating lifecycleのphysical fixed KatanA buffer/undo/dirty/preview/file E2Eは未達。
- [/] 12.4ao6 `docs/v0-1-0-kle-opaque-host-action-boundary-design.md` に従い、残るKLE `EditorAction`/`EditorActionRequest`/`EditorActionSource`/`EditorActionControl`/`EditorEvent::ActionRequested` とaction payload（save/format/image/clipboard/diagnostic/problems/gutter/scroll/jump/view mode）を削除し、KLE/KLE-egui/StorybookをKUC rootのopaque one-shot receiptだけへ移行する。意味actionのcompatibility wrapper、KatanA `AppAction` mapping、document/path/URL/range/cursor/coordinate payload、counter-only successを追加しない。各固定source leafはphysical KatanA inputからfinal external effectまたはno-mutation typed blockerまでを別途host E2Eで証明する。2026-08-23: action semantic API removal is partial, not complete. KLE core/egui/Storybook `EditorAction` family and authoring, scroll/gutter/diagnostic/Problems/navigation/view semantic contracts were removed; action-host-boundary AST passes. Remaining fixed-host effects and full opaque boundary are unproven.
- [ ] 12.4ao1 固定KatanA sourceに一般文書Replace/Replace AllのUI/action routeが存在しないことをrelease gateで明示的に検出する。`AppAction::ReplaceText` と `DocumentEditOps::handle_replace_text` はlint/review等の単一span置換であり、一般検索置換の完了根拠にしない。KLE側のlocal `replace` / `replace_all`、query/range/content mutation、KatanA-specific action mappingを禁止し、host仕様が確定するまでReplace/Replace Allを未達blockerとして保持する。
  - [/] 2026-08-28: `katana-parity-check` の source-closure input は user-mandated JSON を hash のみで受理していたため、schema、固定2 leaf の順序、`katana_direct_ui_origin=false`、generic `SearchStrip`、単一span `ReplaceText` resolver、KLE ownership prohibition を strict に検証するよう更新した。さらに fixed KatanA source の全 Rust file を read-only scan し、`ReplaceText` の qualified/unqualified construct が action type と dispatch 以外に存在せず、`ReplaceAll` は全 source で不在、dispatch/handler は host-owned span replacement、document search に replace control がないことを fail-closed に検証する。実ドキュメントを読む regression を含め201 checker tests、strict clippy、AST lint が通過した。
  - [ ] 固定KatanA sourceから一般文書Replace/Replace Allのcontrol/action originが不在であることを生成closureで明示し、KatanA hostの追加仕様とactual host E2Eが成立するまでrelease blockerを解除しない。
- [ ] 12.4ao2 authoringの14操作（inline 4、structure 8、reference 2）と`CodeBlockKind` 17種を、selection/no-selection、reference/read-only、UTF-8/IME、cursor restore、dirty/undo/previewの実KatanA buffer effectまで個別検証する。toolbar/context/floating popupのpointer/keyboard/AccessKit、outside/Escape close、focus handoff、viewport clamp、diagnostic suppressionを分離し、KLE/KUC単体のaction・候補列挙・Storybook表示を完了証拠にしない。
- [ ] 12.4ao3 image ingestをfile picker、clipboard raw image、clipboard file list、percent-encoded image `file://`、ordinary text paste、payload-none/error、read-only、unsaved documentに分離し、asset file、Markdown link、dirty、Explorer refresh、no-mutation/errorを実hostで検証する。OS clipboard依存のignored test、`pending_action`、固定座標、fixed wait、手動操作、KLE/KUC payload注入は完了証拠にしない。

- [ ] 12.4ap KUC/KLE root integration design を固定する。KUC generic root と platform raster が owner であり、KLE は token / `show` / one-shot transit だけを所有する。現行KUC transportはopaque forwardだけ可能でhostがgeneric eventを一回消費するpublic dispatcherを持たないため、先にKUCへgeneric dispatch primitiveを追加する。KLE child render、font/emoji fallback、local search/replace semantics、host state mutation、KUCへのKatana固有API持込みは禁止する。現状は KUC generic root はあるが KLE main editor `show` と host effect の接続がなく、Storybook counter-only forwarder は不合格であることを design gate に記録する。2026-08-23: この前提のKUC slot-mount identityは実装済み。legacy tokenの両slot同居は同一familyとしてfail-closed、distinct versioned familyだけを許可し、retained root更新でtext/selection/scroll/focus/compositionを保持する。adapter全144件、KUC core 226件、format、adapter strict clippyは通過したが、KLE root integrationとactual host effectは未着手のため本項は未完了のままとする。2026-08-26: KUC `FullTextCommandSurfaceScenarioSession` を追加し、generic text/selection/search/replacement input の次frame projectionをKUC内でのみ保持する。physical IME commit後のone-shot dispatch、monotonic lease revision、Japanese/`⭐️` VS16/ZWJ の保持、replacement inputがtextを変更しないことをKUC testで固定した。KLE Storybookはこのsessionのopaque leaseのみをretain/synchronizeし、direct root context、counter-only router、fixture/presentation payloadをASTで拒否する。これはgeneric Storybook UIの入力維持に限り、KatanA search/replace/buffer/undo/file effectとfixed-host E2Eの未達を解消しない。
- [ ] 12.4aq KUC/KLE root integration implementation を行う。最初にKUCがopaque transportを一回だけgeneric text/search/command/context-menu dispatcherへ展開するpublic contractを実装し、host-issued opaque target/revision/correlation以外のKatana固有action/document/path/URL/range/replace semanticsを持ち込まない。KUC private sanitized style builderはgeneric `TextCommandSurfaceStyle` factoryへ移し、sanitized rootとStorybook hostが同じKUC theme/font/raster factoryを共有する。次にKLEはKUC `EguiTextCommandSurfaceRootFactory` のopaque tokenをdecode/clone/serialize/inspectせず、actual main editor `show`で受け取り、一回だけreal host forwarderへ渡す。KUC root frame record/hash、AccessKit record、KLE transit receipt、host effect correlationを同一execution correlationに結合し、KUC `SanitizedDocumentRootFactory` の再導入、KLE local renderer/fallback、fixture callback、counter-only adapterを追加しない。
- [ ] 12.4ar KUC/KLE root integration evidence を作成する。成功条件は actual main `show`、non-counter real host forwarder、KUC root / AccessKit / host effect が同じ correlation で結合されることである。valid KUC presentation tokenはhostがKUC generic modelから生成するため、KLE test/runtimeへchild model constructor又はopaque payload組立を持ち込まない。現行Storybook `main` はproviderを注入せず`MissingHostProjection`で停止するため、同じpublic frame loopへgeneric projectionとreal host forwarderを注入するまでlive harnessと扱わない。KLE binding leaf、provider注入済みStorybook live frame、provider不在`MissingHostProjection`、固定KatanA実hostの入力・文書効果・日本語/`⭐️` code-point保持を別々のstrict testで検証し、source inventory 全件を満たすまで完了扱いにしない。source/import/call、fixture callback、Storybook-only、screenshot/video、画面確認だけでは完了にしない。

---

## 13. v0.1.0 release

### 2026-08-23 Latest Verified Physical-Parity Status

- [/] Local gate progression passed: formatter/check, strict clippy,
  workspace tests (305), AST checks, KUC contract checks, and Storybook motion
  public-root checks.
- [/] `just check` remains failed because
  `katana-host-e2e-context-menu` returns fixed-host `UnavailableBridge`.
  There is no public fixed-KatanA document bootstrap/editor-target input route,
  and no fake bridge is allowed. The failure is fail-closed and is not parity
  evidence.
- [/] 2026-08-26: host-E2E source selection now requires an explicit
  `KATANA_REPO` Git worktree and rejects the former stale `/tmp` source path.
  Against fixed KatanA revision `4f6a6287c650a38633c7baeb544a92e739c68567`,
  `katana-repo-adapter-check` proves that KatanA lacks the KLE/KUC
  dependencies, downstream adapter wrapper/source, Linux/Windows mounts, and
  CI clone/ref wiring. The resulting `UnavailableBridge` is current evidence
  of absent real-host adoption, not a KLE parity success.
- [/] Three-profile source closure and actual fixed KatanA effects remain
  unproven. General document Replace / Replace All remains a host-contract
  blocker. No v0.1.0 release completion is claimed.
  2026-08-28: KatanA #336 confirms KUC v0.3.1 is an intermediate release.
  Downstream adoption requires the published KUC #22 generic full-editor root
  with macOS/Windows/Linux exact color-emoji evidence. A local KUC worktree,
  path/git dependency, or the v0.3.1 gate alone is not release readiness.
  On the same date, `.github/workflows/source-closure.yml` was present only as
  an untracked local file and `gh workflow list` had no `source-closure`
  workflow. Its native job also executes only `physical_open_workspace`, not
  the full editor leaf/E2E matrix. Neither the local workflow file nor an
  Open-Workspace smoke is accepted as three-profile closure or editor parity
  evidence. The local workflow now requires all capture, assembly, and native
  host jobs to resolve the tag derived from the workspace `katana-ui-core`
  dependency version (`v0.1.4` at this current path-dependency revision);
  static gate regressions reject a missing checkout or an unpinned `master`
  checkout. This checkout is CI dependency resolution and provenance only,
  never registry/public-release evidence; KLE must update the contract with its
  registry dependency in KLE issue #8.
  Publication now requires the materialized source-closure tree, copies it into
  the canonical source-closure publication, and verifies its tree fingerprint
  before and after the copy. The native parity command receives that exact
  canonical artifact directory rather than the checker default. This makes the
  generated source/branch/action evidence observable to the native gate while
  retaining failure for the still-absent generated leaf manifest, execution
  records, and Storybook media artifacts; partial source materialization is not
  accepted as parity.
  The materializer now serializes its source-derived leaf-join candidates as
  `leaf-manifest.json` with their `candidate`/unresolved facets preserved. This
  makes missing leaf ownership and evidence observable from canonical output;
  it does not synthesize terminal statuses, execution records, or Storybook
  media, so the strict validator continues to reject the incomplete closure.
  A local macOS capture against fixed KatanA revision
  `4f6a6287c650a38633c7baeb544a92e739c68567` recorded 1,093 Rust source files,
  155 active cfg edges, and 389 inactive cfg edges. It is one native profile
  only. Requesting `windows-latest` on that macOS host was rejected with a
  rustc-host mismatch, confirming that local OS masquerading cannot fill the
  Windows/Linux evidence gap.
  The local workflow contract now requires a subsequent
  `KATANA_REPO=../katana just katana-parity-check` step, and the checker rejects
  its removal. This closes the CI design gap but cannot provide execution
  evidence until the workflow is tracked, merged, and run against the required
  three-profile inputs and actual KatanA bridge. KLE issue #11 owns that
  tracked-workflow, execution, and full-host-E2E condition.
  2026-08-28: the source-closure validator now rejects a manifest unless every
  source-derived leaf has its declared execution and Storybook stage, complete
  macOS/Windows/Linux profile coverage, a relative regular numbered PNG stage
  under the artifact root, validated PNG chunk structure (IHDR/IDAT/IEND and
  CRC32), and a matching SHA-256. Missing leaf/stage/profile/media, invalid
  media format, and tampered media regressions are covered;
  `katana-parity-check` had 213 passing tests, with formatter, strict Clippy,
  AST lint, and OpenSpec strict validation passing. These are checker-contract
  tests only: no canonical full-leaf
  artifact, tracked three-profile workflow run, or actual KatanA host evidence
  exists yet.
  2026-08-28: the former workflow seed of only
  `views/panels/editor/text_edit.rs` was rejected as source-universe
  incomplete. `docs/v0-1-0-source-closure-roots.json` now fixes the reference
  revision and distinguishes the required direct editor/integration/tab-bar
  directories from individually declared app/state/preview roots. The
  assembler expands only those roots against the pinned KatanA checkout,
  canonicalizes/sorts the resolved Rust paths into `SourceClosureInput`, and
  rejects missing roots, symlinks, mixed `--seed-path` bypasses, or a workflow
  without the manifest. Two resolver regressions and a workflow mutation test
  cover this contract. A subsequent exact path comparison against the
  source-universe ledger added the four required KatanA test-oracle roots
  (`doc_search_tests`, `document_edit_tests`, and both diff-review test files);
  the deliberately nonexistent `tests/integration/editor/kle_downstream_adapter.rs`
  remains excluded and fail-closed. `katana-parity-check` has 216 passing tests, formatter,
  strict Clippy, AST lint, and OpenSpec strict validation passing. This is a
  source-root admission repair only: the generated closure still has candidate
  leaves and no three-profile execution/media or KatanA host evidence.
  The root admission now also captures the raw
  `v0-1-0-katana-editor-source-universe.md` bytes in provenance and carries
  their SHA-256 into every `ManifestRoot`. The fixed-revision root manifest
  stores the same SHA-256; assembly rejects a document/manifest/provenance
  mismatch before expanding any seed. A drift regression proves that changing
  one side alone is rejected. The source-universe capture and the fixture
  evidence writer were split into cohesive modules to retain the AST file-size
  boundary. `katana-parity-check` has 217 passing tests, formatter, strict
  Clippy, AST lint, and OpenSpec strict validation passing. This strengthens
  source authority only and remains separate from actual leaf execution,
  Storybook media, KUC release, and KatanA host adoption.
  The local `Justfile` recipes were also corrected from their stale
  parameterized single `--seed-path` invocation to the same root-manifest,
  source-universe, and fixed KatanA checkout contract used by CI. The release
  gate now rejects either recipe if provenance omits the source-universe input
  or assembly bypasses the manifest. The `just --list` parse check and the
  source-universe SHA-256 equality check passed; `katana-parity-check` has 218
  passing tests with formatter, strict Clippy, AST lint, and OpenSpec strict.
  The release-blocker audit also fails as required: `v0.1.0` has no remote tag,
  GitHub Release, or crates.io publication; every KUC dependency is still a
  local path source; and KatanA tag-dependent evidence is unavailable. The
  historical audit additionally recorded duplicate KDV follow-up issues #21, #22, and #23; KDV adoption is downstream and no longer participates in the KLE release audit.
  KDV is a sibling repository, so this KLE worktree does not modify or close
  them without an issue-owned downstream task.
  2026-08-28: source-universe document に記された各 Rust path は tokenized
  ledger との exact comparison で manifest expansion に含まれることを要求し、
  `editor/**`、`tests/integration/editor/**`、`top_bar/tab_bar/**` は individual
  file root へ劣化させず、対応する directory root の存在を必須化した。固定
  KatanA checkout との監査で未解決の document reference はゼロである。
  documented path omission と documented directory-root omission の回帰を含め、
  `katana-parity-check` は 220 passing tests、formatter、strict Clippy、AST
  lint、OpenSpec strict validation が通過した。さらに source-universe の
  `{dispatch,dispatch_secondary}.rs` のような brace route を実在 path に展開し、
  片方の root omission を同じ ledger validation で reject する。拡張後の
  `katana-parity-check` は 221 passing tests、formatter、strict Clippy、AST
  lint、OpenSpec strict validation が通過した。brace の一方だけを manifest
  expansion に残しもう一方の実在 file を外す regression も ledger validation
  が reject し、拡張後の `katana-parity-check` は 222 passing tests となった。
  `assemble-input` の CLI allowlist が Justfile/CI の `--source-universe` を
  誤って reject していたため修正し、同 option が次の必須引数検証へ到達する
  regression を追加した。fixed KatanA checkout で actual Justfile recipe を
  実行すると `unknown source-closure option` ではなく、未生成 run の
  `root provenance is missing` で fail-closed になることを確認した。修正後の
  `katana-parity-check` は 223 passing tests である。
  manifest の全 file root が source-universe の direct/brace source route に
  必ず根拠付けられる reverse validation と、checked-in document/manifest を
  結合する regression を追加した。資料にない root は reject し、既存 fixture
  でも missing-file が undocumented-root より先に診断されることを維持した。
  checked-in directory roots についても complete-directory marker を満たす
  regression を追加し、双方向契約後の `katana-parity-check` は 226 passing
  tests である。
  これは source-root admission の強化に限られ、terminal leaf execution、
  three-profile media、KUC 公開、actual KatanA host effect は未達のままである。
  2026-09-04: `SourceRequirementsLedger` を release gate に接続し、Editor Parity
  Requirements section で machine-resolvable な KatanA UI source が
  source-universe の direct root 又は explicit directory root に必ず包摂されることを
  検査するようにした。初回監査で workspace search の `search.rs` /
  `search_tabs/**`、slideshow の `modal.rs` / `controls.rs`、source-address の
  `state/url_tab.rs`、HTML browser の `content_html_browser.rs`、view-mode card の
  `tangochou.rs` が root manifest から漏れていることを検出した。fixed revision
  `4f6a6287c650a38633c7baeb544a92e739c68567` で全 source の実在を確認し、
  source-universe/root manifest/sha256 を更新した。相対 `app_frame`、`top_bar`、
  `preview_pane`、integration-test path は KatanA UI crate 内の明示 namespace として
  正規化し、KUC/KLE source や曖昧な basename を KatanA evidence として採用しない。
  source-closure suite 133 passed、strict Clippy、formatter、OpenSpec strict、
  `git diff --check` が通過した。`just release-verify` は workspace 369 passed、AST、
  KUC contract 3 passed、Unicode glyph contract 13 passed、motion contract 1 passed の後、
  canonical source-closure artifact 未生成で RC parity が fail-closed した。この修正は
  coverage admission の是正であり、terminal leaf、three-profile Storybook artifact、
  KUC #40、Floem #1086、KatanA #336 host E2Eのいずれも未達である。

- [/] 13.1 `cargo fmt` / `cargo clippy --workspace -- -D warnings` / `cargo test --workspace` が、KUC binding migration と fallback 削除を含む current worktree で通る。2026-08-23にformat check、`cargo clippy --workspace --all-targets --locked -- -D warnings`、`cargo test --workspace --locked -- --test-threads=1`（296 passed, 14 suites, 131.94s）を通過した。追加で `cargo llvm-cov report --summary-only --fail-under-lines 64` は 74.17% lines で通過した。2026-08-28 の source-root admission 更新後にも、出力を保存した `cargo test --workspace --locked -- --test-threads=1` は 14 suites / 361 passed / 0 failed で完走した。workspace Clippy の唯一の warning は KLE lint ではなく `katana-language-editor-floem -> floem -> wgpu -> metal -> block v0.1.6` の future-incompatibility である。`release-blocker-audit` は毎回まず `cargo check --workspace --all-targets --locked` を実行して Cargo report を fresh にした上で、これを `future-incompatibility` blocker として fail-closed にし、互換な dependency update を完了するまで v0.1.0 公開を許可しない。これはcurrent local worktreeの品質gateであり、fixed-host E2E、three-profile emoji、source closure execution、release publicationを通過した意味ではない。
  - 2026-09-04: pinned Floem main は `wgpu 27.0.1 -> metal 0.32.0 -> block 0.1.6` を通常 dependency として保持し、`--no-default-features` でも回避できない。latest compatible update が存在しないことを確認し、[Floem #1086](https://github.com/lapce/floem/issues/1086) に upstream fix を起票した。KLE local patch、warning suppression、feature-only graph removal は不許可である。
- [x] 13.2 `kle-linter` の `prohibited-color-literal` / KUC ownership / Storybook fallback を含む全ルールで違反ゼロ。2026-08-23に `cargo test -p kle-linter --locked` はunit 47件とworkspace AST 2件の計49件を通過し、`just ast-lint` も同じworkspace検査を通過した。compiled graph から外れた source を AST 対象外にする、`allow`/exclude を追加する、test expectation を弱める対応は禁止する。これはKLE側の静的境界合格であり、KUCの実raster/physical-input、fixed-host E2E、three-profile emojiの未証明を解消しない。
- [ ] 13.2a AST failure を責務移行と同じ batch で解消する。`platform_text_surface.rs` は分割して温存せず KUC root consumer へ置換する。`kuc_artifact_aggregate*.rs` は KUC opaque frame 到着後に消し、`direct_text_surface_actual_input_context_menu.rs` / `direct_text_surface_integration_tests.rs` は KUC root の leaf-scoped RawInput scenario に責務分割する。`tools/kle-storybook` の `window_renderer.rs` / `window_renderer_fallback.rs` / `window.rs` / `tests_motion.rs` と shape-count/minifb/fallback route は KUC composited frame encoder に置換する。`storybook_command_chrome_presentation.rs` の local palette/geometry は KUC presentation data へ移す。`leaf_inventory_*` は generated closure/leaf schema の cohesive module に再構成し、E0432/import、file/function length を lint 抑制なしで解消する。comment style と named constants は残った real module でのみ通常修正する。`diagnostics/visual.rs`、`localization.rs`、`clipboard_paste_control.rs` 等の残る責務違反も、同じ release gate で focused helper に分離する。
- [ ] 13.2b `docs/v0-1-0-current-implementation-migration-ledger.md` の current residue を owner ごとの atomic batch で移管する。KUC opaque root/catalog、KLE aggregate/local surface/search、Storybook compositor/fallback/palette、host bridge unsupported path、parity checker static/legacy adapter path を compatibility wrapper や source exclusion なしで置換する。各 row は replacement の strict test/AST 条件を通すまで physical deletion に進まず、旧経路を release evidence として受理しない。
- [x] 13.2c KUC `just ast-lint` を release gate として復旧する。2026-08-26 時点で `kal` が `i64` を超える literal の解析で panic していたため、同値の byte construction にして 469 件の実違反を可視化した。違反は magic-numbers 378、file-length 33、type-separation 21、pub-free-fn 13、comment-style 12、その他 12 であり、`docs/architecture/ui-separation/ast-lint-remediation-plan.md` に KUC generic ownership を壊さない分割設計を固定した。設定緩和、source root 除外、`allow`、fallback、KLE/KatanA 固有 API の KUC 流入は不受理とする。`source_address_strip` / `status_bar` / `diagnostics_list` / `tab_strip_retained` を types/raster/paint/interaction/adapter の cohesive module に順次分割した。2026-08-27 に KUC `just ast-lint`、`just check`（2,187 tests）、`just storybook-regression`（77 pages / 403 live interactions）、`just release-readiness-check` を通過した。これは generic KUC quality gate の完了であり、dirty KUC worktree の ownership/publication audit、released version の KLE pin、actual KatanA host effect は別の未完条件として残る。
- [/] 13.3 Storybook live harness と、KUC platform text-raster runtime を通る same-surface acceptance artifact が通る。2026-08-23に static root の120回反復をacceptance経路から除去し、`--live-acceptance-artifact` はKUC `FullTextCommandSurfaceMotionPlan` の完結した76-frame周期を必ず使うようにした。`target/acceptance/kle-storybook-live-acceptance/` にGIF/MP4/manifest/76 per-frame receiptを生成し、manifestで23種のPNG hashと76件のKUC provenanceを記録した。`cargo test -p kle-storybook --locked -- --test-threads=1` は28件通過。ただしこのartifactはKUC/KLE surface証跡に限定され、three-profile color emoji、platform font identity、fixed KatanA host E2Eの代替ではない。
  2026-08-28 に `storybook-motion-artifact-gate` を終了コード 0 で再実行し、92-frame の GIF/MP4/manifest と、KUC motion plan と idle-frame 非混入を照合する focused test（1 passed, 178.32s）を取得した。これは KLE/KUC Storybook の再現性証跡であり、KUC #22 の公開済み cross-platform color-glyph proof、three-profile source closure を満たしたことにはならない。KatanA #336 の real-host effect は KLE 公開後の downstream DoD である。
  同日の scenario manifest focused suite（20 passed）を再監査した結果、現行 motion catalog は document find、workspace search、14 authoring toolbar trigger、Replace blocker だけであり、KatanA 全 editor leaf を列挙していない。従って生成済み動画は partial local evidence に限定し、full Storybook / full parity の根拠から除外する。full Storybook は canonical source closure の全 leaf を input とし、各 leaf の KUC root frame、host transit、actual KatanA effect、artifactを同一 correlation で結べるまで未完了とする。
  誤解を招く pre-release の `--live-acceptance-artifact` CLI mode と
  `storybook-live-acceptance-artifact` recipe を削除した。残る
  `storybook-motion-artifact` は partial/non-release と明示し、削除した
  alias には回帰 test を追加した。重複した concurrent invocation を停止後、
  `kle-storybook` full suite は単独 process の完了を観測したが、実行基盤が
  terminal output/exit status を返さなかったため pass evidence には数えない。
  focused alias test、Storybook Clippy、AST lint は通過した。これは evidence
  naming の是正のみであり、full Storybook requirement を縮小しない。
- [ ] 13.4 release tag `v0.1.0` を切り GitHub Release を作成する（post-release 用 `just release-completion-audit` で tag / GitHub Release / crates.io 可視化を read-only 監査する。tag / Release / crates.io が未作成のため未完了。`just release-blocker-audit` は `remote-tag` / `github-release` / `crates.io-core` / `crates.io-egui` を同時に列挙し、現時点の失敗証跡は `docs/v0-1-0-release-readiness.md` に固定済み）。2026-09-04 に single registry crate `katana-ui-core@0.3.5` へ更新し、KLE は `egui` / `text-raster` feature を使用し、Storybook だけが `storybook-artifacts` を選択する。path/git KUC dependency は release path から除去済みである。KUC #34 の公開 API `write_opaque_variable_viewport` により、Katana editor の resize/IME/日本語/`⭐️`/search/Markdown 操作を一つの連続動画として提供する KLE Storybook を 46-frame full plan で検証した。KLE は split video、固定 viewport 入力改変、local raster/compositorで代替しない。`release-blocker-audit` は registry source、feature set、dependency resolution と future-incompatibility を fail-closed で監査する。
- [/] 13.4a R7 の KLE release gate は `docs/v0-1-0-release-integration-design.md` と `docs/v0-1-0-kle-release-evidence-design.md` に従う。`katana-parity-check --mode kle-release` は `downstream_required` と legacy `ExecutionRecord` 不在を要求し、canonical source-closure JSON 実バイト列 hash、profile matrix と profile ごとの fingerprint、leaf/stage/profile 全組、KUC opaque trace/media/AccessKit/one-shot receipt を結合する。候補の path/hash/symlink/coverage 偽装は拒否し、KUC #40 published producer が未提供の間は候補が整合していても必ず失敗する。`rc` は advisory precheck、`downstream-full`（互換 alias `full`）は公開後の legacy host evidence gate である。
- [ ] 13.5 [KatanA #336](https://github.com/HiroyukiFuruno/KatanA/issues/336) は KLE final `v0.1.0` 公開後、exact final registry dependency を採用して実 editor host E2E を行う downstream DoD とする。`downstream-full` は同じ leaf ごとの passing host evidence と legacy execution ID を要求するが、KLE final tag / GitHub Release / crates.io 公開の前提にはしない。KLE は公開済み version、migration note、source-derived requirement ledger と KLE release evidence を handoff artifact として提供する。
- [ ] 13.6 KDV 側に theme / i18n / typography / settings の preset がそろっていない場合は、KLE v0.1.0 公開後に KDV 側 Issue で整備する。KDV の採用状態、重複 Issue、preset 実装は KLE の tag / GitHub Release / crates.io 公開を待たせない。
