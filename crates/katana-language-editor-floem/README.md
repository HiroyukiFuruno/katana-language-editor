# katana-language-editor-floem

Floem 実装の骨組み crate（skeleton crate）です。

v0.1.0 では、`katana-language-editor` の中立 API（neutral API）と `floem` の git 固定の依存関係（dependency）が同時にコンパイルできることだけを保証します。本実装は v0.2.x で追加します。

`floem` は crates.io 公開版ではなく、`https://github.com/lapce/floem` の特定コミット（commit）から取り込みます。
